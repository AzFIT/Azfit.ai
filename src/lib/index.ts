// Barrel export for lib utilities
export { cn, formatElapsed, formatDate, formatNumber, calculateBMI, calculateBMR, calculateTDEE } from './utils';
export { supabase, type SupabaseClient } from './supabase';
export * from './storage';

-- Phase 72: exercise library dedup — SAFE-tier auto-merge
-- Generated by scripts/dedup-exercises.mjs from docs/exercise-dedup-report.md
-- ("## SAFE to auto-merge" table, 101 validated pairs).
-- One transaction: any error rolls the whole run back.
-- Idempotent: every pair is existence-guarded; a second run is a no-op.
-- Referencing tables handled (discovered via pg_constraint, Phase 72 Step 1):
--   exercise_library_muscles / exercise_library_equipment (composite-PK junctions,
--   ON DELETE CASCADE) and trial_assessment_items (repointed before delete).
-- workout_log_entries.exercise_id references public.exercises (program rows),
-- NOT exercise_library — verified; name snapshots stay valid (exact-name pairs).
BEGIN;

DO $$
DECLARE
  keep_id uuid;
  absorb_id uuid;
BEGIN
  SELECT id INTO keep_id FROM public.exercise_library WHERE code = 'EX0018';
  SELECT id INTO absorb_id FROM public.exercise_library WHERE code = 'EX0267';
  IF keep_id IS NULL OR absorb_id IS NULL THEN
    RAISE NOTICE 'dedup72: SKIP EX0018/EX0267 (row missing — already merged or renamed)';
    RETURN;
  END IF;

  -- (a) metadata merge: fill NULL/empty keep content columns from absorb; never overwrite
  UPDATE public.exercise_library k
  SET
    primary_muscle   = COALESCE(NULLIF(k.primary_muscle, ''), a.primary_muscle),
    secondary_muscle = COALESCE(NULLIF(k.secondary_muscle, ''), a.secondary_muscle),
    equipment        = COALESCE(NULLIF(k.equipment, ''), a.equipment),
    difficulty       = COALESCE(k.difficulty, a.difficulty),
    exercise_type    = COALESCE(NULLIF(k.exercise_type, ''), a.exercise_type),
    type             = COALESCE(NULLIF(k.type, ''), a.type),
    met_value        = COALESCE(k.met_value, a.met_value),
    description      = COALESCE(NULLIF(k.description, ''), a.description),
    safety_notes     = COALESCE(NULLIF(k.safety_notes, ''), a.safety_notes),
    youtube_url      = COALESCE(NULLIF(k.youtube_url, ''), a.youtube_url),
    image_url        = COALESCE(NULLIF(k.image_url, ''), a.image_url),
    updated_at       = now()
  FROM public.exercise_library a
  WHERE k.id = keep_id AND a.id = absorb_id;

  -- (b) junction merge: unique absorb tags survive (composite PKs dedupe)
  INSERT INTO public.exercise_library_muscles (exercise_library_id, muscle_name, is_primary)
  SELECT keep_id, m.muscle_name, m.is_primary
  FROM public.exercise_library_muscles m
  WHERE m.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  INSERT INTO public.exercise_library_equipment (exercise_library_id, equipment_type_id)
  SELECT keep_id, e.equipment_type_id
  FROM public.exercise_library_equipment e
  WHERE e.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  -- (c) repoint non-junction FK references
  UPDATE public.trial_assessment_items
  SET exercise_library_id = keep_id
  WHERE exercise_library_id = absorb_id;

  -- (d) delete absorb row (its remaining junction rows cascade)
  DELETE FROM public.exercise_library WHERE id = absorb_id;
  RAISE NOTICE 'dedup72: merged EX0267 -> EX0018 (dips)';
END $$;

DO $$
DECLARE
  keep_id uuid;
  absorb_id uuid;
BEGIN
  SELECT id INTO keep_id FROM public.exercise_library WHERE code = 'EX0030';
  SELECT id INTO absorb_id FROM public.exercise_library WHERE code = 'EX0228';
  IF keep_id IS NULL OR absorb_id IS NULL THEN
    RAISE NOTICE 'dedup72: SKIP EX0030/EX0228 (row missing — already merged or renamed)';
    RETURN;
  END IF;

  -- (a) metadata merge: fill NULL/empty keep content columns from absorb; never overwrite
  UPDATE public.exercise_library k
  SET
    primary_muscle   = COALESCE(NULLIF(k.primary_muscle, ''), a.primary_muscle),
    secondary_muscle = COALESCE(NULLIF(k.secondary_muscle, ''), a.secondary_muscle),
    equipment        = COALESCE(NULLIF(k.equipment, ''), a.equipment),
    difficulty       = COALESCE(k.difficulty, a.difficulty),
    exercise_type    = COALESCE(NULLIF(k.exercise_type, ''), a.exercise_type),
    type             = COALESCE(NULLIF(k.type, ''), a.type),
    met_value        = COALESCE(k.met_value, a.met_value),
    description      = COALESCE(NULLIF(k.description, ''), a.description),
    safety_notes     = COALESCE(NULLIF(k.safety_notes, ''), a.safety_notes),
    youtube_url      = COALESCE(NULLIF(k.youtube_url, ''), a.youtube_url),
    image_url        = COALESCE(NULLIF(k.image_url, ''), a.image_url),
    updated_at       = now()
  FROM public.exercise_library a
  WHERE k.id = keep_id AND a.id = absorb_id;

  -- (b) junction merge: unique absorb tags survive (composite PKs dedupe)
  INSERT INTO public.exercise_library_muscles (exercise_library_id, muscle_name, is_primary)
  SELECT keep_id, m.muscle_name, m.is_primary
  FROM public.exercise_library_muscles m
  WHERE m.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  INSERT INTO public.exercise_library_equipment (exercise_library_id, equipment_type_id)
  SELECT keep_id, e.equipment_type_id
  FROM public.exercise_library_equipment e
  WHERE e.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  -- (c) repoint non-junction FK references
  UPDATE public.trial_assessment_items
  SET exercise_library_id = keep_id
  WHERE exercise_library_id = absorb_id;

  -- (d) delete absorb row (its remaining junction rows cascade)
  DELETE FROM public.exercise_library WHERE id = absorb_id;
  RAISE NOTICE 'dedup72: merged EX0228 -> EX0030 (reverse machine shoulder press)';
END $$;

DO $$
DECLARE
  keep_id uuid;
  absorb_id uuid;
BEGIN
  SELECT id INTO keep_id FROM public.exercise_library WHERE code = 'EX0030';
  SELECT id INTO absorb_id FROM public.exercise_library WHERE code = 'EX0456';
  IF keep_id IS NULL OR absorb_id IS NULL THEN
    RAISE NOTICE 'dedup72: SKIP EX0030/EX0456 (row missing — already merged or renamed)';
    RETURN;
  END IF;

  -- (a) metadata merge: fill NULL/empty keep content columns from absorb; never overwrite
  UPDATE public.exercise_library k
  SET
    primary_muscle   = COALESCE(NULLIF(k.primary_muscle, ''), a.primary_muscle),
    secondary_muscle = COALESCE(NULLIF(k.secondary_muscle, ''), a.secondary_muscle),
    equipment        = COALESCE(NULLIF(k.equipment, ''), a.equipment),
    difficulty       = COALESCE(k.difficulty, a.difficulty),
    exercise_type    = COALESCE(NULLIF(k.exercise_type, ''), a.exercise_type),
    type             = COALESCE(NULLIF(k.type, ''), a.type),
    met_value        = COALESCE(k.met_value, a.met_value),
    description      = COALESCE(NULLIF(k.description, ''), a.description),
    safety_notes     = COALESCE(NULLIF(k.safety_notes, ''), a.safety_notes),
    youtube_url      = COALESCE(NULLIF(k.youtube_url, ''), a.youtube_url),
    image_url        = COALESCE(NULLIF(k.image_url, ''), a.image_url),
    updated_at       = now()
  FROM public.exercise_library a
  WHERE k.id = keep_id AND a.id = absorb_id;

  -- (b) junction merge: unique absorb tags survive (composite PKs dedupe)
  INSERT INTO public.exercise_library_muscles (exercise_library_id, muscle_name, is_primary)
  SELECT keep_id, m.muscle_name, m.is_primary
  FROM public.exercise_library_muscles m
  WHERE m.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  INSERT INTO public.exercise_library_equipment (exercise_library_id, equipment_type_id)
  SELECT keep_id, e.equipment_type_id
  FROM public.exercise_library_equipment e
  WHERE e.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  -- (c) repoint non-junction FK references
  UPDATE public.trial_assessment_items
  SET exercise_library_id = keep_id
  WHERE exercise_library_id = absorb_id;

  -- (d) delete absorb row (its remaining junction rows cascade)
  DELETE FROM public.exercise_library WHERE id = absorb_id;
  RAISE NOTICE 'dedup72: merged EX0456 -> EX0030 (reverse machine shoulder press)';
END $$;

DO $$
DECLARE
  keep_id uuid;
  absorb_id uuid;
BEGIN
  SELECT id INTO keep_id FROM public.exercise_library WHERE code = 'EX0053';
  SELECT id INTO absorb_id FROM public.exercise_library WHERE code = 'EX0305';
  IF keep_id IS NULL OR absorb_id IS NULL THEN
    RAISE NOTICE 'dedup72: SKIP EX0053/EX0305 (row missing — already merged or renamed)';
    RETURN;
  END IF;

  -- (a) metadata merge: fill NULL/empty keep content columns from absorb; never overwrite
  UPDATE public.exercise_library k
  SET
    primary_muscle   = COALESCE(NULLIF(k.primary_muscle, ''), a.primary_muscle),
    secondary_muscle = COALESCE(NULLIF(k.secondary_muscle, ''), a.secondary_muscle),
    equipment        = COALESCE(NULLIF(k.equipment, ''), a.equipment),
    difficulty       = COALESCE(k.difficulty, a.difficulty),
    exercise_type    = COALESCE(NULLIF(k.exercise_type, ''), a.exercise_type),
    type             = COALESCE(NULLIF(k.type, ''), a.type),
    met_value        = COALESCE(k.met_value, a.met_value),
    description      = COALESCE(NULLIF(k.description, ''), a.description),
    safety_notes     = COALESCE(NULLIF(k.safety_notes, ''), a.safety_notes),
    youtube_url      = COALESCE(NULLIF(k.youtube_url, ''), a.youtube_url),
    image_url        = COALESCE(NULLIF(k.image_url, ''), a.image_url),
    updated_at       = now()
  FROM public.exercise_library a
  WHERE k.id = keep_id AND a.id = absorb_id;

  -- (b) junction merge: unique absorb tags survive (composite PKs dedupe)
  INSERT INTO public.exercise_library_muscles (exercise_library_id, muscle_name, is_primary)
  SELECT keep_id, m.muscle_name, m.is_primary
  FROM public.exercise_library_muscles m
  WHERE m.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  INSERT INTO public.exercise_library_equipment (exercise_library_id, equipment_type_id)
  SELECT keep_id, e.equipment_type_id
  FROM public.exercise_library_equipment e
  WHERE e.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  -- (c) repoint non-junction FK references
  UPDATE public.trial_assessment_items
  SET exercise_library_id = keep_id
  WHERE exercise_library_id = absorb_id;

  -- (d) delete absorb row (its remaining junction rows cascade)
  DELETE FROM public.exercise_library WHERE id = absorb_id;
  RAISE NOTICE 'dedup72: merged EX0305 -> EX0053 (seated cable fly lower fibres)';
END $$;

DO $$
DECLARE
  keep_id uuid;
  absorb_id uuid;
BEGIN
  SELECT id INTO keep_id FROM public.exercise_library WHERE code = 'EX0054';
  SELECT id INTO absorb_id FROM public.exercise_library WHERE code = 'EX0306';
  IF keep_id IS NULL OR absorb_id IS NULL THEN
    RAISE NOTICE 'dedup72: SKIP EX0054/EX0306 (row missing — already merged or renamed)';
    RETURN;
  END IF;

  -- (a) metadata merge: fill NULL/empty keep content columns from absorb; never overwrite
  UPDATE public.exercise_library k
  SET
    primary_muscle   = COALESCE(NULLIF(k.primary_muscle, ''), a.primary_muscle),
    secondary_muscle = COALESCE(NULLIF(k.secondary_muscle, ''), a.secondary_muscle),
    equipment        = COALESCE(NULLIF(k.equipment, ''), a.equipment),
    difficulty       = COALESCE(k.difficulty, a.difficulty),
    exercise_type    = COALESCE(NULLIF(k.exercise_type, ''), a.exercise_type),
    type             = COALESCE(NULLIF(k.type, ''), a.type),
    met_value        = COALESCE(k.met_value, a.met_value),
    description      = COALESCE(NULLIF(k.description, ''), a.description),
    safety_notes     = COALESCE(NULLIF(k.safety_notes, ''), a.safety_notes),
    youtube_url      = COALESCE(NULLIF(k.youtube_url, ''), a.youtube_url),
    image_url        = COALESCE(NULLIF(k.image_url, ''), a.image_url),
    updated_at       = now()
  FROM public.exercise_library a
  WHERE k.id = keep_id AND a.id = absorb_id;

  -- (b) junction merge: unique absorb tags survive (composite PKs dedupe)
  INSERT INTO public.exercise_library_muscles (exercise_library_id, muscle_name, is_primary)
  SELECT keep_id, m.muscle_name, m.is_primary
  FROM public.exercise_library_muscles m
  WHERE m.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  INSERT INTO public.exercise_library_equipment (exercise_library_id, equipment_type_id)
  SELECT keep_id, e.equipment_type_id
  FROM public.exercise_library_equipment e
  WHERE e.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  -- (c) repoint non-junction FK references
  UPDATE public.trial_assessment_items
  SET exercise_library_id = keep_id
  WHERE exercise_library_id = absorb_id;

  -- (d) delete absorb row (its remaining junction rows cascade)
  DELETE FROM public.exercise_library WHERE id = absorb_id;
  RAISE NOTICE 'dedup72: merged EX0306 -> EX0054 (seated cable fly mid fibres)';
END $$;

DO $$
DECLARE
  keep_id uuid;
  absorb_id uuid;
BEGIN
  SELECT id INTO keep_id FROM public.exercise_library WHERE code = 'EX0055';
  SELECT id INTO absorb_id FROM public.exercise_library WHERE code = 'EX0307';
  IF keep_id IS NULL OR absorb_id IS NULL THEN
    RAISE NOTICE 'dedup72: SKIP EX0055/EX0307 (row missing — already merged or renamed)';
    RETURN;
  END IF;

  -- (a) metadata merge: fill NULL/empty keep content columns from absorb; never overwrite
  UPDATE public.exercise_library k
  SET
    primary_muscle   = COALESCE(NULLIF(k.primary_muscle, ''), a.primary_muscle),
    secondary_muscle = COALESCE(NULLIF(k.secondary_muscle, ''), a.secondary_muscle),
    equipment        = COALESCE(NULLIF(k.equipment, ''), a.equipment),
    difficulty       = COALESCE(k.difficulty, a.difficulty),
    exercise_type    = COALESCE(NULLIF(k.exercise_type, ''), a.exercise_type),
    type             = COALESCE(NULLIF(k.type, ''), a.type),
    met_value        = COALESCE(k.met_value, a.met_value),
    description      = COALESCE(NULLIF(k.description, ''), a.description),
    safety_notes     = COALESCE(NULLIF(k.safety_notes, ''), a.safety_notes),
    youtube_url      = COALESCE(NULLIF(k.youtube_url, ''), a.youtube_url),
    image_url        = COALESCE(NULLIF(k.image_url, ''), a.image_url),
    updated_at       = now()
  FROM public.exercise_library a
  WHERE k.id = keep_id AND a.id = absorb_id;

  -- (b) junction merge: unique absorb tags survive (composite PKs dedupe)
  INSERT INTO public.exercise_library_muscles (exercise_library_id, muscle_name, is_primary)
  SELECT keep_id, m.muscle_name, m.is_primary
  FROM public.exercise_library_muscles m
  WHERE m.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  INSERT INTO public.exercise_library_equipment (exercise_library_id, equipment_type_id)
  SELECT keep_id, e.equipment_type_id
  FROM public.exercise_library_equipment e
  WHERE e.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  -- (c) repoint non-junction FK references
  UPDATE public.trial_assessment_items
  SET exercise_library_id = keep_id
  WHERE exercise_library_id = absorb_id;

  -- (d) delete absorb row (its remaining junction rows cascade)
  DELETE FROM public.exercise_library WHERE id = absorb_id;
  RAISE NOTICE 'dedup72: merged EX0307 -> EX0055 (seated cable fly upper fibres)';
END $$;

DO $$
DECLARE
  keep_id uuid;
  absorb_id uuid;
BEGIN
  SELECT id INTO keep_id FROM public.exercise_library WHERE code = 'EX0060';
  SELECT id INTO absorb_id FROM public.exercise_library WHERE code = 'EX0320';
  IF keep_id IS NULL OR absorb_id IS NULL THEN
    RAISE NOTICE 'dedup72: SKIP EX0060/EX0320 (row missing — already merged or renamed)';
    RETURN;
  END IF;

  -- (a) metadata merge: fill NULL/empty keep content columns from absorb; never overwrite
  UPDATE public.exercise_library k
  SET
    primary_muscle   = COALESCE(NULLIF(k.primary_muscle, ''), a.primary_muscle),
    secondary_muscle = COALESCE(NULLIF(k.secondary_muscle, ''), a.secondary_muscle),
    equipment        = COALESCE(NULLIF(k.equipment, ''), a.equipment),
    difficulty       = COALESCE(k.difficulty, a.difficulty),
    exercise_type    = COALESCE(NULLIF(k.exercise_type, ''), a.exercise_type),
    type             = COALESCE(NULLIF(k.type, ''), a.type),
    met_value        = COALESCE(k.met_value, a.met_value),
    description      = COALESCE(NULLIF(k.description, ''), a.description),
    safety_notes     = COALESCE(NULLIF(k.safety_notes, ''), a.safety_notes),
    youtube_url      = COALESCE(NULLIF(k.youtube_url, ''), a.youtube_url),
    image_url        = COALESCE(NULLIF(k.image_url, ''), a.image_url),
    updated_at       = now()
  FROM public.exercise_library a
  WHERE k.id = keep_id AND a.id = absorb_id;

  -- (b) junction merge: unique absorb tags survive (composite PKs dedupe)
  INSERT INTO public.exercise_library_muscles (exercise_library_id, muscle_name, is_primary)
  SELECT keep_id, m.muscle_name, m.is_primary
  FROM public.exercise_library_muscles m
  WHERE m.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  INSERT INTO public.exercise_library_equipment (exercise_library_id, equipment_type_id)
  SELECT keep_id, e.equipment_type_id
  FROM public.exercise_library_equipment e
  WHERE e.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  -- (c) repoint non-junction FK references
  UPDATE public.trial_assessment_items
  SET exercise_library_id = keep_id
  WHERE exercise_library_id = absorb_id;

  -- (d) delete absorb row (its remaining junction rows cascade)
  DELETE FROM public.exercise_library WHERE id = absorb_id;
  RAISE NOTICE 'dedup72: merged EX0320 -> EX0060 (stretch push ups)';
END $$;

DO $$
DECLARE
  keep_id uuid;
  absorb_id uuid;
BEGIN
  SELECT id INTO keep_id FROM public.exercise_library WHERE code = 'EX0067';
  SELECT id INTO absorb_id FROM public.exercise_library WHERE code = 'EX0289';
  IF keep_id IS NULL OR absorb_id IS NULL THEN
    RAISE NOTICE 'dedup72: SKIP EX0067/EX0289 (row missing — already merged or renamed)';
    RETURN;
  END IF;

  -- (a) metadata merge: fill NULL/empty keep content columns from absorb; never overwrite
  UPDATE public.exercise_library k
  SET
    primary_muscle   = COALESCE(NULLIF(k.primary_muscle, ''), a.primary_muscle),
    secondary_muscle = COALESCE(NULLIF(k.secondary_muscle, ''), a.secondary_muscle),
    equipment        = COALESCE(NULLIF(k.equipment, ''), a.equipment),
    difficulty       = COALESCE(k.difficulty, a.difficulty),
    exercise_type    = COALESCE(NULLIF(k.exercise_type, ''), a.exercise_type),
    type             = COALESCE(NULLIF(k.type, ''), a.type),
    met_value        = COALESCE(k.met_value, a.met_value),
    description      = COALESCE(NULLIF(k.description, ''), a.description),
    safety_notes     = COALESCE(NULLIF(k.safety_notes, ''), a.safety_notes),
    youtube_url      = COALESCE(NULLIF(k.youtube_url, ''), a.youtube_url),
    image_url        = COALESCE(NULLIF(k.image_url, ''), a.image_url),
    updated_at       = now()
  FROM public.exercise_library a
  WHERE k.id = keep_id AND a.id = absorb_id;

  -- (b) junction merge: unique absorb tags survive (composite PKs dedupe)
  INSERT INTO public.exercise_library_muscles (exercise_library_id, muscle_name, is_primary)
  SELECT keep_id, m.muscle_name, m.is_primary
  FROM public.exercise_library_muscles m
  WHERE m.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  INSERT INTO public.exercise_library_equipment (exercise_library_id, equipment_type_id)
  SELECT keep_id, e.equipment_type_id
  FROM public.exercise_library_equipment e
  WHERE e.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  -- (c) repoint non-junction FK references
  UPDATE public.trial_assessment_items
  SET exercise_library_id = keep_id
  WHERE exercise_library_id = absorb_id;

  -- (d) delete absorb row (its remaining junction rows cascade)
  DELETE FROM public.exercise_library WHERE id = absorb_id;
  RAISE NOTICE 'dedup72: merged EX0289 -> EX0067 (lat pulldown pronated mid grip)';
END $$;

DO $$
DECLARE
  keep_id uuid;
  absorb_id uuid;
BEGIN
  SELECT id INTO keep_id FROM public.exercise_library WHERE code = 'EX0068';
  SELECT id INTO absorb_id FROM public.exercise_library WHERE code = 'EX0290';
  IF keep_id IS NULL OR absorb_id IS NULL THEN
    RAISE NOTICE 'dedup72: SKIP EX0068/EX0290 (row missing — already merged or renamed)';
    RETURN;
  END IF;

  -- (a) metadata merge: fill NULL/empty keep content columns from absorb; never overwrite
  UPDATE public.exercise_library k
  SET
    primary_muscle   = COALESCE(NULLIF(k.primary_muscle, ''), a.primary_muscle),
    secondary_muscle = COALESCE(NULLIF(k.secondary_muscle, ''), a.secondary_muscle),
    equipment        = COALESCE(NULLIF(k.equipment, ''), a.equipment),
    difficulty       = COALESCE(k.difficulty, a.difficulty),
    exercise_type    = COALESCE(NULLIF(k.exercise_type, ''), a.exercise_type),
    type             = COALESCE(NULLIF(k.type, ''), a.type),
    met_value        = COALESCE(k.met_value, a.met_value),
    description      = COALESCE(NULLIF(k.description, ''), a.description),
    safety_notes     = COALESCE(NULLIF(k.safety_notes, ''), a.safety_notes),
    youtube_url      = COALESCE(NULLIF(k.youtube_url, ''), a.youtube_url),
    image_url        = COALESCE(NULLIF(k.image_url, ''), a.image_url),
    updated_at       = now()
  FROM public.exercise_library a
  WHERE k.id = keep_id AND a.id = absorb_id;

  -- (b) junction merge: unique absorb tags survive (composite PKs dedupe)
  INSERT INTO public.exercise_library_muscles (exercise_library_id, muscle_name, is_primary)
  SELECT keep_id, m.muscle_name, m.is_primary
  FROM public.exercise_library_muscles m
  WHERE m.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  INSERT INTO public.exercise_library_equipment (exercise_library_id, equipment_type_id)
  SELECT keep_id, e.equipment_type_id
  FROM public.exercise_library_equipment e
  WHERE e.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  -- (c) repoint non-junction FK references
  UPDATE public.trial_assessment_items
  SET exercise_library_id = keep_id
  WHERE exercise_library_id = absorb_id;

  -- (d) delete absorb row (its remaining junction rows cascade)
  DELETE FROM public.exercise_library WHERE id = absorb_id;
  RAISE NOTICE 'dedup72: merged EX0290 -> EX0068 (lat pulldown pronated wide grip)';
END $$;

DO $$
DECLARE
  keep_id uuid;
  absorb_id uuid;
BEGIN
  SELECT id INTO keep_id FROM public.exercise_library WHERE code = 'EX0069';
  SELECT id INTO absorb_id FROM public.exercise_library WHERE code = 'EX0291';
  IF keep_id IS NULL OR absorb_id IS NULL THEN
    RAISE NOTICE 'dedup72: SKIP EX0069/EX0291 (row missing — already merged or renamed)';
    RETURN;
  END IF;

  -- (a) metadata merge: fill NULL/empty keep content columns from absorb; never overwrite
  UPDATE public.exercise_library k
  SET
    primary_muscle   = COALESCE(NULLIF(k.primary_muscle, ''), a.primary_muscle),
    secondary_muscle = COALESCE(NULLIF(k.secondary_muscle, ''), a.secondary_muscle),
    equipment        = COALESCE(NULLIF(k.equipment, ''), a.equipment),
    difficulty       = COALESCE(k.difficulty, a.difficulty),
    exercise_type    = COALESCE(NULLIF(k.exercise_type, ''), a.exercise_type),
    type             = COALESCE(NULLIF(k.type, ''), a.type),
    met_value        = COALESCE(k.met_value, a.met_value),
    description      = COALESCE(NULLIF(k.description, ''), a.description),
    safety_notes     = COALESCE(NULLIF(k.safety_notes, ''), a.safety_notes),
    youtube_url      = COALESCE(NULLIF(k.youtube_url, ''), a.youtube_url),
    image_url        = COALESCE(NULLIF(k.image_url, ''), a.image_url),
    updated_at       = now()
  FROM public.exercise_library a
  WHERE k.id = keep_id AND a.id = absorb_id;

  -- (b) junction merge: unique absorb tags survive (composite PKs dedupe)
  INSERT INTO public.exercise_library_muscles (exercise_library_id, muscle_name, is_primary)
  SELECT keep_id, m.muscle_name, m.is_primary
  FROM public.exercise_library_muscles m
  WHERE m.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  INSERT INTO public.exercise_library_equipment (exercise_library_id, equipment_type_id)
  SELECT keep_id, e.equipment_type_id
  FROM public.exercise_library_equipment e
  WHERE e.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  -- (c) repoint non-junction FK references
  UPDATE public.trial_assessment_items
  SET exercise_library_id = keep_id
  WHERE exercise_library_id = absorb_id;

  -- (d) delete absorb row (its remaining junction rows cascade)
  DELETE FROM public.exercise_library WHERE id = absorb_id;
  RAISE NOTICE 'dedup72: merged EX0291 -> EX0069 (lat pulldown semi supinated)';
END $$;

DO $$
DECLARE
  keep_id uuid;
  absorb_id uuid;
BEGIN
  SELECT id INTO keep_id FROM public.exercise_library WHERE code = 'EX0071';
  SELECT id INTO absorb_id FROM public.exercise_library WHERE code = 'EX0292';
  IF keep_id IS NULL OR absorb_id IS NULL THEN
    RAISE NOTICE 'dedup72: SKIP EX0071/EX0292 (row missing — already merged or renamed)';
    RETURN;
  END IF;

  -- (a) metadata merge: fill NULL/empty keep content columns from absorb; never overwrite
  UPDATE public.exercise_library k
  SET
    primary_muscle   = COALESCE(NULLIF(k.primary_muscle, ''), a.primary_muscle),
    secondary_muscle = COALESCE(NULLIF(k.secondary_muscle, ''), a.secondary_muscle),
    equipment        = COALESCE(NULLIF(k.equipment, ''), a.equipment),
    difficulty       = COALESCE(k.difficulty, a.difficulty),
    exercise_type    = COALESCE(NULLIF(k.exercise_type, ''), a.exercise_type),
    type             = COALESCE(NULLIF(k.type, ''), a.type),
    met_value        = COALESCE(k.met_value, a.met_value),
    description      = COALESCE(NULLIF(k.description, ''), a.description),
    safety_notes     = COALESCE(NULLIF(k.safety_notes, ''), a.safety_notes),
    youtube_url      = COALESCE(NULLIF(k.youtube_url, ''), a.youtube_url),
    image_url        = COALESCE(NULLIF(k.image_url, ''), a.image_url),
    updated_at       = now()
  FROM public.exercise_library a
  WHERE k.id = keep_id AND a.id = absorb_id;

  -- (b) junction merge: unique absorb tags survive (composite PKs dedupe)
  INSERT INTO public.exercise_library_muscles (exercise_library_id, muscle_name, is_primary)
  SELECT keep_id, m.muscle_name, m.is_primary
  FROM public.exercise_library_muscles m
  WHERE m.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  INSERT INTO public.exercise_library_equipment (exercise_library_id, equipment_type_id)
  SELECT keep_id, e.equipment_type_id
  FROM public.exercise_library_equipment e
  WHERE e.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  -- (c) repoint non-junction FK references
  UPDATE public.trial_assessment_items
  SET exercise_library_id = keep_id
  WHERE exercise_library_id = absorb_id;

  -- (d) delete absorb row (its remaining junction rows cascade)
  DELETE FROM public.exercise_library WHERE id = absorb_id;
  RAISE NOTICE 'dedup72: merged EX0292 -> EX0071 (lat pulldown single arm)';
END $$;

DO $$
DECLARE
  keep_id uuid;
  absorb_id uuid;
BEGIN
  SELECT id INTO keep_id FROM public.exercise_library WHERE code = 'EX0072';
  SELECT id INTO absorb_id FROM public.exercise_library WHERE code = 'EX0293';
  IF keep_id IS NULL OR absorb_id IS NULL THEN
    RAISE NOTICE 'dedup72: SKIP EX0072/EX0293 (row missing — already merged or renamed)';
    RETURN;
  END IF;

  -- (a) metadata merge: fill NULL/empty keep content columns from absorb; never overwrite
  UPDATE public.exercise_library k
  SET
    primary_muscle   = COALESCE(NULLIF(k.primary_muscle, ''), a.primary_muscle),
    secondary_muscle = COALESCE(NULLIF(k.secondary_muscle, ''), a.secondary_muscle),
    equipment        = COALESCE(NULLIF(k.equipment, ''), a.equipment),
    difficulty       = COALESCE(k.difficulty, a.difficulty),
    exercise_type    = COALESCE(NULLIF(k.exercise_type, ''), a.exercise_type),
    type             = COALESCE(NULLIF(k.type, ''), a.type),
    met_value        = COALESCE(k.met_value, a.met_value),
    description      = COALESCE(NULLIF(k.description, ''), a.description),
    safety_notes     = COALESCE(NULLIF(k.safety_notes, ''), a.safety_notes),
    youtube_url      = COALESCE(NULLIF(k.youtube_url, ''), a.youtube_url),
    image_url        = COALESCE(NULLIF(k.image_url, ''), a.image_url),
    updated_at       = now()
  FROM public.exercise_library a
  WHERE k.id = keep_id AND a.id = absorb_id;

  -- (b) junction merge: unique absorb tags survive (composite PKs dedupe)
  INSERT INTO public.exercise_library_muscles (exercise_library_id, muscle_name, is_primary)
  SELECT keep_id, m.muscle_name, m.is_primary
  FROM public.exercise_library_muscles m
  WHERE m.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  INSERT INTO public.exercise_library_equipment (exercise_library_id, equipment_type_id)
  SELECT keep_id, e.equipment_type_id
  FROM public.exercise_library_equipment e
  WHERE e.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  -- (c) repoint non-junction FK references
  UPDATE public.trial_assessment_items
  SET exercise_library_id = keep_id
  WHERE exercise_library_id = absorb_id;

  -- (d) delete absorb row (its remaining junction rows cascade)
  DELETE FROM public.exercise_library WHERE id = absorb_id;
  RAISE NOTICE 'dedup72: merged EX0293 -> EX0072 (lat pulldown supinated)';
END $$;

DO $$
DECLARE
  keep_id uuid;
  absorb_id uuid;
BEGIN
  SELECT id INTO keep_id FROM public.exercise_library WHERE code = 'EX0079';
  SELECT id INTO absorb_id FROM public.exercise_library WHERE code = 'EX0308';
  IF keep_id IS NULL OR absorb_id IS NULL THEN
    RAISE NOTICE 'dedup72: SKIP EX0079/EX0308 (row missing — already merged or renamed)';
    RETURN;
  END IF;

  -- (a) metadata merge: fill NULL/empty keep content columns from absorb; never overwrite
  UPDATE public.exercise_library k
  SET
    primary_muscle   = COALESCE(NULLIF(k.primary_muscle, ''), a.primary_muscle),
    secondary_muscle = COALESCE(NULLIF(k.secondary_muscle, ''), a.secondary_muscle),
    equipment        = COALESCE(NULLIF(k.equipment, ''), a.equipment),
    difficulty       = COALESCE(k.difficulty, a.difficulty),
    exercise_type    = COALESCE(NULLIF(k.exercise_type, ''), a.exercise_type),
    type             = COALESCE(NULLIF(k.type, ''), a.type),
    met_value        = COALESCE(k.met_value, a.met_value),
    description      = COALESCE(NULLIF(k.description, ''), a.description),
    safety_notes     = COALESCE(NULLIF(k.safety_notes, ''), a.safety_notes),
    youtube_url      = COALESCE(NULLIF(k.youtube_url, ''), a.youtube_url),
    image_url        = COALESCE(NULLIF(k.image_url, ''), a.image_url),
    updated_at       = now()
  FROM public.exercise_library a
  WHERE k.id = keep_id AND a.id = absorb_id;

  -- (b) junction merge: unique absorb tags survive (composite PKs dedupe)
  INSERT INTO public.exercise_library_muscles (exercise_library_id, muscle_name, is_primary)
  SELECT keep_id, m.muscle_name, m.is_primary
  FROM public.exercise_library_muscles m
  WHERE m.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  INSERT INTO public.exercise_library_equipment (exercise_library_id, equipment_type_id)
  SELECT keep_id, e.equipment_type_id
  FROM public.exercise_library_equipment e
  WHERE e.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  -- (c) repoint non-junction FK references
  UPDATE public.trial_assessment_items
  SET exercise_library_id = keep_id
  WHERE exercise_library_id = absorb_id;

  -- (d) delete absorb row (its remaining junction rows cascade)
  DELETE FROM public.exercise_library WHERE id = absorb_id;
  RAISE NOTICE 'dedup72: merged EX0308 -> EX0079 (seated cable row diverging handles)';
END $$;

DO $$
DECLARE
  keep_id uuid;
  absorb_id uuid;
BEGIN
  SELECT id INTO keep_id FROM public.exercise_library WHERE code = 'EX0080';
  SELECT id INTO absorb_id FROM public.exercise_library WHERE code = 'EX0309';
  IF keep_id IS NULL OR absorb_id IS NULL THEN
    RAISE NOTICE 'dedup72: SKIP EX0080/EX0309 (row missing — already merged or renamed)';
    RETURN;
  END IF;

  -- (a) metadata merge: fill NULL/empty keep content columns from absorb; never overwrite
  UPDATE public.exercise_library k
  SET
    primary_muscle   = COALESCE(NULLIF(k.primary_muscle, ''), a.primary_muscle),
    secondary_muscle = COALESCE(NULLIF(k.secondary_muscle, ''), a.secondary_muscle),
    equipment        = COALESCE(NULLIF(k.equipment, ''), a.equipment),
    difficulty       = COALESCE(k.difficulty, a.difficulty),
    exercise_type    = COALESCE(NULLIF(k.exercise_type, ''), a.exercise_type),
    type             = COALESCE(NULLIF(k.type, ''), a.type),
    met_value        = COALESCE(k.met_value, a.met_value),
    description      = COALESCE(NULLIF(k.description, ''), a.description),
    safety_notes     = COALESCE(NULLIF(k.safety_notes, ''), a.safety_notes),
    youtube_url      = COALESCE(NULLIF(k.youtube_url, ''), a.youtube_url),
    image_url        = COALESCE(NULLIF(k.image_url, ''), a.image_url),
    updated_at       = now()
  FROM public.exercise_library a
  WHERE k.id = keep_id AND a.id = absorb_id;

  -- (b) junction merge: unique absorb tags survive (composite PKs dedupe)
  INSERT INTO public.exercise_library_muscles (exercise_library_id, muscle_name, is_primary)
  SELECT keep_id, m.muscle_name, m.is_primary
  FROM public.exercise_library_muscles m
  WHERE m.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  INSERT INTO public.exercise_library_equipment (exercise_library_id, equipment_type_id)
  SELECT keep_id, e.equipment_type_id
  FROM public.exercise_library_equipment e
  WHERE e.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  -- (c) repoint non-junction FK references
  UPDATE public.trial_assessment_items
  SET exercise_library_id = keep_id
  WHERE exercise_library_id = absorb_id;

  -- (d) delete absorb row (its remaining junction rows cascade)
  DELETE FROM public.exercise_library WHERE id = absorb_id;
  RAISE NOTICE 'dedup72: merged EX0309 -> EX0080 (seated cable row neutral grip)';
END $$;

DO $$
DECLARE
  keep_id uuid;
  absorb_id uuid;
BEGIN
  SELECT id INTO keep_id FROM public.exercise_library WHERE code = 'EX0081';
  SELECT id INTO absorb_id FROM public.exercise_library WHERE code = 'EX0310';
  IF keep_id IS NULL OR absorb_id IS NULL THEN
    RAISE NOTICE 'dedup72: SKIP EX0081/EX0310 (row missing — already merged or renamed)';
    RETURN;
  END IF;

  -- (a) metadata merge: fill NULL/empty keep content columns from absorb; never overwrite
  UPDATE public.exercise_library k
  SET
    primary_muscle   = COALESCE(NULLIF(k.primary_muscle, ''), a.primary_muscle),
    secondary_muscle = COALESCE(NULLIF(k.secondary_muscle, ''), a.secondary_muscle),
    equipment        = COALESCE(NULLIF(k.equipment, ''), a.equipment),
    difficulty       = COALESCE(k.difficulty, a.difficulty),
    exercise_type    = COALESCE(NULLIF(k.exercise_type, ''), a.exercise_type),
    type             = COALESCE(NULLIF(k.type, ''), a.type),
    met_value        = COALESCE(k.met_value, a.met_value),
    description      = COALESCE(NULLIF(k.description, ''), a.description),
    safety_notes     = COALESCE(NULLIF(k.safety_notes, ''), a.safety_notes),
    youtube_url      = COALESCE(NULLIF(k.youtube_url, ''), a.youtube_url),
    image_url        = COALESCE(NULLIF(k.image_url, ''), a.image_url),
    updated_at       = now()
  FROM public.exercise_library a
  WHERE k.id = keep_id AND a.id = absorb_id;

  -- (b) junction merge: unique absorb tags survive (composite PKs dedupe)
  INSERT INTO public.exercise_library_muscles (exercise_library_id, muscle_name, is_primary)
  SELECT keep_id, m.muscle_name, m.is_primary
  FROM public.exercise_library_muscles m
  WHERE m.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  INSERT INTO public.exercise_library_equipment (exercise_library_id, equipment_type_id)
  SELECT keep_id, e.equipment_type_id
  FROM public.exercise_library_equipment e
  WHERE e.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  -- (c) repoint non-junction FK references
  UPDATE public.trial_assessment_items
  SET exercise_library_id = keep_id
  WHERE exercise_library_id = absorb_id;

  -- (d) delete absorb row (its remaining junction rows cascade)
  DELETE FROM public.exercise_library WHERE id = absorb_id;
  RAISE NOTICE 'dedup72: merged EX0310 -> EX0081 (seated cable row single arm)';
END $$;

DO $$
DECLARE
  keep_id uuid;
  absorb_id uuid;
BEGIN
  SELECT id INTO keep_id FROM public.exercise_library WHERE code = 'EX0082';
  SELECT id INTO absorb_id FROM public.exercise_library WHERE code = 'EX0311';
  IF keep_id IS NULL OR absorb_id IS NULL THEN
    RAISE NOTICE 'dedup72: SKIP EX0082/EX0311 (row missing — already merged or renamed)';
    RETURN;
  END IF;

  -- (a) metadata merge: fill NULL/empty keep content columns from absorb; never overwrite
  UPDATE public.exercise_library k
  SET
    primary_muscle   = COALESCE(NULLIF(k.primary_muscle, ''), a.primary_muscle),
    secondary_muscle = COALESCE(NULLIF(k.secondary_muscle, ''), a.secondary_muscle),
    equipment        = COALESCE(NULLIF(k.equipment, ''), a.equipment),
    difficulty       = COALESCE(k.difficulty, a.difficulty),
    exercise_type    = COALESCE(NULLIF(k.exercise_type, ''), a.exercise_type),
    type             = COALESCE(NULLIF(k.type, ''), a.type),
    met_value        = COALESCE(k.met_value, a.met_value),
    description      = COALESCE(NULLIF(k.description, ''), a.description),
    safety_notes     = COALESCE(NULLIF(k.safety_notes, ''), a.safety_notes),
    youtube_url      = COALESCE(NULLIF(k.youtube_url, ''), a.youtube_url),
    image_url        = COALESCE(NULLIF(k.image_url, ''), a.image_url),
    updated_at       = now()
  FROM public.exercise_library a
  WHERE k.id = keep_id AND a.id = absorb_id;

  -- (b) junction merge: unique absorb tags survive (composite PKs dedupe)
  INSERT INTO public.exercise_library_muscles (exercise_library_id, muscle_name, is_primary)
  SELECT keep_id, m.muscle_name, m.is_primary
  FROM public.exercise_library_muscles m
  WHERE m.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  INSERT INTO public.exercise_library_equipment (exercise_library_id, equipment_type_id)
  SELECT keep_id, e.equipment_type_id
  FROM public.exercise_library_equipment e
  WHERE e.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  -- (c) repoint non-junction FK references
  UPDATE public.trial_assessment_items
  SET exercise_library_id = keep_id
  WHERE exercise_library_id = absorb_id;

  -- (d) delete absorb row (its remaining junction rows cascade)
  DELETE FROM public.exercise_library WHERE id = absorb_id;
  RAISE NOTICE 'dedup72: merged EX0311 -> EX0082 (seated cable row wide grip)';
END $$;

DO $$
DECLARE
  keep_id uuid;
  absorb_id uuid;
BEGIN
  SELECT id INTO keep_id FROM public.exercise_library WHERE code = 'EX0084';
  SELECT id INTO absorb_id FROM public.exercise_library WHERE code = 'EX0231';
  IF keep_id IS NULL OR absorb_id IS NULL THEN
    RAISE NOTICE 'dedup72: SKIP EX0084/EX0231 (row missing — already merged or renamed)';
    RETURN;
  END IF;

  -- (a) metadata merge: fill NULL/empty keep content columns from absorb; never overwrite
  UPDATE public.exercise_library k
  SET
    primary_muscle   = COALESCE(NULLIF(k.primary_muscle, ''), a.primary_muscle),
    secondary_muscle = COALESCE(NULLIF(k.secondary_muscle, ''), a.secondary_muscle),
    equipment        = COALESCE(NULLIF(k.equipment, ''), a.equipment),
    difficulty       = COALESCE(k.difficulty, a.difficulty),
    exercise_type    = COALESCE(NULLIF(k.exercise_type, ''), a.exercise_type),
    type             = COALESCE(NULLIF(k.type, ''), a.type),
    met_value        = COALESCE(k.met_value, a.met_value),
    description      = COALESCE(NULLIF(k.description, ''), a.description),
    safety_notes     = COALESCE(NULLIF(k.safety_notes, ''), a.safety_notes),
    youtube_url      = COALESCE(NULLIF(k.youtube_url, ''), a.youtube_url),
    image_url        = COALESCE(NULLIF(k.image_url, ''), a.image_url),
    updated_at       = now()
  FROM public.exercise_library a
  WHERE k.id = keep_id AND a.id = absorb_id;

  -- (b) junction merge: unique absorb tags survive (composite PKs dedupe)
  INSERT INTO public.exercise_library_muscles (exercise_library_id, muscle_name, is_primary)
  SELECT keep_id, m.muscle_name, m.is_primary
  FROM public.exercise_library_muscles m
  WHERE m.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  INSERT INTO public.exercise_library_equipment (exercise_library_id, equipment_type_id)
  SELECT keep_id, e.equipment_type_id
  FROM public.exercise_library_equipment e
  WHERE e.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  -- (c) repoint non-junction FK references
  UPDATE public.trial_assessment_items
  SET exercise_library_id = keep_id
  WHERE exercise_library_id = absorb_id;

  -- (d) delete absorb row (its remaining junction rows cascade)
  DELETE FROM public.exercise_library WHERE id = absorb_id;
  RAISE NOTICE 'dedup72: merged EX0231 -> EX0084 (1 2 kneeling cable pulldown)';
END $$;

DO $$
DECLARE
  keep_id uuid;
  absorb_id uuid;
BEGIN
  SELECT id INTO keep_id FROM public.exercise_library WHERE code = 'EX0086';
  SELECT id INTO absorb_id FROM public.exercise_library WHERE code = 'EX0232';
  IF keep_id IS NULL OR absorb_id IS NULL THEN
    RAISE NOTICE 'dedup72: SKIP EX0086/EX0232 (row missing — already merged or renamed)';
    RETURN;
  END IF;

  -- (a) metadata merge: fill NULL/empty keep content columns from absorb; never overwrite
  UPDATE public.exercise_library k
  SET
    primary_muscle   = COALESCE(NULLIF(k.primary_muscle, ''), a.primary_muscle),
    secondary_muscle = COALESCE(NULLIF(k.secondary_muscle, ''), a.secondary_muscle),
    equipment        = COALESCE(NULLIF(k.equipment, ''), a.equipment),
    difficulty       = COALESCE(k.difficulty, a.difficulty),
    exercise_type    = COALESCE(NULLIF(k.exercise_type, ''), a.exercise_type),
    type             = COALESCE(NULLIF(k.type, ''), a.type),
    met_value        = COALESCE(k.met_value, a.met_value),
    description      = COALESCE(NULLIF(k.description, ''), a.description),
    safety_notes     = COALESCE(NULLIF(k.safety_notes, ''), a.safety_notes),
    youtube_url      = COALESCE(NULLIF(k.youtube_url, ''), a.youtube_url),
    image_url        = COALESCE(NULLIF(k.image_url, ''), a.image_url),
    updated_at       = now()
  FROM public.exercise_library a
  WHERE k.id = keep_id AND a.id = absorb_id;

  -- (b) junction merge: unique absorb tags survive (composite PKs dedupe)
  INSERT INTO public.exercise_library_muscles (exercise_library_id, muscle_name, is_primary)
  SELECT keep_id, m.muscle_name, m.is_primary
  FROM public.exercise_library_muscles m
  WHERE m.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  INSERT INTO public.exercise_library_equipment (exercise_library_id, equipment_type_id)
  SELECT keep_id, e.equipment_type_id
  FROM public.exercise_library_equipment e
  WHERE e.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  -- (c) repoint non-junction FK references
  UPDATE public.trial_assessment_items
  SET exercise_library_id = keep_id
  WHERE exercise_library_id = absorb_id;

  -- (d) delete absorb row (its remaining junction rows cascade)
  DELETE FROM public.exercise_library WHERE id = absorb_id;
  RAISE NOTICE 'dedup72: merged EX0232 -> EX0086 (1 2 kneeling rope pulldown)';
END $$;

DO $$
DECLARE
  keep_id uuid;
  absorb_id uuid;
BEGIN
  SELECT id INTO keep_id FROM public.exercise_library WHERE code = 'EX0094';
  SELECT id INTO absorb_id FROM public.exercise_library WHERE code = 'EX0248';
  IF keep_id IS NULL OR absorb_id IS NULL THEN
    RAISE NOTICE 'dedup72: SKIP EX0094/EX0248 (row missing — already merged or renamed)';
    RETURN;
  END IF;

  -- (a) metadata merge: fill NULL/empty keep content columns from absorb; never overwrite
  UPDATE public.exercise_library k
  SET
    primary_muscle   = COALESCE(NULLIF(k.primary_muscle, ''), a.primary_muscle),
    secondary_muscle = COALESCE(NULLIF(k.secondary_muscle, ''), a.secondary_muscle),
    equipment        = COALESCE(NULLIF(k.equipment, ''), a.equipment),
    difficulty       = COALESCE(k.difficulty, a.difficulty),
    exercise_type    = COALESCE(NULLIF(k.exercise_type, ''), a.exercise_type),
    type             = COALESCE(NULLIF(k.type, ''), a.type),
    met_value        = COALESCE(k.met_value, a.met_value),
    description      = COALESCE(NULLIF(k.description, ''), a.description),
    safety_notes     = COALESCE(NULLIF(k.safety_notes, ''), a.safety_notes),
    youtube_url      = COALESCE(NULLIF(k.youtube_url, ''), a.youtube_url),
    image_url        = COALESCE(NULLIF(k.image_url, ''), a.image_url),
    updated_at       = now()
  FROM public.exercise_library a
  WHERE k.id = keep_id AND a.id = absorb_id;

  -- (b) junction merge: unique absorb tags survive (composite PKs dedupe)
  INSERT INTO public.exercise_library_muscles (exercise_library_id, muscle_name, is_primary)
  SELECT keep_id, m.muscle_name, m.is_primary
  FROM public.exercise_library_muscles m
  WHERE m.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  INSERT INTO public.exercise_library_equipment (exercise_library_id, equipment_type_id)
  SELECT keep_id, e.equipment_type_id
  FROM public.exercise_library_equipment e
  WHERE e.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  -- (c) repoint non-junction FK references
  UPDATE public.trial_assessment_items
  SET exercise_library_id = keep_id
  WHERE exercise_library_id = absorb_id;

  -- (d) delete absorb row (its remaining junction rows cascade)
  DELETE FROM public.exercise_library WHERE id = absorb_id;
  RAISE NOTICE 'dedup72: merged EX0248 -> EX0094 (chest supported straight arm pulldown)';
END $$;

DO $$
DECLARE
  keep_id uuid;
  absorb_id uuid;
BEGIN
  SELECT id INTO keep_id FROM public.exercise_library WHERE code = 'EX0097';
  SELECT id INTO absorb_id FROM public.exercise_library WHERE code = 'EX0270';
  IF keep_id IS NULL OR absorb_id IS NULL THEN
    RAISE NOTICE 'dedup72: SKIP EX0097/EX0270 (row missing — already merged or renamed)';
    RETURN;
  END IF;

  -- (a) metadata merge: fill NULL/empty keep content columns from absorb; never overwrite
  UPDATE public.exercise_library k
  SET
    primary_muscle   = COALESCE(NULLIF(k.primary_muscle, ''), a.primary_muscle),
    secondary_muscle = COALESCE(NULLIF(k.secondary_muscle, ''), a.secondary_muscle),
    equipment        = COALESCE(NULLIF(k.equipment, ''), a.equipment),
    difficulty       = COALESCE(k.difficulty, a.difficulty),
    exercise_type    = COALESCE(NULLIF(k.exercise_type, ''), a.exercise_type),
    type             = COALESCE(NULLIF(k.type, ''), a.type),
    met_value        = COALESCE(k.met_value, a.met_value),
    description      = COALESCE(NULLIF(k.description, ''), a.description),
    safety_notes     = COALESCE(NULLIF(k.safety_notes, ''), a.safety_notes),
    youtube_url      = COALESCE(NULLIF(k.youtube_url, ''), a.youtube_url),
    image_url        = COALESCE(NULLIF(k.image_url, ''), a.image_url),
    updated_at       = now()
  FROM public.exercise_library a
  WHERE k.id = keep_id AND a.id = absorb_id;

  -- (b) junction merge: unique absorb tags survive (composite PKs dedupe)
  INSERT INTO public.exercise_library_muscles (exercise_library_id, muscle_name, is_primary)
  SELECT keep_id, m.muscle_name, m.is_primary
  FROM public.exercise_library_muscles m
  WHERE m.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  INSERT INTO public.exercise_library_equipment (exercise_library_id, equipment_type_id)
  SELECT keep_id, e.equipment_type_id
  FROM public.exercise_library_equipment e
  WHERE e.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  -- (c) repoint non-junction FK references
  UPDATE public.trial_assessment_items
  SET exercise_library_id = keep_id
  WHERE exercise_library_id = absorb_id;

  -- (d) delete absorb row (its remaining junction rows cascade)
  DELETE FROM public.exercise_library WHERE id = absorb_id;
  RAISE NOTICE 'dedup72: merged EX0270 -> EX0097 (dual cable pulldown)';
END $$;

DO $$
DECLARE
  keep_id uuid;
  absorb_id uuid;
BEGIN
  SELECT id INTO keep_id FROM public.exercise_library WHERE code = 'EX0098';
  SELECT id INTO absorb_id FROM public.exercise_library WHERE code = 'EX0280';
  IF keep_id IS NULL OR absorb_id IS NULL THEN
    RAISE NOTICE 'dedup72: SKIP EX0098/EX0280 (row missing — already merged or renamed)';
    RETURN;
  END IF;

  -- (a) metadata merge: fill NULL/empty keep content columns from absorb; never overwrite
  UPDATE public.exercise_library k
  SET
    primary_muscle   = COALESCE(NULLIF(k.primary_muscle, ''), a.primary_muscle),
    secondary_muscle = COALESCE(NULLIF(k.secondary_muscle, ''), a.secondary_muscle),
    equipment        = COALESCE(NULLIF(k.equipment, ''), a.equipment),
    difficulty       = COALESCE(k.difficulty, a.difficulty),
    exercise_type    = COALESCE(NULLIF(k.exercise_type, ''), a.exercise_type),
    type             = COALESCE(NULLIF(k.type, ''), a.type),
    met_value        = COALESCE(k.met_value, a.met_value),
    description      = COALESCE(NULLIF(k.description, ''), a.description),
    safety_notes     = COALESCE(NULLIF(k.safety_notes, ''), a.safety_notes),
    youtube_url      = COALESCE(NULLIF(k.youtube_url, ''), a.youtube_url),
    image_url        = COALESCE(NULLIF(k.image_url, ''), a.image_url),
    updated_at       = now()
  FROM public.exercise_library a
  WHERE k.id = keep_id AND a.id = absorb_id;

  -- (b) junction merge: unique absorb tags survive (composite PKs dedupe)
  INSERT INTO public.exercise_library_muscles (exercise_library_id, muscle_name, is_primary)
  SELECT keep_id, m.muscle_name, m.is_primary
  FROM public.exercise_library_muscles m
  WHERE m.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  INSERT INTO public.exercise_library_equipment (exercise_library_id, equipment_type_id)
  SELECT keep_id, e.equipment_type_id
  FROM public.exercise_library_equipment e
  WHERE e.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  -- (c) repoint non-junction FK references
  UPDATE public.trial_assessment_items
  SET exercise_library_id = keep_id
  WHERE exercise_library_id = absorb_id;

  -- (d) delete absorb row (its remaining junction rows cascade)
  DELETE FROM public.exercise_library WHERE id = absorb_id;
  RAISE NOTICE 'dedup72: merged EX0280 -> EX0098 (football bar pullover)';
END $$;

DO $$
DECLARE
  keep_id uuid;
  absorb_id uuid;
BEGIN
  SELECT id INTO keep_id FROM public.exercise_library WHERE code = 'EX0104';
  SELECT id INTO absorb_id FROM public.exercise_library WHERE code = 'EX0319';
  IF keep_id IS NULL OR absorb_id IS NULL THEN
    RAISE NOTICE 'dedup72: SKIP EX0104/EX0319 (row missing — already merged or renamed)';
    RETURN;
  END IF;

  -- (a) metadata merge: fill NULL/empty keep content columns from absorb; never overwrite
  UPDATE public.exercise_library k
  SET
    primary_muscle   = COALESCE(NULLIF(k.primary_muscle, ''), a.primary_muscle),
    secondary_muscle = COALESCE(NULLIF(k.secondary_muscle, ''), a.secondary_muscle),
    equipment        = COALESCE(NULLIF(k.equipment, ''), a.equipment),
    difficulty       = COALESCE(k.difficulty, a.difficulty),
    exercise_type    = COALESCE(NULLIF(k.exercise_type, ''), a.exercise_type),
    type             = COALESCE(NULLIF(k.type, ''), a.type),
    met_value        = COALESCE(k.met_value, a.met_value),
    description      = COALESCE(NULLIF(k.description, ''), a.description),
    safety_notes     = COALESCE(NULLIF(k.safety_notes, ''), a.safety_notes),
    youtube_url      = COALESCE(NULLIF(k.youtube_url, ''), a.youtube_url),
    image_url        = COALESCE(NULLIF(k.image_url, ''), a.image_url),
    updated_at       = now()
  FROM public.exercise_library a
  WHERE k.id = keep_id AND a.id = absorb_id;

  -- (b) junction merge: unique absorb tags survive (composite PKs dedupe)
  INSERT INTO public.exercise_library_muscles (exercise_library_id, muscle_name, is_primary)
  SELECT keep_id, m.muscle_name, m.is_primary
  FROM public.exercise_library_muscles m
  WHERE m.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  INSERT INTO public.exercise_library_equipment (exercise_library_id, equipment_type_id)
  SELECT keep_id, e.equipment_type_id
  FROM public.exercise_library_equipment e
  WHERE e.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  -- (c) repoint non-junction FK references
  UPDATE public.trial_assessment_items
  SET exercise_library_id = keep_id
  WHERE exercise_library_id = absorb_id;

  -- (d) delete absorb row (its remaining junction rows cascade)
  DELETE FROM public.exercise_library WHERE id = absorb_id;
  RAISE NOTICE 'dedup72: merged EX0319 -> EX0104 (standing straight arm pulldown)';
END $$;

DO $$
DECLARE
  keep_id uuid;
  absorb_id uuid;
BEGIN
  SELECT id INTO keep_id FROM public.exercise_library WHERE code = 'EX0135';
  SELECT id INTO absorb_id FROM public.exercise_library WHERE code = 'EX0302';
  IF keep_id IS NULL OR absorb_id IS NULL THEN
    RAISE NOTICE 'dedup72: SKIP EX0135/EX0302 (row missing — already merged or renamed)';
    RETURN;
  END IF;

  -- (a) metadata merge: fill NULL/empty keep content columns from absorb; never overwrite
  UPDATE public.exercise_library k
  SET
    primary_muscle   = COALESCE(NULLIF(k.primary_muscle, ''), a.primary_muscle),
    secondary_muscle = COALESCE(NULLIF(k.secondary_muscle, ''), a.secondary_muscle),
    equipment        = COALESCE(NULLIF(k.equipment, ''), a.equipment),
    difficulty       = COALESCE(k.difficulty, a.difficulty),
    exercise_type    = COALESCE(NULLIF(k.exercise_type, ''), a.exercise_type),
    type             = COALESCE(NULLIF(k.type, ''), a.type),
    met_value        = COALESCE(k.met_value, a.met_value),
    description      = COALESCE(NULLIF(k.description, ''), a.description),
    safety_notes     = COALESCE(NULLIF(k.safety_notes, ''), a.safety_notes),
    youtube_url      = COALESCE(NULLIF(k.youtube_url, ''), a.youtube_url),
    image_url        = COALESCE(NULLIF(k.image_url, ''), a.image_url),
    updated_at       = now()
  FROM public.exercise_library a
  WHERE k.id = keep_id AND a.id = absorb_id;

  -- (b) junction merge: unique absorb tags survive (composite PKs dedupe)
  INSERT INTO public.exercise_library_muscles (exercise_library_id, muscle_name, is_primary)
  SELECT keep_id, m.muscle_name, m.is_primary
  FROM public.exercise_library_muscles m
  WHERE m.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  INSERT INTO public.exercise_library_equipment (exercise_library_id, equipment_type_id)
  SELECT keep_id, e.equipment_type_id
  FROM public.exercise_library_equipment e
  WHERE e.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  -- (c) repoint non-junction FK references
  UPDATE public.trial_assessment_items
  SET exercise_library_id = keep_id
  WHERE exercise_library_id = absorb_id;

  -- (d) delete absorb row (its remaining junction rows cascade)
  DELETE FROM public.exercise_library WHERE id = absorb_id;
  RAISE NOTICE 'dedup72: merged EX0302 -> EX0135 (reverse sled drag)';
END $$;

DO $$
DECLARE
  keep_id uuid;
  absorb_id uuid;
BEGIN
  SELECT id INTO keep_id FROM public.exercise_library WHERE code = 'EX0135';
  SELECT id INTO absorb_id FROM public.exercise_library WHERE code = 'EX0351';
  IF keep_id IS NULL OR absorb_id IS NULL THEN
    RAISE NOTICE 'dedup72: SKIP EX0135/EX0351 (row missing — already merged or renamed)';
    RETURN;
  END IF;

  -- (a) metadata merge: fill NULL/empty keep content columns from absorb; never overwrite
  UPDATE public.exercise_library k
  SET
    primary_muscle   = COALESCE(NULLIF(k.primary_muscle, ''), a.primary_muscle),
    secondary_muscle = COALESCE(NULLIF(k.secondary_muscle, ''), a.secondary_muscle),
    equipment        = COALESCE(NULLIF(k.equipment, ''), a.equipment),
    difficulty       = COALESCE(k.difficulty, a.difficulty),
    exercise_type    = COALESCE(NULLIF(k.exercise_type, ''), a.exercise_type),
    type             = COALESCE(NULLIF(k.type, ''), a.type),
    met_value        = COALESCE(k.met_value, a.met_value),
    description      = COALESCE(NULLIF(k.description, ''), a.description),
    safety_notes     = COALESCE(NULLIF(k.safety_notes, ''), a.safety_notes),
    youtube_url      = COALESCE(NULLIF(k.youtube_url, ''), a.youtube_url),
    image_url        = COALESCE(NULLIF(k.image_url, ''), a.image_url),
    updated_at       = now()
  FROM public.exercise_library a
  WHERE k.id = keep_id AND a.id = absorb_id;

  -- (b) junction merge: unique absorb tags survive (composite PKs dedupe)
  INSERT INTO public.exercise_library_muscles (exercise_library_id, muscle_name, is_primary)
  SELECT keep_id, m.muscle_name, m.is_primary
  FROM public.exercise_library_muscles m
  WHERE m.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  INSERT INTO public.exercise_library_equipment (exercise_library_id, equipment_type_id)
  SELECT keep_id, e.equipment_type_id
  FROM public.exercise_library_equipment e
  WHERE e.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  -- (c) repoint non-junction FK references
  UPDATE public.trial_assessment_items
  SET exercise_library_id = keep_id
  WHERE exercise_library_id = absorb_id;

  -- (d) delete absorb row (its remaining junction rows cascade)
  DELETE FROM public.exercise_library WHERE id = absorb_id;
  RAISE NOTICE 'dedup72: merged EX0351 -> EX0135 (reverse sled drag)';
END $$;

DO $$
DECLARE
  keep_id uuid;
  absorb_id uuid;
BEGIN
  SELECT id INTO keep_id FROM public.exercise_library WHERE code = 'EX0215';
  SELECT id INTO absorb_id FROM public.exercise_library WHERE code = 'EX0440';
  IF keep_id IS NULL OR absorb_id IS NULL THEN
    RAISE NOTICE 'dedup72: SKIP EX0215/EX0440 (row missing — already merged or renamed)';
    RETURN;
  END IF;

  -- (a) metadata merge: fill NULL/empty keep content columns from absorb; never overwrite
  UPDATE public.exercise_library k
  SET
    primary_muscle   = COALESCE(NULLIF(k.primary_muscle, ''), a.primary_muscle),
    secondary_muscle = COALESCE(NULLIF(k.secondary_muscle, ''), a.secondary_muscle),
    equipment        = COALESCE(NULLIF(k.equipment, ''), a.equipment),
    difficulty       = COALESCE(k.difficulty, a.difficulty),
    exercise_type    = COALESCE(NULLIF(k.exercise_type, ''), a.exercise_type),
    type             = COALESCE(NULLIF(k.type, ''), a.type),
    met_value        = COALESCE(k.met_value, a.met_value),
    description      = COALESCE(NULLIF(k.description, ''), a.description),
    safety_notes     = COALESCE(NULLIF(k.safety_notes, ''), a.safety_notes),
    youtube_url      = COALESCE(NULLIF(k.youtube_url, ''), a.youtube_url),
    image_url        = COALESCE(NULLIF(k.image_url, ''), a.image_url),
    updated_at       = now()
  FROM public.exercise_library a
  WHERE k.id = keep_id AND a.id = absorb_id;

  -- (b) junction merge: unique absorb tags survive (composite PKs dedupe)
  INSERT INTO public.exercise_library_muscles (exercise_library_id, muscle_name, is_primary)
  SELECT keep_id, m.muscle_name, m.is_primary
  FROM public.exercise_library_muscles m
  WHERE m.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  INSERT INTO public.exercise_library_equipment (exercise_library_id, equipment_type_id)
  SELECT keep_id, e.equipment_type_id
  FROM public.exercise_library_equipment e
  WHERE e.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  -- (c) repoint non-junction FK references
  UPDATE public.trial_assessment_items
  SET exercise_library_id = keep_id
  WHERE exercise_library_id = absorb_id;

  -- (d) delete absorb row (its remaining junction rows cascade)
  DELETE FROM public.exercise_library WHERE id = absorb_id;
  RAISE NOTICE 'dedup72: merged EX0440 -> EX0215 (cable lateral raise lying)';
END $$;

DO $$
DECLARE
  keep_id uuid;
  absorb_id uuid;
BEGIN
  SELECT id INTO keep_id FROM public.exercise_library WHERE code = 'EX0216';
  SELECT id INTO absorb_id FROM public.exercise_library WHERE code = 'EX0441';
  IF keep_id IS NULL OR absorb_id IS NULL THEN
    RAISE NOTICE 'dedup72: SKIP EX0216/EX0441 (row missing — already merged or renamed)';
    RETURN;
  END IF;

  -- (a) metadata merge: fill NULL/empty keep content columns from absorb; never overwrite
  UPDATE public.exercise_library k
  SET
    primary_muscle   = COALESCE(NULLIF(k.primary_muscle, ''), a.primary_muscle),
    secondary_muscle = COALESCE(NULLIF(k.secondary_muscle, ''), a.secondary_muscle),
    equipment        = COALESCE(NULLIF(k.equipment, ''), a.equipment),
    difficulty       = COALESCE(k.difficulty, a.difficulty),
    exercise_type    = COALESCE(NULLIF(k.exercise_type, ''), a.exercise_type),
    type             = COALESCE(NULLIF(k.type, ''), a.type),
    met_value        = COALESCE(k.met_value, a.met_value),
    description      = COALESCE(NULLIF(k.description, ''), a.description),
    safety_notes     = COALESCE(NULLIF(k.safety_notes, ''), a.safety_notes),
    youtube_url      = COALESCE(NULLIF(k.youtube_url, ''), a.youtube_url),
    image_url        = COALESCE(NULLIF(k.image_url, ''), a.image_url),
    updated_at       = now()
  FROM public.exercise_library a
  WHERE k.id = keep_id AND a.id = absorb_id;

  -- (b) junction merge: unique absorb tags survive (composite PKs dedupe)
  INSERT INTO public.exercise_library_muscles (exercise_library_id, muscle_name, is_primary)
  SELECT keep_id, m.muscle_name, m.is_primary
  FROM public.exercise_library_muscles m
  WHERE m.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  INSERT INTO public.exercise_library_equipment (exercise_library_id, equipment_type_id)
  SELECT keep_id, e.equipment_type_id
  FROM public.exercise_library_equipment e
  WHERE e.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  -- (c) repoint non-junction FK references
  UPDATE public.trial_assessment_items
  SET exercise_library_id = keep_id
  WHERE exercise_library_id = absorb_id;

  -- (d) delete absorb row (its remaining junction rows cascade)
  DELETE FROM public.exercise_library WHERE id = absorb_id;
  RAISE NOTICE 'dedup72: merged EX0441 -> EX0216 (cable lateral raise standing)';
END $$;

DO $$
DECLARE
  keep_id uuid;
  absorb_id uuid;
BEGIN
  SELECT id INTO keep_id FROM public.exercise_library WHERE code = 'EX0217';
  SELECT id INTO absorb_id FROM public.exercise_library WHERE code = 'EX0444';
  IF keep_id IS NULL OR absorb_id IS NULL THEN
    RAISE NOTICE 'dedup72: SKIP EX0217/EX0444 (row missing — already merged or renamed)';
    RETURN;
  END IF;

  -- (a) metadata merge: fill NULL/empty keep content columns from absorb; never overwrite
  UPDATE public.exercise_library k
  SET
    primary_muscle   = COALESCE(NULLIF(k.primary_muscle, ''), a.primary_muscle),
    secondary_muscle = COALESCE(NULLIF(k.secondary_muscle, ''), a.secondary_muscle),
    equipment        = COALESCE(NULLIF(k.equipment, ''), a.equipment),
    difficulty       = COALESCE(k.difficulty, a.difficulty),
    exercise_type    = COALESCE(NULLIF(k.exercise_type, ''), a.exercise_type),
    type             = COALESCE(NULLIF(k.type, ''), a.type),
    met_value        = COALESCE(k.met_value, a.met_value),
    description      = COALESCE(NULLIF(k.description, ''), a.description),
    safety_notes     = COALESCE(NULLIF(k.safety_notes, ''), a.safety_notes),
    youtube_url      = COALESCE(NULLIF(k.youtube_url, ''), a.youtube_url),
    image_url        = COALESCE(NULLIF(k.image_url, ''), a.image_url),
    updated_at       = now()
  FROM public.exercise_library a
  WHERE k.id = keep_id AND a.id = absorb_id;

  -- (b) junction merge: unique absorb tags survive (composite PKs dedupe)
  INSERT INTO public.exercise_library_muscles (exercise_library_id, muscle_name, is_primary)
  SELECT keep_id, m.muscle_name, m.is_primary
  FROM public.exercise_library_muscles m
  WHERE m.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  INSERT INTO public.exercise_library_equipment (exercise_library_id, equipment_type_id)
  SELECT keep_id, e.equipment_type_id
  FROM public.exercise_library_equipment e
  WHERE e.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  -- (c) repoint non-junction FK references
  UPDATE public.trial_assessment_items
  SET exercise_library_id = keep_id
  WHERE exercise_library_id = absorb_id;

  -- (d) delete absorb row (its remaining junction rows cascade)
  DELETE FROM public.exercise_library WHERE id = absorb_id;
  RAISE NOTICE 'dedup72: merged EX0444 -> EX0217 (cable rear delt fly)';
END $$;

DO $$
DECLARE
  keep_id uuid;
  absorb_id uuid;
BEGIN
  SELECT id INTO keep_id FROM public.exercise_library WHERE code = 'EX0218';
  SELECT id INTO absorb_id FROM public.exercise_library WHERE code = 'EX0445';
  IF keep_id IS NULL OR absorb_id IS NULL THEN
    RAISE NOTICE 'dedup72: SKIP EX0218/EX0445 (row missing — already merged or renamed)';
    RETURN;
  END IF;

  -- (a) metadata merge: fill NULL/empty keep content columns from absorb; never overwrite
  UPDATE public.exercise_library k
  SET
    primary_muscle   = COALESCE(NULLIF(k.primary_muscle, ''), a.primary_muscle),
    secondary_muscle = COALESCE(NULLIF(k.secondary_muscle, ''), a.secondary_muscle),
    equipment        = COALESCE(NULLIF(k.equipment, ''), a.equipment),
    difficulty       = COALESCE(k.difficulty, a.difficulty),
    exercise_type    = COALESCE(NULLIF(k.exercise_type, ''), a.exercise_type),
    type             = COALESCE(NULLIF(k.type, ''), a.type),
    met_value        = COALESCE(k.met_value, a.met_value),
    description      = COALESCE(NULLIF(k.description, ''), a.description),
    safety_notes     = COALESCE(NULLIF(k.safety_notes, ''), a.safety_notes),
    youtube_url      = COALESCE(NULLIF(k.youtube_url, ''), a.youtube_url),
    image_url        = COALESCE(NULLIF(k.image_url, ''), a.image_url),
    updated_at       = now()
  FROM public.exercise_library a
  WHERE k.id = keep_id AND a.id = absorb_id;

  -- (b) junction merge: unique absorb tags survive (composite PKs dedupe)
  INSERT INTO public.exercise_library_muscles (exercise_library_id, muscle_name, is_primary)
  SELECT keep_id, m.muscle_name, m.is_primary
  FROM public.exercise_library_muscles m
  WHERE m.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  INSERT INTO public.exercise_library_equipment (exercise_library_id, equipment_type_id)
  SELECT keep_id, e.equipment_type_id
  FROM public.exercise_library_equipment e
  WHERE e.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  -- (c) repoint non-junction FK references
  UPDATE public.trial_assessment_items
  SET exercise_library_id = keep_id
  WHERE exercise_library_id = absorb_id;

  -- (d) delete absorb row (its remaining junction rows cascade)
  DELETE FROM public.exercise_library WHERE id = absorb_id;
  RAISE NOTICE 'dedup72: merged EX0445 -> EX0218 (db external rotation seated)';
END $$;

DO $$
DECLARE
  keep_id uuid;
  absorb_id uuid;
BEGIN
  SELECT id INTO keep_id FROM public.exercise_library WHERE code = 'EX0219';
  SELECT id INTO absorb_id FROM public.exercise_library WHERE code = 'EX0447';
  IF keep_id IS NULL OR absorb_id IS NULL THEN
    RAISE NOTICE 'dedup72: SKIP EX0219/EX0447 (row missing — already merged or renamed)';
    RETURN;
  END IF;

  -- (a) metadata merge: fill NULL/empty keep content columns from absorb; never overwrite
  UPDATE public.exercise_library k
  SET
    primary_muscle   = COALESCE(NULLIF(k.primary_muscle, ''), a.primary_muscle),
    secondary_muscle = COALESCE(NULLIF(k.secondary_muscle, ''), a.secondary_muscle),
    equipment        = COALESCE(NULLIF(k.equipment, ''), a.equipment),
    difficulty       = COALESCE(k.difficulty, a.difficulty),
    exercise_type    = COALESCE(NULLIF(k.exercise_type, ''), a.exercise_type),
    type             = COALESCE(NULLIF(k.type, ''), a.type),
    met_value        = COALESCE(k.met_value, a.met_value),
    description      = COALESCE(NULLIF(k.description, ''), a.description),
    safety_notes     = COALESCE(NULLIF(k.safety_notes, ''), a.safety_notes),
    youtube_url      = COALESCE(NULLIF(k.youtube_url, ''), a.youtube_url),
    image_url        = COALESCE(NULLIF(k.image_url, ''), a.image_url),
    updated_at       = now()
  FROM public.exercise_library a
  WHERE k.id = keep_id AND a.id = absorb_id;

  -- (b) junction merge: unique absorb tags survive (composite PKs dedupe)
  INSERT INTO public.exercise_library_muscles (exercise_library_id, muscle_name, is_primary)
  SELECT keep_id, m.muscle_name, m.is_primary
  FROM public.exercise_library_muscles m
  WHERE m.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  INSERT INTO public.exercise_library_equipment (exercise_library_id, equipment_type_id)
  SELECT keep_id, e.equipment_type_id
  FROM public.exercise_library_equipment e
  WHERE e.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  -- (c) repoint non-junction FK references
  UPDATE public.trial_assessment_items
  SET exercise_library_id = keep_id
  WHERE exercise_library_id = absorb_id;

  -- (d) delete absorb row (its remaining junction rows cascade)
  DELETE FROM public.exercise_library WHERE id = absorb_id;
  RAISE NOTICE 'dedup72: merged EX0447 -> EX0219 (db lateral raise chest supported)';
END $$;

DO $$
DECLARE
  keep_id uuid;
  absorb_id uuid;
BEGIN
  SELECT id INTO keep_id FROM public.exercise_library WHERE code = 'EX0220';
  SELECT id INTO absorb_id FROM public.exercise_library WHERE code = 'EX0448';
  IF keep_id IS NULL OR absorb_id IS NULL THEN
    RAISE NOTICE 'dedup72: SKIP EX0220/EX0448 (row missing — already merged or renamed)';
    RETURN;
  END IF;

  -- (a) metadata merge: fill NULL/empty keep content columns from absorb; never overwrite
  UPDATE public.exercise_library k
  SET
    primary_muscle   = COALESCE(NULLIF(k.primary_muscle, ''), a.primary_muscle),
    secondary_muscle = COALESCE(NULLIF(k.secondary_muscle, ''), a.secondary_muscle),
    equipment        = COALESCE(NULLIF(k.equipment, ''), a.equipment),
    difficulty       = COALESCE(k.difficulty, a.difficulty),
    exercise_type    = COALESCE(NULLIF(k.exercise_type, ''), a.exercise_type),
    type             = COALESCE(NULLIF(k.type, ''), a.type),
    met_value        = COALESCE(k.met_value, a.met_value),
    description      = COALESCE(NULLIF(k.description, ''), a.description),
    safety_notes     = COALESCE(NULLIF(k.safety_notes, ''), a.safety_notes),
    youtube_url      = COALESCE(NULLIF(k.youtube_url, ''), a.youtube_url),
    image_url        = COALESCE(NULLIF(k.image_url, ''), a.image_url),
    updated_at       = now()
  FROM public.exercise_library a
  WHERE k.id = keep_id AND a.id = absorb_id;

  -- (b) junction merge: unique absorb tags survive (composite PKs dedupe)
  INSERT INTO public.exercise_library_muscles (exercise_library_id, muscle_name, is_primary)
  SELECT keep_id, m.muscle_name, m.is_primary
  FROM public.exercise_library_muscles m
  WHERE m.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  INSERT INTO public.exercise_library_equipment (exercise_library_id, equipment_type_id)
  SELECT keep_id, e.equipment_type_id
  FROM public.exercise_library_equipment e
  WHERE e.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  -- (c) repoint non-junction FK references
  UPDATE public.trial_assessment_items
  SET exercise_library_id = keep_id
  WHERE exercise_library_id = absorb_id;

  -- (d) delete absorb row (its remaining junction rows cascade)
  DELETE FROM public.exercise_library WHERE id = absorb_id;
  RAISE NOTICE 'dedup72: merged EX0448 -> EX0220 (db lateral raise seated)';
END $$;

DO $$
DECLARE
  keep_id uuid;
  absorb_id uuid;
BEGIN
  SELECT id INTO keep_id FROM public.exercise_library WHERE code = 'EX0221';
  SELECT id INTO absorb_id FROM public.exercise_library WHERE code = 'EX0449';
  IF keep_id IS NULL OR absorb_id IS NULL THEN
    RAISE NOTICE 'dedup72: SKIP EX0221/EX0449 (row missing — already merged or renamed)';
    RETURN;
  END IF;

  -- (a) metadata merge: fill NULL/empty keep content columns from absorb; never overwrite
  UPDATE public.exercise_library k
  SET
    primary_muscle   = COALESCE(NULLIF(k.primary_muscle, ''), a.primary_muscle),
    secondary_muscle = COALESCE(NULLIF(k.secondary_muscle, ''), a.secondary_muscle),
    equipment        = COALESCE(NULLIF(k.equipment, ''), a.equipment),
    difficulty       = COALESCE(k.difficulty, a.difficulty),
    exercise_type    = COALESCE(NULLIF(k.exercise_type, ''), a.exercise_type),
    type             = COALESCE(NULLIF(k.type, ''), a.type),
    met_value        = COALESCE(k.met_value, a.met_value),
    description      = COALESCE(NULLIF(k.description, ''), a.description),
    safety_notes     = COALESCE(NULLIF(k.safety_notes, ''), a.safety_notes),
    youtube_url      = COALESCE(NULLIF(k.youtube_url, ''), a.youtube_url),
    image_url        = COALESCE(NULLIF(k.image_url, ''), a.image_url),
    updated_at       = now()
  FROM public.exercise_library a
  WHERE k.id = keep_id AND a.id = absorb_id;

  -- (b) junction merge: unique absorb tags survive (composite PKs dedupe)
  INSERT INTO public.exercise_library_muscles (exercise_library_id, muscle_name, is_primary)
  SELECT keep_id, m.muscle_name, m.is_primary
  FROM public.exercise_library_muscles m
  WHERE m.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  INSERT INTO public.exercise_library_equipment (exercise_library_id, equipment_type_id)
  SELECT keep_id, e.equipment_type_id
  FROM public.exercise_library_equipment e
  WHERE e.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  -- (c) repoint non-junction FK references
  UPDATE public.trial_assessment_items
  SET exercise_library_id = keep_id
  WHERE exercise_library_id = absorb_id;

  -- (d) delete absorb row (its remaining junction rows cascade)
  DELETE FROM public.exercise_library WHERE id = absorb_id;
  RAISE NOTICE 'dedup72: merged EX0449 -> EX0221 (db lateral raise single arm)';
END $$;

DO $$
DECLARE
  keep_id uuid;
  absorb_id uuid;
BEGIN
  SELECT id INTO keep_id FROM public.exercise_library WHERE code = 'EX0222';
  SELECT id INTO absorb_id FROM public.exercise_library WHERE code = 'EX0450';
  IF keep_id IS NULL OR absorb_id IS NULL THEN
    RAISE NOTICE 'dedup72: SKIP EX0222/EX0450 (row missing — already merged or renamed)';
    RETURN;
  END IF;

  -- (a) metadata merge: fill NULL/empty keep content columns from absorb; never overwrite
  UPDATE public.exercise_library k
  SET
    primary_muscle   = COALESCE(NULLIF(k.primary_muscle, ''), a.primary_muscle),
    secondary_muscle = COALESCE(NULLIF(k.secondary_muscle, ''), a.secondary_muscle),
    equipment        = COALESCE(NULLIF(k.equipment, ''), a.equipment),
    difficulty       = COALESCE(k.difficulty, a.difficulty),
    exercise_type    = COALESCE(NULLIF(k.exercise_type, ''), a.exercise_type),
    type             = COALESCE(NULLIF(k.type, ''), a.type),
    met_value        = COALESCE(k.met_value, a.met_value),
    description      = COALESCE(NULLIF(k.description, ''), a.description),
    safety_notes     = COALESCE(NULLIF(k.safety_notes, ''), a.safety_notes),
    youtube_url      = COALESCE(NULLIF(k.youtube_url, ''), a.youtube_url),
    image_url        = COALESCE(NULLIF(k.image_url, ''), a.image_url),
    updated_at       = now()
  FROM public.exercise_library a
  WHERE k.id = keep_id AND a.id = absorb_id;

  -- (b) junction merge: unique absorb tags survive (composite PKs dedupe)
  INSERT INTO public.exercise_library_muscles (exercise_library_id, muscle_name, is_primary)
  SELECT keep_id, m.muscle_name, m.is_primary
  FROM public.exercise_library_muscles m
  WHERE m.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  INSERT INTO public.exercise_library_equipment (exercise_library_id, equipment_type_id)
  SELECT keep_id, e.equipment_type_id
  FROM public.exercise_library_equipment e
  WHERE e.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  -- (c) repoint non-junction FK references
  UPDATE public.trial_assessment_items
  SET exercise_library_id = keep_id
  WHERE exercise_library_id = absorb_id;

  -- (d) delete absorb row (its remaining junction rows cascade)
  DELETE FROM public.exercise_library WHERE id = absorb_id;
  RAISE NOTICE 'dedup72: merged EX0450 -> EX0222 (db lateral raise standing)';
END $$;

DO $$
DECLARE
  keep_id uuid;
  absorb_id uuid;
BEGIN
  SELECT id INTO keep_id FROM public.exercise_library WHERE code = 'EX0223';
  SELECT id INTO absorb_id FROM public.exercise_library WHERE code = 'EX0451';
  IF keep_id IS NULL OR absorb_id IS NULL THEN
    RAISE NOTICE 'dedup72: SKIP EX0223/EX0451 (row missing — already merged or renamed)';
    RETURN;
  END IF;

  -- (a) metadata merge: fill NULL/empty keep content columns from absorb; never overwrite
  UPDATE public.exercise_library k
  SET
    primary_muscle   = COALESCE(NULLIF(k.primary_muscle, ''), a.primary_muscle),
    secondary_muscle = COALESCE(NULLIF(k.secondary_muscle, ''), a.secondary_muscle),
    equipment        = COALESCE(NULLIF(k.equipment, ''), a.equipment),
    difficulty       = COALESCE(k.difficulty, a.difficulty),
    exercise_type    = COALESCE(NULLIF(k.exercise_type, ''), a.exercise_type),
    type             = COALESCE(NULLIF(k.type, ''), a.type),
    met_value        = COALESCE(k.met_value, a.met_value),
    description      = COALESCE(NULLIF(k.description, ''), a.description),
    safety_notes     = COALESCE(NULLIF(k.safety_notes, ''), a.safety_notes),
    youtube_url      = COALESCE(NULLIF(k.youtube_url, ''), a.youtube_url),
    image_url        = COALESCE(NULLIF(k.image_url, ''), a.image_url),
    updated_at       = now()
  FROM public.exercise_library a
  WHERE k.id = keep_id AND a.id = absorb_id;

  -- (b) junction merge: unique absorb tags survive (composite PKs dedupe)
  INSERT INTO public.exercise_library_muscles (exercise_library_id, muscle_name, is_primary)
  SELECT keep_id, m.muscle_name, m.is_primary
  FROM public.exercise_library_muscles m
  WHERE m.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  INSERT INTO public.exercise_library_equipment (exercise_library_id, equipment_type_id)
  SELECT keep_id, e.equipment_type_id
  FROM public.exercise_library_equipment e
  WHERE e.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  -- (c) repoint non-junction FK references
  UPDATE public.trial_assessment_items
  SET exercise_library_id = keep_id
  WHERE exercise_library_id = absorb_id;

  -- (d) delete absorb row (its remaining junction rows cascade)
  DELETE FROM public.exercise_library WHERE id = absorb_id;
  RAISE NOTICE 'dedup72: merged EX0451 -> EX0223 (db prone y raise)';
END $$;

DO $$
DECLARE
  keep_id uuid;
  absorb_id uuid;
BEGIN
  SELECT id INTO keep_id FROM public.exercise_library WHERE code = 'EX0224';
  SELECT id INTO absorb_id FROM public.exercise_library WHERE code = 'EX0452';
  IF keep_id IS NULL OR absorb_id IS NULL THEN
    RAISE NOTICE 'dedup72: SKIP EX0224/EX0452 (row missing — already merged or renamed)';
    RETURN;
  END IF;

  -- (a) metadata merge: fill NULL/empty keep content columns from absorb; never overwrite
  UPDATE public.exercise_library k
  SET
    primary_muscle   = COALESCE(NULLIF(k.primary_muscle, ''), a.primary_muscle),
    secondary_muscle = COALESCE(NULLIF(k.secondary_muscle, ''), a.secondary_muscle),
    equipment        = COALESCE(NULLIF(k.equipment, ''), a.equipment),
    difficulty       = COALESCE(k.difficulty, a.difficulty),
    exercise_type    = COALESCE(NULLIF(k.exercise_type, ''), a.exercise_type),
    type             = COALESCE(NULLIF(k.type, ''), a.type),
    met_value        = COALESCE(k.met_value, a.met_value),
    description      = COALESCE(NULLIF(k.description, ''), a.description),
    safety_notes     = COALESCE(NULLIF(k.safety_notes, ''), a.safety_notes),
    youtube_url      = COALESCE(NULLIF(k.youtube_url, ''), a.youtube_url),
    image_url        = COALESCE(NULLIF(k.image_url, ''), a.image_url),
    updated_at       = now()
  FROM public.exercise_library a
  WHERE k.id = keep_id AND a.id = absorb_id;

  -- (b) junction merge: unique absorb tags survive (composite PKs dedupe)
  INSERT INTO public.exercise_library_muscles (exercise_library_id, muscle_name, is_primary)
  SELECT keep_id, m.muscle_name, m.is_primary
  FROM public.exercise_library_muscles m
  WHERE m.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  INSERT INTO public.exercise_library_equipment (exercise_library_id, equipment_type_id)
  SELECT keep_id, e.equipment_type_id
  FROM public.exercise_library_equipment e
  WHERE e.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  -- (c) repoint non-junction FK references
  UPDATE public.trial_assessment_items
  SET exercise_library_id = keep_id
  WHERE exercise_library_id = absorb_id;

  -- (d) delete absorb row (its remaining junction rows cascade)
  DELETE FROM public.exercise_library WHERE id = absorb_id;
  RAISE NOTICE 'dedup72: merged EX0452 -> EX0224 (db rear delt fly)';
END $$;

DO $$
DECLARE
  keep_id uuid;
  absorb_id uuid;
BEGIN
  SELECT id INTO keep_id FROM public.exercise_library WHERE code = 'EX0225';
  SELECT id INTO absorb_id FROM public.exercise_library WHERE code = 'EX0453';
  IF keep_id IS NULL OR absorb_id IS NULL THEN
    RAISE NOTICE 'dedup72: SKIP EX0225/EX0453 (row missing — already merged or renamed)';
    RETURN;
  END IF;

  -- (a) metadata merge: fill NULL/empty keep content columns from absorb; never overwrite
  UPDATE public.exercise_library k
  SET
    primary_muscle   = COALESCE(NULLIF(k.primary_muscle, ''), a.primary_muscle),
    secondary_muscle = COALESCE(NULLIF(k.secondary_muscle, ''), a.secondary_muscle),
    equipment        = COALESCE(NULLIF(k.equipment, ''), a.equipment),
    difficulty       = COALESCE(k.difficulty, a.difficulty),
    exercise_type    = COALESCE(NULLIF(k.exercise_type, ''), a.exercise_type),
    type             = COALESCE(NULLIF(k.type, ''), a.type),
    met_value        = COALESCE(k.met_value, a.met_value),
    description      = COALESCE(NULLIF(k.description, ''), a.description),
    safety_notes     = COALESCE(NULLIF(k.safety_notes, ''), a.safety_notes),
    youtube_url      = COALESCE(NULLIF(k.youtube_url, ''), a.youtube_url),
    image_url        = COALESCE(NULLIF(k.image_url, ''), a.image_url),
    updated_at       = now()
  FROM public.exercise_library a
  WHERE k.id = keep_id AND a.id = absorb_id;

  -- (b) junction merge: unique absorb tags survive (composite PKs dedupe)
  INSERT INTO public.exercise_library_muscles (exercise_library_id, muscle_name, is_primary)
  SELECT keep_id, m.muscle_name, m.is_primary
  FROM public.exercise_library_muscles m
  WHERE m.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  INSERT INTO public.exercise_library_equipment (exercise_library_id, equipment_type_id)
  SELECT keep_id, e.equipment_type_id
  FROM public.exercise_library_equipment e
  WHERE e.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  -- (c) repoint non-junction FK references
  UPDATE public.trial_assessment_items
  SET exercise_library_id = keep_id
  WHERE exercise_library_id = absorb_id;

  -- (d) delete absorb row (its remaining junction rows cascade)
  DELETE FROM public.exercise_library WHERE id = absorb_id;
  RAISE NOTICE 'dedup72: merged EX0453 -> EX0225 (face pull)';
END $$;

DO $$
DECLARE
  keep_id uuid;
  absorb_id uuid;
BEGIN
  SELECT id INTO keep_id FROM public.exercise_library WHERE code = 'EX0226';
  SELECT id INTO absorb_id FROM public.exercise_library WHERE code = 'EX0454';
  IF keep_id IS NULL OR absorb_id IS NULL THEN
    RAISE NOTICE 'dedup72: SKIP EX0226/EX0454 (row missing — already merged or renamed)';
    RETURN;
  END IF;

  -- (a) metadata merge: fill NULL/empty keep content columns from absorb; never overwrite
  UPDATE public.exercise_library k
  SET
    primary_muscle   = COALESCE(NULLIF(k.primary_muscle, ''), a.primary_muscle),
    secondary_muscle = COALESCE(NULLIF(k.secondary_muscle, ''), a.secondary_muscle),
    equipment        = COALESCE(NULLIF(k.equipment, ''), a.equipment),
    difficulty       = COALESCE(k.difficulty, a.difficulty),
    exercise_type    = COALESCE(NULLIF(k.exercise_type, ''), a.exercise_type),
    type             = COALESCE(NULLIF(k.type, ''), a.type),
    met_value        = COALESCE(k.met_value, a.met_value),
    description      = COALESCE(NULLIF(k.description, ''), a.description),
    safety_notes     = COALESCE(NULLIF(k.safety_notes, ''), a.safety_notes),
    youtube_url      = COALESCE(NULLIF(k.youtube_url, ''), a.youtube_url),
    image_url        = COALESCE(NULLIF(k.image_url, ''), a.image_url),
    updated_at       = now()
  FROM public.exercise_library a
  WHERE k.id = keep_id AND a.id = absorb_id;

  -- (b) junction merge: unique absorb tags survive (composite PKs dedupe)
  INSERT INTO public.exercise_library_muscles (exercise_library_id, muscle_name, is_primary)
  SELECT keep_id, m.muscle_name, m.is_primary
  FROM public.exercise_library_muscles m
  WHERE m.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  INSERT INTO public.exercise_library_equipment (exercise_library_id, equipment_type_id)
  SELECT keep_id, e.equipment_type_id
  FROM public.exercise_library_equipment e
  WHERE e.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  -- (c) repoint non-junction FK references
  UPDATE public.trial_assessment_items
  SET exercise_library_id = keep_id
  WHERE exercise_library_id = absorb_id;

  -- (d) delete absorb row (its remaining junction rows cascade)
  DELETE FROM public.exercise_library WHERE id = absorb_id;
  RAISE NOTICE 'dedup72: merged EX0454 -> EX0226 (face pull with external rotation)';
END $$;

DO $$
DECLARE
  keep_id uuid;
  absorb_id uuid;
BEGIN
  SELECT id INTO keep_id FROM public.exercise_library WHERE code = 'EX0227';
  SELECT id INTO absorb_id FROM public.exercise_library WHERE code = 'EX0455';
  IF keep_id IS NULL OR absorb_id IS NULL THEN
    RAISE NOTICE 'dedup72: SKIP EX0227/EX0455 (row missing — already merged or renamed)';
    RETURN;
  END IF;

  -- (a) metadata merge: fill NULL/empty keep content columns from absorb; never overwrite
  UPDATE public.exercise_library k
  SET
    primary_muscle   = COALESCE(NULLIF(k.primary_muscle, ''), a.primary_muscle),
    secondary_muscle = COALESCE(NULLIF(k.secondary_muscle, ''), a.secondary_muscle),
    equipment        = COALESCE(NULLIF(k.equipment, ''), a.equipment),
    difficulty       = COALESCE(k.difficulty, a.difficulty),
    exercise_type    = COALESCE(NULLIF(k.exercise_type, ''), a.exercise_type),
    type             = COALESCE(NULLIF(k.type, ''), a.type),
    met_value        = COALESCE(k.met_value, a.met_value),
    description      = COALESCE(NULLIF(k.description, ''), a.description),
    safety_notes     = COALESCE(NULLIF(k.safety_notes, ''), a.safety_notes),
    youtube_url      = COALESCE(NULLIF(k.youtube_url, ''), a.youtube_url),
    image_url        = COALESCE(NULLIF(k.image_url, ''), a.image_url),
    updated_at       = now()
  FROM public.exercise_library a
  WHERE k.id = keep_id AND a.id = absorb_id;

  -- (b) junction merge: unique absorb tags survive (composite PKs dedupe)
  INSERT INTO public.exercise_library_muscles (exercise_library_id, muscle_name, is_primary)
  SELECT keep_id, m.muscle_name, m.is_primary
  FROM public.exercise_library_muscles m
  WHERE m.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  INSERT INTO public.exercise_library_equipment (exercise_library_id, equipment_type_id)
  SELECT keep_id, e.equipment_type_id
  FROM public.exercise_library_equipment e
  WHERE e.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  -- (c) repoint non-junction FK references
  UPDATE public.trial_assessment_items
  SET exercise_library_id = keep_id
  WHERE exercise_library_id = absorb_id;

  -- (d) delete absorb row (its remaining junction rows cascade)
  DELETE FROM public.exercise_library WHERE id = absorb_id;
  RAISE NOTICE 'dedup72: merged EX0455 -> EX0227 (machine rear delt fly)';
END $$;

DO $$
DECLARE
  keep_id uuid;
  absorb_id uuid;
BEGIN
  SELECT id INTO keep_id FROM public.exercise_library WHERE code = 'EX0229';
  SELECT id INTO absorb_id FROM public.exercise_library WHERE code = 'EX0457';
  IF keep_id IS NULL OR absorb_id IS NULL THEN
    RAISE NOTICE 'dedup72: SKIP EX0229/EX0457 (row missing — already merged or renamed)';
    RETURN;
  END IF;

  -- (a) metadata merge: fill NULL/empty keep content columns from absorb; never overwrite
  UPDATE public.exercise_library k
  SET
    primary_muscle   = COALESCE(NULLIF(k.primary_muscle, ''), a.primary_muscle),
    secondary_muscle = COALESCE(NULLIF(k.secondary_muscle, ''), a.secondary_muscle),
    equipment        = COALESCE(NULLIF(k.equipment, ''), a.equipment),
    difficulty       = COALESCE(k.difficulty, a.difficulty),
    exercise_type    = COALESCE(NULLIF(k.exercise_type, ''), a.exercise_type),
    type             = COALESCE(NULLIF(k.type, ''), a.type),
    met_value        = COALESCE(k.met_value, a.met_value),
    description      = COALESCE(NULLIF(k.description, ''), a.description),
    safety_notes     = COALESCE(NULLIF(k.safety_notes, ''), a.safety_notes),
    youtube_url      = COALESCE(NULLIF(k.youtube_url, ''), a.youtube_url),
    image_url        = COALESCE(NULLIF(k.image_url, ''), a.image_url),
    updated_at       = now()
  FROM public.exercise_library a
  WHERE k.id = keep_id AND a.id = absorb_id;

  -- (b) junction merge: unique absorb tags survive (composite PKs dedupe)
  INSERT INTO public.exercise_library_muscles (exercise_library_id, muscle_name, is_primary)
  SELECT keep_id, m.muscle_name, m.is_primary
  FROM public.exercise_library_muscles m
  WHERE m.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  INSERT INTO public.exercise_library_equipment (exercise_library_id, equipment_type_id)
  SELECT keep_id, e.equipment_type_id
  FROM public.exercise_library_equipment e
  WHERE e.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  -- (c) repoint non-junction FK references
  UPDATE public.trial_assessment_items
  SET exercise_library_id = keep_id
  WHERE exercise_library_id = absorb_id;

  -- (d) delete absorb row (its remaining junction rows cascade)
  DELETE FROM public.exercise_library WHERE id = absorb_id;
  RAISE NOTICE 'dedup72: merged EX0457 -> EX0229 (unilateral sots press)';
END $$;

DO $$
DECLARE
  keep_id uuid;
  absorb_id uuid;
BEGIN
  SELECT id INTO keep_id FROM public.exercise_library WHERE code = 'EX0230';
  SELECT id INTO absorb_id FROM public.exercise_library WHERE code = 'EX0458';
  IF keep_id IS NULL OR absorb_id IS NULL THEN
    RAISE NOTICE 'dedup72: SKIP EX0230/EX0458 (row missing — already merged or renamed)';
    RETURN;
  END IF;

  -- (a) metadata merge: fill NULL/empty keep content columns from absorb; never overwrite
  UPDATE public.exercise_library k
  SET
    primary_muscle   = COALESCE(NULLIF(k.primary_muscle, ''), a.primary_muscle),
    secondary_muscle = COALESCE(NULLIF(k.secondary_muscle, ''), a.secondary_muscle),
    equipment        = COALESCE(NULLIF(k.equipment, ''), a.equipment),
    difficulty       = COALESCE(k.difficulty, a.difficulty),
    exercise_type    = COALESCE(NULLIF(k.exercise_type, ''), a.exercise_type),
    type             = COALESCE(NULLIF(k.type, ''), a.type),
    met_value        = COALESCE(k.met_value, a.met_value),
    description      = COALESCE(NULLIF(k.description, ''), a.description),
    safety_notes     = COALESCE(NULLIF(k.safety_notes, ''), a.safety_notes),
    youtube_url      = COALESCE(NULLIF(k.youtube_url, ''), a.youtube_url),
    image_url        = COALESCE(NULLIF(k.image_url, ''), a.image_url),
    updated_at       = now()
  FROM public.exercise_library a
  WHERE k.id = keep_id AND a.id = absorb_id;

  -- (b) junction merge: unique absorb tags survive (composite PKs dedupe)
  INSERT INTO public.exercise_library_muscles (exercise_library_id, muscle_name, is_primary)
  SELECT keep_id, m.muscle_name, m.is_primary
  FROM public.exercise_library_muscles m
  WHERE m.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  INSERT INTO public.exercise_library_equipment (exercise_library_id, equipment_type_id)
  SELECT keep_id, e.equipment_type_id
  FROM public.exercise_library_equipment e
  WHERE e.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  -- (c) repoint non-junction FK references
  UPDATE public.trial_assessment_items
  SET exercise_library_id = keep_id
  WHERE exercise_library_id = absorb_id;

  -- (d) delete absorb row (its remaining junction rows cascade)
  DELETE FROM public.exercise_library WHERE id = absorb_id;
  RAISE NOTICE 'dedup72: merged EX0458 -> EX0230 (upper back cable row wide grip)';
END $$;

DO $$
DECLARE
  keep_id uuid;
  absorb_id uuid;
BEGIN
  SELECT id INTO keep_id FROM public.exercise_library WHERE code = 'EX0233';
  SELECT id INTO absorb_id FROM public.exercise_library WHERE code = 'EX0392';
  IF keep_id IS NULL OR absorb_id IS NULL THEN
    RAISE NOTICE 'dedup72: SKIP EX0233/EX0392 (row missing — already merged or renamed)';
    RETURN;
  END IF;

  -- (a) metadata merge: fill NULL/empty keep content columns from absorb; never overwrite
  UPDATE public.exercise_library k
  SET
    primary_muscle   = COALESCE(NULLIF(k.primary_muscle, ''), a.primary_muscle),
    secondary_muscle = COALESCE(NULLIF(k.secondary_muscle, ''), a.secondary_muscle),
    equipment        = COALESCE(NULLIF(k.equipment, ''), a.equipment),
    difficulty       = COALESCE(k.difficulty, a.difficulty),
    exercise_type    = COALESCE(NULLIF(k.exercise_type, ''), a.exercise_type),
    type             = COALESCE(NULLIF(k.type, ''), a.type),
    met_value        = COALESCE(k.met_value, a.met_value),
    description      = COALESCE(NULLIF(k.description, ''), a.description),
    safety_notes     = COALESCE(NULLIF(k.safety_notes, ''), a.safety_notes),
    youtube_url      = COALESCE(NULLIF(k.youtube_url, ''), a.youtube_url),
    image_url        = COALESCE(NULLIF(k.image_url, ''), a.image_url),
    updated_at       = now()
  FROM public.exercise_library a
  WHERE k.id = keep_id AND a.id = absorb_id;

  -- (b) junction merge: unique absorb tags survive (composite PKs dedupe)
  INSERT INTO public.exercise_library_muscles (exercise_library_id, muscle_name, is_primary)
  SELECT keep_id, m.muscle_name, m.is_primary
  FROM public.exercise_library_muscles m
  WHERE m.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  INSERT INTO public.exercise_library_equipment (exercise_library_id, equipment_type_id)
  SELECT keep_id, e.equipment_type_id
  FROM public.exercise_library_equipment e
  WHERE e.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  -- (c) repoint non-junction FK references
  UPDATE public.trial_assessment_items
  SET exercise_library_id = keep_id
  WHERE exercise_library_id = absorb_id;

  -- (d) delete absorb row (its remaining junction rows cascade)
  DELETE FROM public.exercise_library WHERE id = absorb_id;
  RAISE NOTICE 'dedup72: merged EX0392 -> EX0233 (30 db incline spider curl)';
END $$;

DO $$
DECLARE
  keep_id uuid;
  absorb_id uuid;
BEGIN
  SELECT id INTO keep_id FROM public.exercise_library WHERE code = 'EX0234';
  SELECT id INTO absorb_id FROM public.exercise_library WHERE code = 'EX0396';
  IF keep_id IS NULL OR absorb_id IS NULL THEN
    RAISE NOTICE 'dedup72: SKIP EX0234/EX0396 (row missing — already merged or renamed)';
    RETURN;
  END IF;

  -- (a) metadata merge: fill NULL/empty keep content columns from absorb; never overwrite
  UPDATE public.exercise_library k
  SET
    primary_muscle   = COALESCE(NULLIF(k.primary_muscle, ''), a.primary_muscle),
    secondary_muscle = COALESCE(NULLIF(k.secondary_muscle, ''), a.secondary_muscle),
    equipment        = COALESCE(NULLIF(k.equipment, ''), a.equipment),
    difficulty       = COALESCE(k.difficulty, a.difficulty),
    exercise_type    = COALESCE(NULLIF(k.exercise_type, ''), a.exercise_type),
    type             = COALESCE(NULLIF(k.type, ''), a.type),
    met_value        = COALESCE(k.met_value, a.met_value),
    description      = COALESCE(NULLIF(k.description, ''), a.description),
    safety_notes     = COALESCE(NULLIF(k.safety_notes, ''), a.safety_notes),
    youtube_url      = COALESCE(NULLIF(k.youtube_url, ''), a.youtube_url),
    image_url        = COALESCE(NULLIF(k.image_url, ''), a.image_url),
    updated_at       = now()
  FROM public.exercise_library a
  WHERE k.id = keep_id AND a.id = absorb_id;

  -- (b) junction merge: unique absorb tags survive (composite PKs dedupe)
  INSERT INTO public.exercise_library_muscles (exercise_library_id, muscle_name, is_primary)
  SELECT keep_id, m.muscle_name, m.is_primary
  FROM public.exercise_library_muscles m
  WHERE m.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  INSERT INTO public.exercise_library_equipment (exercise_library_id, equipment_type_id)
  SELECT keep_id, e.equipment_type_id
  FROM public.exercise_library_equipment e
  WHERE e.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  -- (c) repoint non-junction FK references
  UPDATE public.trial_assessment_items
  SET exercise_library_id = keep_id
  WHERE exercise_library_id = absorb_id;

  -- (d) delete absorb row (its remaining junction rows cascade)
  DELETE FROM public.exercise_library WHERE id = absorb_id;
  RAISE NOTICE 'dedup72: merged EX0396 -> EX0234 (50 ez bar incline spider curl)';
END $$;

DO $$
DECLARE
  keep_id uuid;
  absorb_id uuid;
BEGIN
  SELECT id INTO keep_id FROM public.exercise_library WHERE code = 'EX0235';
  SELECT id INTO absorb_id FROM public.exercise_library WHERE code = 'EX0397';
  IF keep_id IS NULL OR absorb_id IS NULL THEN
    RAISE NOTICE 'dedup72: SKIP EX0235/EX0397 (row missing — already merged or renamed)';
    RETURN;
  END IF;

  -- (a) metadata merge: fill NULL/empty keep content columns from absorb; never overwrite
  UPDATE public.exercise_library k
  SET
    primary_muscle   = COALESCE(NULLIF(k.primary_muscle, ''), a.primary_muscle),
    secondary_muscle = COALESCE(NULLIF(k.secondary_muscle, ''), a.secondary_muscle),
    equipment        = COALESCE(NULLIF(k.equipment, ''), a.equipment),
    difficulty       = COALESCE(k.difficulty, a.difficulty),
    exercise_type    = COALESCE(NULLIF(k.exercise_type, ''), a.exercise_type),
    type             = COALESCE(NULLIF(k.type, ''), a.type),
    met_value        = COALESCE(k.met_value, a.met_value),
    description      = COALESCE(NULLIF(k.description, ''), a.description),
    safety_notes     = COALESCE(NULLIF(k.safety_notes, ''), a.safety_notes),
    youtube_url      = COALESCE(NULLIF(k.youtube_url, ''), a.youtube_url),
    image_url        = COALESCE(NULLIF(k.image_url, ''), a.image_url),
    updated_at       = now()
  FROM public.exercise_library a
  WHERE k.id = keep_id AND a.id = absorb_id;

  -- (b) junction merge: unique absorb tags survive (composite PKs dedupe)
  INSERT INTO public.exercise_library_muscles (exercise_library_id, muscle_name, is_primary)
  SELECT keep_id, m.muscle_name, m.is_primary
  FROM public.exercise_library_muscles m
  WHERE m.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  INSERT INTO public.exercise_library_equipment (exercise_library_id, equipment_type_id)
  SELECT keep_id, e.equipment_type_id
  FROM public.exercise_library_equipment e
  WHERE e.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  -- (c) repoint non-junction FK references
  UPDATE public.trial_assessment_items
  SET exercise_library_id = keep_id
  WHERE exercise_library_id = absorb_id;

  -- (d) delete absorb row (its remaining junction rows cascade)
  DELETE FROM public.exercise_library WHERE id = absorb_id;
  RAISE NOTICE 'dedup72: merged EX0397 -> EX0235 (60 db incline curl)';
END $$;

DO $$
DECLARE
  keep_id uuid;
  absorb_id uuid;
BEGIN
  SELECT id INTO keep_id FROM public.exercise_library WHERE code = 'EX0236';
  SELECT id INTO absorb_id FROM public.exercise_library WHERE code = 'EX0398';
  IF keep_id IS NULL OR absorb_id IS NULL THEN
    RAISE NOTICE 'dedup72: SKIP EX0236/EX0398 (row missing — already merged or renamed)';
    RETURN;
  END IF;

  -- (a) metadata merge: fill NULL/empty keep content columns from absorb; never overwrite
  UPDATE public.exercise_library k
  SET
    primary_muscle   = COALESCE(NULLIF(k.primary_muscle, ''), a.primary_muscle),
    secondary_muscle = COALESCE(NULLIF(k.secondary_muscle, ''), a.secondary_muscle),
    equipment        = COALESCE(NULLIF(k.equipment, ''), a.equipment),
    difficulty       = COALESCE(k.difficulty, a.difficulty),
    exercise_type    = COALESCE(NULLIF(k.exercise_type, ''), a.exercise_type),
    type             = COALESCE(NULLIF(k.type, ''), a.type),
    met_value        = COALESCE(k.met_value, a.met_value),
    description      = COALESCE(NULLIF(k.description, ''), a.description),
    safety_notes     = COALESCE(NULLIF(k.safety_notes, ''), a.safety_notes),
    youtube_url      = COALESCE(NULLIF(k.youtube_url, ''), a.youtube_url),
    image_url        = COALESCE(NULLIF(k.image_url, ''), a.image_url),
    updated_at       = now()
  FROM public.exercise_library a
  WHERE k.id = keep_id AND a.id = absorb_id;

  -- (b) junction merge: unique absorb tags survive (composite PKs dedupe)
  INSERT INTO public.exercise_library_muscles (exercise_library_id, muscle_name, is_primary)
  SELECT keep_id, m.muscle_name, m.is_primary
  FROM public.exercise_library_muscles m
  WHERE m.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  INSERT INTO public.exercise_library_equipment (exercise_library_id, equipment_type_id)
  SELECT keep_id, e.equipment_type_id
  FROM public.exercise_library_equipment e
  WHERE e.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  -- (c) repoint non-junction FK references
  UPDATE public.trial_assessment_items
  SET exercise_library_id = keep_id
  WHERE exercise_library_id = absorb_id;

  -- (d) delete absorb row (its remaining junction rows cascade)
  DELETE FROM public.exercise_library WHERE id = absorb_id;
  RAISE NOTICE 'dedup72: merged EX0398 -> EX0236 (80 db incline curl)';
END $$;

DO $$
DECLARE
  keep_id uuid;
  absorb_id uuid;
BEGIN
  SELECT id INTO keep_id FROM public.exercise_library WHERE code = 'EX0237';
  SELECT id INTO absorb_id FROM public.exercise_library WHERE code = 'EX0335';
  IF keep_id IS NULL OR absorb_id IS NULL THEN
    RAISE NOTICE 'dedup72: SKIP EX0237/EX0335 (row missing — already merged or renamed)';
    RETURN;
  END IF;

  -- (a) metadata merge: fill NULL/empty keep content columns from absorb; never overwrite
  UPDATE public.exercise_library k
  SET
    primary_muscle   = COALESCE(NULLIF(k.primary_muscle, ''), a.primary_muscle),
    secondary_muscle = COALESCE(NULLIF(k.secondary_muscle, ''), a.secondary_muscle),
    equipment        = COALESCE(NULLIF(k.equipment, ''), a.equipment),
    difficulty       = COALESCE(k.difficulty, a.difficulty),
    exercise_type    = COALESCE(NULLIF(k.exercise_type, ''), a.exercise_type),
    type             = COALESCE(NULLIF(k.type, ''), a.type),
    met_value        = COALESCE(k.met_value, a.met_value),
    description      = COALESCE(NULLIF(k.description, ''), a.description),
    safety_notes     = COALESCE(NULLIF(k.safety_notes, ''), a.safety_notes),
    youtube_url      = COALESCE(NULLIF(k.youtube_url, ''), a.youtube_url),
    image_url        = COALESCE(NULLIF(k.image_url, ''), a.image_url),
    updated_at       = now()
  FROM public.exercise_library a
  WHERE k.id = keep_id AND a.id = absorb_id;

  -- (b) junction merge: unique absorb tags survive (composite PKs dedupe)
  INSERT INTO public.exercise_library_muscles (exercise_library_id, muscle_name, is_primary)
  SELECT keep_id, m.muscle_name, m.is_primary
  FROM public.exercise_library_muscles m
  WHERE m.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  INSERT INTO public.exercise_library_equipment (exercise_library_id, equipment_type_id)
  SELECT keep_id, e.equipment_type_id
  FROM public.exercise_library_equipment e
  WHERE e.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  -- (c) repoint non-junction FK references
  UPDATE public.trial_assessment_items
  SET exercise_library_id = keep_id
  WHERE exercise_library_id = absorb_id;

  -- (d) delete absorb row (its remaining junction rows cascade)
  DELETE FROM public.exercise_library WHERE id = absorb_id;
  RAISE NOTICE 'dedup72: merged EX0335 -> EX0237 (banded palof press)';
END $$;

DO $$
DECLARE
  keep_id uuid;
  absorb_id uuid;
BEGIN
  SELECT id INTO keep_id FROM public.exercise_library WHERE code = 'EX0237';
  SELECT id INTO absorb_id FROM public.exercise_library WHERE code = 'EX0366';
  IF keep_id IS NULL OR absorb_id IS NULL THEN
    RAISE NOTICE 'dedup72: SKIP EX0237/EX0366 (row missing — already merged or renamed)';
    RETURN;
  END IF;

  -- (a) metadata merge: fill NULL/empty keep content columns from absorb; never overwrite
  UPDATE public.exercise_library k
  SET
    primary_muscle   = COALESCE(NULLIF(k.primary_muscle, ''), a.primary_muscle),
    secondary_muscle = COALESCE(NULLIF(k.secondary_muscle, ''), a.secondary_muscle),
    equipment        = COALESCE(NULLIF(k.equipment, ''), a.equipment),
    difficulty       = COALESCE(k.difficulty, a.difficulty),
    exercise_type    = COALESCE(NULLIF(k.exercise_type, ''), a.exercise_type),
    type             = COALESCE(NULLIF(k.type, ''), a.type),
    met_value        = COALESCE(k.met_value, a.met_value),
    description      = COALESCE(NULLIF(k.description, ''), a.description),
    safety_notes     = COALESCE(NULLIF(k.safety_notes, ''), a.safety_notes),
    youtube_url      = COALESCE(NULLIF(k.youtube_url, ''), a.youtube_url),
    image_url        = COALESCE(NULLIF(k.image_url, ''), a.image_url),
    updated_at       = now()
  FROM public.exercise_library a
  WHERE k.id = keep_id AND a.id = absorb_id;

  -- (b) junction merge: unique absorb tags survive (composite PKs dedupe)
  INSERT INTO public.exercise_library_muscles (exercise_library_id, muscle_name, is_primary)
  SELECT keep_id, m.muscle_name, m.is_primary
  FROM public.exercise_library_muscles m
  WHERE m.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  INSERT INTO public.exercise_library_equipment (exercise_library_id, equipment_type_id)
  SELECT keep_id, e.equipment_type_id
  FROM public.exercise_library_equipment e
  WHERE e.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  -- (c) repoint non-junction FK references
  UPDATE public.trial_assessment_items
  SET exercise_library_id = keep_id
  WHERE exercise_library_id = absorb_id;

  -- (d) delete absorb row (its remaining junction rows cascade)
  DELETE FROM public.exercise_library WHERE id = absorb_id;
  RAISE NOTICE 'dedup72: merged EX0366 -> EX0237 (banded palof press)';
END $$;

DO $$
DECLARE
  keep_id uuid;
  absorb_id uuid;
BEGIN
  SELECT id INTO keep_id FROM public.exercise_library WHERE code = 'EX0239';
  SELECT id INTO absorb_id FROM public.exercise_library WHERE code = 'EX0367';
  IF keep_id IS NULL OR absorb_id IS NULL THEN
    RAISE NOTICE 'dedup72: SKIP EX0239/EX0367 (row missing — already merged or renamed)';
    RETURN;
  END IF;

  -- (a) metadata merge: fill NULL/empty keep content columns from absorb; never overwrite
  UPDATE public.exercise_library k
  SET
    primary_muscle   = COALESCE(NULLIF(k.primary_muscle, ''), a.primary_muscle),
    secondary_muscle = COALESCE(NULLIF(k.secondary_muscle, ''), a.secondary_muscle),
    equipment        = COALESCE(NULLIF(k.equipment, ''), a.equipment),
    difficulty       = COALESCE(k.difficulty, a.difficulty),
    exercise_type    = COALESCE(NULLIF(k.exercise_type, ''), a.exercise_type),
    type             = COALESCE(NULLIF(k.type, ''), a.type),
    met_value        = COALESCE(k.met_value, a.met_value),
    description      = COALESCE(NULLIF(k.description, ''), a.description),
    safety_notes     = COALESCE(NULLIF(k.safety_notes, ''), a.safety_notes),
    youtube_url      = COALESCE(NULLIF(k.youtube_url, ''), a.youtube_url),
    image_url        = COALESCE(NULLIF(k.image_url, ''), a.image_url),
    updated_at       = now()
  FROM public.exercise_library a
  WHERE k.id = keep_id AND a.id = absorb_id;

  -- (b) junction merge: unique absorb tags survive (composite PKs dedupe)
  INSERT INTO public.exercise_library_muscles (exercise_library_id, muscle_name, is_primary)
  SELECT keep_id, m.muscle_name, m.is_primary
  FROM public.exercise_library_muscles m
  WHERE m.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  INSERT INTO public.exercise_library_equipment (exercise_library_id, equipment_type_id)
  SELECT keep_id, e.equipment_type_id
  FROM public.exercise_library_equipment e
  WHERE e.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  -- (c) repoint non-junction FK references
  UPDATE public.trial_assessment_items
  SET exercise_library_id = keep_id
  WHERE exercise_library_id = absorb_id;

  -- (d) delete absorb row (its remaining junction rows cascade)
  DELETE FROM public.exercise_library WHERE id = absorb_id;
  RAISE NOTICE 'dedup72: merged EX0367 -> EX0239 (bear crawl)';
END $$;

DO $$
DECLARE
  keep_id uuid;
  absorb_id uuid;
BEGIN
  SELECT id INTO keep_id FROM public.exercise_library WHERE code = 'EX0240';
  SELECT id INTO absorb_id FROM public.exercise_library WHERE code = 'EX0337';
  IF keep_id IS NULL OR absorb_id IS NULL THEN
    RAISE NOTICE 'dedup72: SKIP EX0240/EX0337 (row missing — already merged or renamed)';
    RETURN;
  END IF;

  -- (a) metadata merge: fill NULL/empty keep content columns from absorb; never overwrite
  UPDATE public.exercise_library k
  SET
    primary_muscle   = COALESCE(NULLIF(k.primary_muscle, ''), a.primary_muscle),
    secondary_muscle = COALESCE(NULLIF(k.secondary_muscle, ''), a.secondary_muscle),
    equipment        = COALESCE(NULLIF(k.equipment, ''), a.equipment),
    difficulty       = COALESCE(k.difficulty, a.difficulty),
    exercise_type    = COALESCE(NULLIF(k.exercise_type, ''), a.exercise_type),
    type             = COALESCE(NULLIF(k.type, ''), a.type),
    met_value        = COALESCE(k.met_value, a.met_value),
    description      = COALESCE(NULLIF(k.description, ''), a.description),
    safety_notes     = COALESCE(NULLIF(k.safety_notes, ''), a.safety_notes),
    youtube_url      = COALESCE(NULLIF(k.youtube_url, ''), a.youtube_url),
    image_url        = COALESCE(NULLIF(k.image_url, ''), a.image_url),
    updated_at       = now()
  FROM public.exercise_library a
  WHERE k.id = keep_id AND a.id = absorb_id;

  -- (b) junction merge: unique absorb tags survive (composite PKs dedupe)
  INSERT INTO public.exercise_library_muscles (exercise_library_id, muscle_name, is_primary)
  SELECT keep_id, m.muscle_name, m.is_primary
  FROM public.exercise_library_muscles m
  WHERE m.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  INSERT INTO public.exercise_library_equipment (exercise_library_id, equipment_type_id)
  SELECT keep_id, e.equipment_type_id
  FROM public.exercise_library_equipment e
  WHERE e.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  -- (c) repoint non-junction FK references
  UPDATE public.trial_assessment_items
  SET exercise_library_id = keep_id
  WHERE exercise_library_id = absorb_id;

  -- (d) delete absorb row (its remaining junction rows cascade)
  DELETE FROM public.exercise_library WHERE id = absorb_id;
  RAISE NOTICE 'dedup72: merged EX0337 -> EX0240 (bike sprints)';
END $$;

DO $$
DECLARE
  keep_id uuid;
  absorb_id uuid;
BEGIN
  SELECT id INTO keep_id FROM public.exercise_library WHERE code = 'EX0241';
  SELECT id INTO absorb_id FROM public.exercise_library WHERE code = 'EX0439';
  IF keep_id IS NULL OR absorb_id IS NULL THEN
    RAISE NOTICE 'dedup72: SKIP EX0241/EX0439 (row missing — already merged or renamed)';
    RETURN;
  END IF;

  -- (a) metadata merge: fill NULL/empty keep content columns from absorb; never overwrite
  UPDATE public.exercise_library k
  SET
    primary_muscle   = COALESCE(NULLIF(k.primary_muscle, ''), a.primary_muscle),
    secondary_muscle = COALESCE(NULLIF(k.secondary_muscle, ''), a.secondary_muscle),
    equipment        = COALESCE(NULLIF(k.equipment, ''), a.equipment),
    difficulty       = COALESCE(k.difficulty, a.difficulty),
    exercise_type    = COALESCE(NULLIF(k.exercise_type, ''), a.exercise_type),
    type             = COALESCE(NULLIF(k.type, ''), a.type),
    met_value        = COALESCE(k.met_value, a.met_value),
    description      = COALESCE(NULLIF(k.description, ''), a.description),
    safety_notes     = COALESCE(NULLIF(k.safety_notes, ''), a.safety_notes),
    youtube_url      = COALESCE(NULLIF(k.youtube_url, ''), a.youtube_url),
    image_url        = COALESCE(NULLIF(k.image_url, ''), a.image_url),
    updated_at       = now()
  FROM public.exercise_library a
  WHERE k.id = keep_id AND a.id = absorb_id;

  -- (b) junction merge: unique absorb tags survive (composite PKs dedupe)
  INSERT INTO public.exercise_library_muscles (exercise_library_id, muscle_name, is_primary)
  SELECT keep_id, m.muscle_name, m.is_primary
  FROM public.exercise_library_muscles m
  WHERE m.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  INSERT INTO public.exercise_library_equipment (exercise_library_id, equipment_type_id)
  SELECT keep_id, e.equipment_type_id
  FROM public.exercise_library_equipment e
  WHERE e.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  -- (c) repoint non-junction FK references
  UPDATE public.trial_assessment_items
  SET exercise_library_id = keep_id
  WHERE exercise_library_id = absorb_id;

  -- (d) delete absorb row (its remaining junction rows cascade)
  DELETE FROM public.exercise_library WHERE id = absorb_id;
  RAISE NOTICE 'dedup72: merged EX0439 -> EX0241 (bilateral sots press)';
END $$;

DO $$
DECLARE
  keep_id uuid;
  absorb_id uuid;
BEGIN
  SELECT id INTO keep_id FROM public.exercise_library WHERE code = 'EX0243';
  SELECT id INTO absorb_id FROM public.exercise_library WHERE code = 'EX0400';
  IF keep_id IS NULL OR absorb_id IS NULL THEN
    RAISE NOTICE 'dedup72: SKIP EX0243/EX0400 (row missing — already merged or renamed)';
    RETURN;
  END IF;

  -- (a) metadata merge: fill NULL/empty keep content columns from absorb; never overwrite
  UPDATE public.exercise_library k
  SET
    primary_muscle   = COALESCE(NULLIF(k.primary_muscle, ''), a.primary_muscle),
    secondary_muscle = COALESCE(NULLIF(k.secondary_muscle, ''), a.secondary_muscle),
    equipment        = COALESCE(NULLIF(k.equipment, ''), a.equipment),
    difficulty       = COALESCE(k.difficulty, a.difficulty),
    exercise_type    = COALESCE(NULLIF(k.exercise_type, ''), a.exercise_type),
    type             = COALESCE(NULLIF(k.type, ''), a.type),
    met_value        = COALESCE(k.met_value, a.met_value),
    description      = COALESCE(NULLIF(k.description, ''), a.description),
    safety_notes     = COALESCE(NULLIF(k.safety_notes, ''), a.safety_notes),
    youtube_url      = COALESCE(NULLIF(k.youtube_url, ''), a.youtube_url),
    image_url        = COALESCE(NULLIF(k.image_url, ''), a.image_url),
    updated_at       = now()
  FROM public.exercise_library a
  WHERE k.id = keep_id AND a.id = absorb_id;

  -- (b) junction merge: unique absorb tags survive (composite PKs dedupe)
  INSERT INTO public.exercise_library_muscles (exercise_library_id, muscle_name, is_primary)
  SELECT keep_id, m.muscle_name, m.is_primary
  FROM public.exercise_library_muscles m
  WHERE m.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  INSERT INTO public.exercise_library_equipment (exercise_library_id, equipment_type_id)
  SELECT keep_id, e.equipment_type_id
  FROM public.exercise_library_equipment e
  WHERE e.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  -- (c) repoint non-junction FK references
  UPDATE public.trial_assessment_items
  SET exercise_library_id = keep_id
  WHERE exercise_library_id = absorb_id;

  -- (d) delete absorb row (its remaining junction rows cascade)
  DELETE FROM public.exercise_library WHERE id = absorb_id;
  RAISE NOTICE 'dedup72: merged EX0400 -> EX0243 (cable drag curl)';
END $$;

DO $$
DECLARE
  keep_id uuid;
  absorb_id uuid;
BEGIN
  SELECT id INTO keep_id FROM public.exercise_library WHERE code = 'EX0245';
  SELECT id INTO absorb_id FROM public.exercise_library WHERE code = 'EX0338';
  IF keep_id IS NULL OR absorb_id IS NULL THEN
    RAISE NOTICE 'dedup72: SKIP EX0245/EX0338 (row missing — already merged or renamed)';
    RETURN;
  END IF;

  -- (a) metadata merge: fill NULL/empty keep content columns from absorb; never overwrite
  UPDATE public.exercise_library k
  SET
    primary_muscle   = COALESCE(NULLIF(k.primary_muscle, ''), a.primary_muscle),
    secondary_muscle = COALESCE(NULLIF(k.secondary_muscle, ''), a.secondary_muscle),
    equipment        = COALESCE(NULLIF(k.equipment, ''), a.equipment),
    difficulty       = COALESCE(k.difficulty, a.difficulty),
    exercise_type    = COALESCE(NULLIF(k.exercise_type, ''), a.exercise_type),
    type             = COALESCE(NULLIF(k.type, ''), a.type),
    met_value        = COALESCE(k.met_value, a.met_value),
    description      = COALESCE(NULLIF(k.description, ''), a.description),
    safety_notes     = COALESCE(NULLIF(k.safety_notes, ''), a.safety_notes),
    youtube_url      = COALESCE(NULLIF(k.youtube_url, ''), a.youtube_url),
    image_url        = COALESCE(NULLIF(k.image_url, ''), a.image_url),
    updated_at       = now()
  FROM public.exercise_library a
  WHERE k.id = keep_id AND a.id = absorb_id;

  -- (b) junction merge: unique absorb tags survive (composite PKs dedupe)
  INSERT INTO public.exercise_library_muscles (exercise_library_id, muscle_name, is_primary)
  SELECT keep_id, m.muscle_name, m.is_primary
  FROM public.exercise_library_muscles m
  WHERE m.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  INSERT INTO public.exercise_library_equipment (exercise_library_id, equipment_type_id)
  SELECT keep_id, e.equipment_type_id
  FROM public.exercise_library_equipment e
  WHERE e.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  -- (c) repoint non-junction FK references
  UPDATE public.trial_assessment_items
  SET exercise_library_id = keep_id
  WHERE exercise_library_id = absorb_id;

  -- (d) delete absorb row (its remaining junction rows cascade)
  DELETE FROM public.exercise_library WHERE id = absorb_id;
  RAISE NOTICE 'dedup72: merged EX0338 -> EX0245 (cable palof press)';
END $$;

DO $$
DECLARE
  keep_id uuid;
  absorb_id uuid;
BEGIN
  SELECT id INTO keep_id FROM public.exercise_library WHERE code = 'EX0245';
  SELECT id INTO absorb_id FROM public.exercise_library WHERE code = 'EX0368';
  IF keep_id IS NULL OR absorb_id IS NULL THEN
    RAISE NOTICE 'dedup72: SKIP EX0245/EX0368 (row missing — already merged or renamed)';
    RETURN;
  END IF;

  -- (a) metadata merge: fill NULL/empty keep content columns from absorb; never overwrite
  UPDATE public.exercise_library k
  SET
    primary_muscle   = COALESCE(NULLIF(k.primary_muscle, ''), a.primary_muscle),
    secondary_muscle = COALESCE(NULLIF(k.secondary_muscle, ''), a.secondary_muscle),
    equipment        = COALESCE(NULLIF(k.equipment, ''), a.equipment),
    difficulty       = COALESCE(k.difficulty, a.difficulty),
    exercise_type    = COALESCE(NULLIF(k.exercise_type, ''), a.exercise_type),
    type             = COALESCE(NULLIF(k.type, ''), a.type),
    met_value        = COALESCE(k.met_value, a.met_value),
    description      = COALESCE(NULLIF(k.description, ''), a.description),
    safety_notes     = COALESCE(NULLIF(k.safety_notes, ''), a.safety_notes),
    youtube_url      = COALESCE(NULLIF(k.youtube_url, ''), a.youtube_url),
    image_url        = COALESCE(NULLIF(k.image_url, ''), a.image_url),
    updated_at       = now()
  FROM public.exercise_library a
  WHERE k.id = keep_id AND a.id = absorb_id;

  -- (b) junction merge: unique absorb tags survive (composite PKs dedupe)
  INSERT INTO public.exercise_library_muscles (exercise_library_id, muscle_name, is_primary)
  SELECT keep_id, m.muscle_name, m.is_primary
  FROM public.exercise_library_muscles m
  WHERE m.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  INSERT INTO public.exercise_library_equipment (exercise_library_id, equipment_type_id)
  SELECT keep_id, e.equipment_type_id
  FROM public.exercise_library_equipment e
  WHERE e.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  -- (c) repoint non-junction FK references
  UPDATE public.trial_assessment_items
  SET exercise_library_id = keep_id
  WHERE exercise_library_id = absorb_id;

  -- (d) delete absorb row (its remaining junction rows cascade)
  DELETE FROM public.exercise_library WHERE id = absorb_id;
  RAISE NOTICE 'dedup72: merged EX0368 -> EX0245 (cable palof press)';
END $$;

DO $$
DECLARE
  keep_id uuid;
  absorb_id uuid;
BEGIN
  SELECT id INTO keep_id FROM public.exercise_library WHERE code = 'EX0246';
  SELECT id INTO absorb_id FROM public.exercise_library WHERE code = 'EX0401';
  IF keep_id IS NULL OR absorb_id IS NULL THEN
    RAISE NOTICE 'dedup72: SKIP EX0246/EX0401 (row missing — already merged or renamed)';
    RETURN;
  END IF;

  -- (a) metadata merge: fill NULL/empty keep content columns from absorb; never overwrite
  UPDATE public.exercise_library k
  SET
    primary_muscle   = COALESCE(NULLIF(k.primary_muscle, ''), a.primary_muscle),
    secondary_muscle = COALESCE(NULLIF(k.secondary_muscle, ''), a.secondary_muscle),
    equipment        = COALESCE(NULLIF(k.equipment, ''), a.equipment),
    difficulty       = COALESCE(k.difficulty, a.difficulty),
    exercise_type    = COALESCE(NULLIF(k.exercise_type, ''), a.exercise_type),
    type             = COALESCE(NULLIF(k.type, ''), a.type),
    met_value        = COALESCE(k.met_value, a.met_value),
    description      = COALESCE(NULLIF(k.description, ''), a.description),
    safety_notes     = COALESCE(NULLIF(k.safety_notes, ''), a.safety_notes),
    youtube_url      = COALESCE(NULLIF(k.youtube_url, ''), a.youtube_url),
    image_url        = COALESCE(NULLIF(k.image_url, ''), a.image_url),
    updated_at       = now()
  FROM public.exercise_library a
  WHERE k.id = keep_id AND a.id = absorb_id;

  -- (b) junction merge: unique absorb tags survive (composite PKs dedupe)
  INSERT INTO public.exercise_library_muscles (exercise_library_id, muscle_name, is_primary)
  SELECT keep_id, m.muscle_name, m.is_primary
  FROM public.exercise_library_muscles m
  WHERE m.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  INSERT INTO public.exercise_library_equipment (exercise_library_id, equipment_type_id)
  SELECT keep_id, e.equipment_type_id
  FROM public.exercise_library_equipment e
  WHERE e.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  -- (c) repoint non-junction FK references
  UPDATE public.trial_assessment_items
  SET exercise_library_id = keep_id
  WHERE exercise_library_id = absorb_id;

  -- (d) delete absorb row (its remaining junction rows cascade)
  DELETE FROM public.exercise_library WHERE id = absorb_id;
  RAISE NOTICE 'dedup72: merged EX0401 -> EX0246 (cable preacher curl single arm)';
END $$;

DO $$
DECLARE
  keep_id uuid;
  absorb_id uuid;
BEGIN
  SELECT id INTO keep_id FROM public.exercise_library WHERE code = 'EX0247';
  SELECT id INTO absorb_id FROM public.exercise_library WHERE code = 'EX0423';
  IF keep_id IS NULL OR absorb_id IS NULL THEN
    RAISE NOTICE 'dedup72: SKIP EX0247/EX0423 (row missing — already merged or renamed)';
    RETURN;
  END IF;

  -- (a) metadata merge: fill NULL/empty keep content columns from absorb; never overwrite
  UPDATE public.exercise_library k
  SET
    primary_muscle   = COALESCE(NULLIF(k.primary_muscle, ''), a.primary_muscle),
    secondary_muscle = COALESCE(NULLIF(k.secondary_muscle, ''), a.secondary_muscle),
    equipment        = COALESCE(NULLIF(k.equipment, ''), a.equipment),
    difficulty       = COALESCE(k.difficulty, a.difficulty),
    exercise_type    = COALESCE(NULLIF(k.exercise_type, ''), a.exercise_type),
    type             = COALESCE(NULLIF(k.type, ''), a.type),
    met_value        = COALESCE(k.met_value, a.met_value),
    description      = COALESCE(NULLIF(k.description, ''), a.description),
    safety_notes     = COALESCE(NULLIF(k.safety_notes, ''), a.safety_notes),
    youtube_url      = COALESCE(NULLIF(k.youtube_url, ''), a.youtube_url),
    image_url        = COALESCE(NULLIF(k.image_url, ''), a.image_url),
    updated_at       = now()
  FROM public.exercise_library a
  WHERE k.id = keep_id AND a.id = absorb_id;

  -- (b) junction merge: unique absorb tags survive (composite PKs dedupe)
  INSERT INTO public.exercise_library_muscles (exercise_library_id, muscle_name, is_primary)
  SELECT keep_id, m.muscle_name, m.is_primary
  FROM public.exercise_library_muscles m
  WHERE m.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  INSERT INTO public.exercise_library_equipment (exercise_library_id, equipment_type_id)
  SELECT keep_id, e.equipment_type_id
  FROM public.exercise_library_equipment e
  WHERE e.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  -- (c) repoint non-junction FK references
  UPDATE public.trial_assessment_items
  SET exercise_library_id = keep_id
  WHERE exercise_library_id = absorb_id;

  -- (d) delete absorb row (its remaining junction rows cascade)
  DELETE FROM public.exercise_library WHERE id = absorb_id;
  RAISE NOTICE 'dedup72: merged EX0423 -> EX0247 (cable tricep extension single arm)';
END $$;

DO $$
DECLARE
  keep_id uuid;
  absorb_id uuid;
BEGIN
  SELECT id INTO keep_id FROM public.exercise_library WHERE code = 'EX0249';
  SELECT id INTO absorb_id FROM public.exercise_library WHERE code = 'EX0426';
  IF keep_id IS NULL OR absorb_id IS NULL THEN
    RAISE NOTICE 'dedup72: SKIP EX0249/EX0426 (row missing — already merged or renamed)';
    RETURN;
  END IF;

  -- (a) metadata merge: fill NULL/empty keep content columns from absorb; never overwrite
  UPDATE public.exercise_library k
  SET
    primary_muscle   = COALESCE(NULLIF(k.primary_muscle, ''), a.primary_muscle),
    secondary_muscle = COALESCE(NULLIF(k.secondary_muscle, ''), a.secondary_muscle),
    equipment        = COALESCE(NULLIF(k.equipment, ''), a.equipment),
    difficulty       = COALESCE(k.difficulty, a.difficulty),
    exercise_type    = COALESCE(NULLIF(k.exercise_type, ''), a.exercise_type),
    type             = COALESCE(NULLIF(k.type, ''), a.type),
    met_value        = COALESCE(k.met_value, a.met_value),
    description      = COALESCE(NULLIF(k.description, ''), a.description),
    safety_notes     = COALESCE(NULLIF(k.safety_notes, ''), a.safety_notes),
    youtube_url      = COALESCE(NULLIF(k.youtube_url, ''), a.youtube_url),
    image_url        = COALESCE(NULLIF(k.image_url, ''), a.image_url),
    updated_at       = now()
  FROM public.exercise_library a
  WHERE k.id = keep_id AND a.id = absorb_id;

  -- (b) junction merge: unique absorb tags survive (composite PKs dedupe)
  INSERT INTO public.exercise_library_muscles (exercise_library_id, muscle_name, is_primary)
  SELECT keep_id, m.muscle_name, m.is_primary
  FROM public.exercise_library_muscles m
  WHERE m.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  INSERT INTO public.exercise_library_equipment (exercise_library_id, equipment_type_id)
  SELECT keep_id, e.equipment_type_id
  FROM public.exercise_library_equipment e
  WHERE e.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  -- (c) repoint non-junction FK references
  UPDATE public.trial_assessment_items
  SET exercise_library_id = keep_id
  WHERE exercise_library_id = absorb_id;

  -- (d) delete absorb row (its remaining junction rows cascade)
  DELETE FROM public.exercise_library WHERE id = absorb_id;
  RAISE NOTICE 'dedup72: merged EX0426 -> EX0249 (cross cable tricep extension standing)';
END $$;

DO $$
DECLARE
  keep_id uuid;
  absorb_id uuid;
BEGIN
  SELECT id INTO keep_id FROM public.exercise_library WHERE code = 'EX0251';
  SELECT id INTO absorb_id FROM public.exercise_library WHERE code = 'EX0405';
  IF keep_id IS NULL OR absorb_id IS NULL THEN
    RAISE NOTICE 'dedup72: SKIP EX0251/EX0405 (row missing — already merged or renamed)';
    RETURN;
  END IF;

  -- (a) metadata merge: fill NULL/empty keep content columns from absorb; never overwrite
  UPDATE public.exercise_library k
  SET
    primary_muscle   = COALESCE(NULLIF(k.primary_muscle, ''), a.primary_muscle),
    secondary_muscle = COALESCE(NULLIF(k.secondary_muscle, ''), a.secondary_muscle),
    equipment        = COALESCE(NULLIF(k.equipment, ''), a.equipment),
    difficulty       = COALESCE(k.difficulty, a.difficulty),
    exercise_type    = COALESCE(NULLIF(k.exercise_type, ''), a.exercise_type),
    type             = COALESCE(NULLIF(k.type, ''), a.type),
    met_value        = COALESCE(k.met_value, a.met_value),
    description      = COALESCE(NULLIF(k.description, ''), a.description),
    safety_notes     = COALESCE(NULLIF(k.safety_notes, ''), a.safety_notes),
    youtube_url      = COALESCE(NULLIF(k.youtube_url, ''), a.youtube_url),
    image_url        = COALESCE(NULLIF(k.image_url, ''), a.image_url),
    updated_at       = now()
  FROM public.exercise_library a
  WHERE k.id = keep_id AND a.id = absorb_id;

  -- (b) junction merge: unique absorb tags survive (composite PKs dedupe)
  INSERT INTO public.exercise_library_muscles (exercise_library_id, muscle_name, is_primary)
  SELECT keep_id, m.muscle_name, m.is_primary
  FROM public.exercise_library_muscles m
  WHERE m.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  INSERT INTO public.exercise_library_equipment (exercise_library_id, equipment_type_id)
  SELECT keep_id, e.equipment_type_id
  FROM public.exercise_library_equipment e
  WHERE e.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  -- (c) repoint non-junction FK references
  UPDATE public.trial_assessment_items
  SET exercise_library_id = keep_id
  WHERE exercise_library_id = absorb_id;

  -- (d) delete absorb row (its remaining junction rows cascade)
  DELETE FROM public.exercise_library WHERE id = absorb_id;
  RAISE NOTICE 'dedup72: merged EX0405 -> EX0251 (db concentration curl)';
END $$;

DO $$
DECLARE
  keep_id uuid;
  absorb_id uuid;
BEGIN
  SELECT id INTO keep_id FROM public.exercise_library WHERE code = 'EX0253';
  SELECT id INTO absorb_id FROM public.exercise_library WHERE code = 'EX0406';
  IF keep_id IS NULL OR absorb_id IS NULL THEN
    RAISE NOTICE 'dedup72: SKIP EX0253/EX0406 (row missing — already merged or renamed)';
    RETURN;
  END IF;

  -- (a) metadata merge: fill NULL/empty keep content columns from absorb; never overwrite
  UPDATE public.exercise_library k
  SET
    primary_muscle   = COALESCE(NULLIF(k.primary_muscle, ''), a.primary_muscle),
    secondary_muscle = COALESCE(NULLIF(k.secondary_muscle, ''), a.secondary_muscle),
    equipment        = COALESCE(NULLIF(k.equipment, ''), a.equipment),
    difficulty       = COALESCE(k.difficulty, a.difficulty),
    exercise_type    = COALESCE(NULLIF(k.exercise_type, ''), a.exercise_type),
    type             = COALESCE(NULLIF(k.type, ''), a.type),
    met_value        = COALESCE(k.met_value, a.met_value),
    description      = COALESCE(NULLIF(k.description, ''), a.description),
    safety_notes     = COALESCE(NULLIF(k.safety_notes, ''), a.safety_notes),
    youtube_url      = COALESCE(NULLIF(k.youtube_url, ''), a.youtube_url),
    image_url        = COALESCE(NULLIF(k.image_url, ''), a.image_url),
    updated_at       = now()
  FROM public.exercise_library a
  WHERE k.id = keep_id AND a.id = absorb_id;

  -- (b) junction merge: unique absorb tags survive (composite PKs dedupe)
  INSERT INTO public.exercise_library_muscles (exercise_library_id, muscle_name, is_primary)
  SELECT keep_id, m.muscle_name, m.is_primary
  FROM public.exercise_library_muscles m
  WHERE m.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  INSERT INTO public.exercise_library_equipment (exercise_library_id, equipment_type_id)
  SELECT keep_id, e.equipment_type_id
  FROM public.exercise_library_equipment e
  WHERE e.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  -- (c) repoint non-junction FK references
  UPDATE public.trial_assessment_items
  SET exercise_library_id = keep_id
  WHERE exercise_library_id = absorb_id;

  -- (d) delete absorb row (its remaining junction rows cascade)
  DELETE FROM public.exercise_library WHERE id = absorb_id;
  RAISE NOTICE 'dedup72: merged EX0406 -> EX0253 (db hammer curl)';
END $$;

DO $$
DECLARE
  keep_id uuid;
  absorb_id uuid;
BEGIN
  SELECT id INTO keep_id FROM public.exercise_library WHERE code = 'EX0254';
  SELECT id INTO absorb_id FROM public.exercise_library WHERE code = 'EX0428';
  IF keep_id IS NULL OR absorb_id IS NULL THEN
    RAISE NOTICE 'dedup72: SKIP EX0254/EX0428 (row missing — already merged or renamed)';
    RETURN;
  END IF;

  -- (a) metadata merge: fill NULL/empty keep content columns from absorb; never overwrite
  UPDATE public.exercise_library k
  SET
    primary_muscle   = COALESCE(NULLIF(k.primary_muscle, ''), a.primary_muscle),
    secondary_muscle = COALESCE(NULLIF(k.secondary_muscle, ''), a.secondary_muscle),
    equipment        = COALESCE(NULLIF(k.equipment, ''), a.equipment),
    difficulty       = COALESCE(k.difficulty, a.difficulty),
    exercise_type    = COALESCE(NULLIF(k.exercise_type, ''), a.exercise_type),
    type             = COALESCE(NULLIF(k.type, ''), a.type),
    met_value        = COALESCE(k.met_value, a.met_value),
    description      = COALESCE(NULLIF(k.description, ''), a.description),
    safety_notes     = COALESCE(NULLIF(k.safety_notes, ''), a.safety_notes),
    youtube_url      = COALESCE(NULLIF(k.youtube_url, ''), a.youtube_url),
    image_url        = COALESCE(NULLIF(k.image_url, ''), a.image_url),
    updated_at       = now()
  FROM public.exercise_library a
  WHERE k.id = keep_id AND a.id = absorb_id;

  -- (b) junction merge: unique absorb tags survive (composite PKs dedupe)
  INSERT INTO public.exercise_library_muscles (exercise_library_id, muscle_name, is_primary)
  SELECT keep_id, m.muscle_name, m.is_primary
  FROM public.exercise_library_muscles m
  WHERE m.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  INSERT INTO public.exercise_library_equipment (exercise_library_id, equipment_type_id)
  SELECT keep_id, e.equipment_type_id
  FROM public.exercise_library_equipment e
  WHERE e.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  -- (c) repoint non-junction FK references
  UPDATE public.trial_assessment_items
  SET exercise_library_id = keep_id
  WHERE exercise_library_id = absorb_id;

  -- (d) delete absorb row (its remaining junction rows cascade)
  DELETE FROM public.exercise_library WHERE id = absorb_id;
  RAISE NOTICE 'dedup72: merged EX0428 -> EX0254 (db lying tricep extention decline)';
END $$;

DO $$
DECLARE
  keep_id uuid;
  absorb_id uuid;
BEGIN
  SELECT id INTO keep_id FROM public.exercise_library WHERE code = 'EX0255';
  SELECT id INTO absorb_id FROM public.exercise_library WHERE code = 'EX0429';
  IF keep_id IS NULL OR absorb_id IS NULL THEN
    RAISE NOTICE 'dedup72: SKIP EX0255/EX0429 (row missing — already merged or renamed)';
    RETURN;
  END IF;

  -- (a) metadata merge: fill NULL/empty keep content columns from absorb; never overwrite
  UPDATE public.exercise_library k
  SET
    primary_muscle   = COALESCE(NULLIF(k.primary_muscle, ''), a.primary_muscle),
    secondary_muscle = COALESCE(NULLIF(k.secondary_muscle, ''), a.secondary_muscle),
    equipment        = COALESCE(NULLIF(k.equipment, ''), a.equipment),
    difficulty       = COALESCE(k.difficulty, a.difficulty),
    exercise_type    = COALESCE(NULLIF(k.exercise_type, ''), a.exercise_type),
    type             = COALESCE(NULLIF(k.type, ''), a.type),
    met_value        = COALESCE(k.met_value, a.met_value),
    description      = COALESCE(NULLIF(k.description, ''), a.description),
    safety_notes     = COALESCE(NULLIF(k.safety_notes, ''), a.safety_notes),
    youtube_url      = COALESCE(NULLIF(k.youtube_url, ''), a.youtube_url),
    image_url        = COALESCE(NULLIF(k.image_url, ''), a.image_url),
    updated_at       = now()
  FROM public.exercise_library a
  WHERE k.id = keep_id AND a.id = absorb_id;

  -- (b) junction merge: unique absorb tags survive (composite PKs dedupe)
  INSERT INTO public.exercise_library_muscles (exercise_library_id, muscle_name, is_primary)
  SELECT keep_id, m.muscle_name, m.is_primary
  FROM public.exercise_library_muscles m
  WHERE m.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  INSERT INTO public.exercise_library_equipment (exercise_library_id, equipment_type_id)
  SELECT keep_id, e.equipment_type_id
  FROM public.exercise_library_equipment e
  WHERE e.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  -- (c) repoint non-junction FK references
  UPDATE public.trial_assessment_items
  SET exercise_library_id = keep_id
  WHERE exercise_library_id = absorb_id;

  -- (d) delete absorb row (its remaining junction rows cascade)
  DELETE FROM public.exercise_library WHERE id = absorb_id;
  RAISE NOTICE 'dedup72: merged EX0429 -> EX0255 (db lying tricep extention flat)';
END $$;

DO $$
DECLARE
  keep_id uuid;
  absorb_id uuid;
BEGIN
  SELECT id INTO keep_id FROM public.exercise_library WHERE code = 'EX0256';
  SELECT id INTO absorb_id FROM public.exercise_library WHERE code = 'EX0430';
  IF keep_id IS NULL OR absorb_id IS NULL THEN
    RAISE NOTICE 'dedup72: SKIP EX0256/EX0430 (row missing — already merged or renamed)';
    RETURN;
  END IF;

  -- (a) metadata merge: fill NULL/empty keep content columns from absorb; never overwrite
  UPDATE public.exercise_library k
  SET
    primary_muscle   = COALESCE(NULLIF(k.primary_muscle, ''), a.primary_muscle),
    secondary_muscle = COALESCE(NULLIF(k.secondary_muscle, ''), a.secondary_muscle),
    equipment        = COALESCE(NULLIF(k.equipment, ''), a.equipment),
    difficulty       = COALESCE(k.difficulty, a.difficulty),
    exercise_type    = COALESCE(NULLIF(k.exercise_type, ''), a.exercise_type),
    type             = COALESCE(NULLIF(k.type, ''), a.type),
    met_value        = COALESCE(k.met_value, a.met_value),
    description      = COALESCE(NULLIF(k.description, ''), a.description),
    safety_notes     = COALESCE(NULLIF(k.safety_notes, ''), a.safety_notes),
    youtube_url      = COALESCE(NULLIF(k.youtube_url, ''), a.youtube_url),
    image_url        = COALESCE(NULLIF(k.image_url, ''), a.image_url),
    updated_at       = now()
  FROM public.exercise_library a
  WHERE k.id = keep_id AND a.id = absorb_id;

  -- (b) junction merge: unique absorb tags survive (composite PKs dedupe)
  INSERT INTO public.exercise_library_muscles (exercise_library_id, muscle_name, is_primary)
  SELECT keep_id, m.muscle_name, m.is_primary
  FROM public.exercise_library_muscles m
  WHERE m.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  INSERT INTO public.exercise_library_equipment (exercise_library_id, equipment_type_id)
  SELECT keep_id, e.equipment_type_id
  FROM public.exercise_library_equipment e
  WHERE e.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  -- (c) repoint non-junction FK references
  UPDATE public.trial_assessment_items
  SET exercise_library_id = keep_id
  WHERE exercise_library_id = absorb_id;

  -- (d) delete absorb row (its remaining junction rows cascade)
  DELETE FROM public.exercise_library WHERE id = absorb_id;
  RAISE NOTICE 'dedup72: merged EX0430 -> EX0256 (db lying tricep extention incline)';
END $$;

DO $$
DECLARE
  keep_id uuid;
  absorb_id uuid;
BEGIN
  SELECT id INTO keep_id FROM public.exercise_library WHERE code = 'EX0257';
  SELECT id INTO absorb_id FROM public.exercise_library WHERE code = 'EX0408';
  IF keep_id IS NULL OR absorb_id IS NULL THEN
    RAISE NOTICE 'dedup72: SKIP EX0257/EX0408 (row missing — already merged or renamed)';
    RETURN;
  END IF;

  -- (a) metadata merge: fill NULL/empty keep content columns from absorb; never overwrite
  UPDATE public.exercise_library k
  SET
    primary_muscle   = COALESCE(NULLIF(k.primary_muscle, ''), a.primary_muscle),
    secondary_muscle = COALESCE(NULLIF(k.secondary_muscle, ''), a.secondary_muscle),
    equipment        = COALESCE(NULLIF(k.equipment, ''), a.equipment),
    difficulty       = COALESCE(k.difficulty, a.difficulty),
    exercise_type    = COALESCE(NULLIF(k.exercise_type, ''), a.exercise_type),
    type             = COALESCE(NULLIF(k.type, ''), a.type),
    met_value        = COALESCE(k.met_value, a.met_value),
    description      = COALESCE(NULLIF(k.description, ''), a.description),
    safety_notes     = COALESCE(NULLIF(k.safety_notes, ''), a.safety_notes),
    youtube_url      = COALESCE(NULLIF(k.youtube_url, ''), a.youtube_url),
    image_url        = COALESCE(NULLIF(k.image_url, ''), a.image_url),
    updated_at       = now()
  FROM public.exercise_library a
  WHERE k.id = keep_id AND a.id = absorb_id;

  -- (b) junction merge: unique absorb tags survive (composite PKs dedupe)
  INSERT INTO public.exercise_library_muscles (exercise_library_id, muscle_name, is_primary)
  SELECT keep_id, m.muscle_name, m.is_primary
  FROM public.exercise_library_muscles m
  WHERE m.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  INSERT INTO public.exercise_library_equipment (exercise_library_id, equipment_type_id)
  SELECT keep_id, e.equipment_type_id
  FROM public.exercise_library_equipment e
  WHERE e.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  -- (c) repoint non-junction FK references
  UPDATE public.trial_assessment_items
  SET exercise_library_id = keep_id
  WHERE exercise_library_id = absorb_id;

  -- (d) delete absorb row (its remaining junction rows cascade)
  DELETE FROM public.exercise_library WHERE id = absorb_id;
  RAISE NOTICE 'dedup72: merged EX0408 -> EX0257 (db preacher curl)';
END $$;

DO $$
DECLARE
  keep_id uuid;
  absorb_id uuid;
BEGIN
  SELECT id INTO keep_id FROM public.exercise_library WHERE code = 'EX0258';
  SELECT id INTO absorb_id FROM public.exercise_library WHERE code = 'EX0410';
  IF keep_id IS NULL OR absorb_id IS NULL THEN
    RAISE NOTICE 'dedup72: SKIP EX0258/EX0410 (row missing — already merged or renamed)';
    RETURN;
  END IF;

  -- (a) metadata merge: fill NULL/empty keep content columns from absorb; never overwrite
  UPDATE public.exercise_library k
  SET
    primary_muscle   = COALESCE(NULLIF(k.primary_muscle, ''), a.primary_muscle),
    secondary_muscle = COALESCE(NULLIF(k.secondary_muscle, ''), a.secondary_muscle),
    equipment        = COALESCE(NULLIF(k.equipment, ''), a.equipment),
    difficulty       = COALESCE(k.difficulty, a.difficulty),
    exercise_type    = COALESCE(NULLIF(k.exercise_type, ''), a.exercise_type),
    type             = COALESCE(NULLIF(k.type, ''), a.type),
    met_value        = COALESCE(k.met_value, a.met_value),
    description      = COALESCE(NULLIF(k.description, ''), a.description),
    safety_notes     = COALESCE(NULLIF(k.safety_notes, ''), a.safety_notes),
    youtube_url      = COALESCE(NULLIF(k.youtube_url, ''), a.youtube_url),
    image_url        = COALESCE(NULLIF(k.image_url, ''), a.image_url),
    updated_at       = now()
  FROM public.exercise_library a
  WHERE k.id = keep_id AND a.id = absorb_id;

  -- (b) junction merge: unique absorb tags survive (composite PKs dedupe)
  INSERT INTO public.exercise_library_muscles (exercise_library_id, muscle_name, is_primary)
  SELECT keep_id, m.muscle_name, m.is_primary
  FROM public.exercise_library_muscles m
  WHERE m.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  INSERT INTO public.exercise_library_equipment (exercise_library_id, equipment_type_id)
  SELECT keep_id, e.equipment_type_id
  FROM public.exercise_library_equipment e
  WHERE e.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  -- (c) repoint non-junction FK references
  UPDATE public.trial_assessment_items
  SET exercise_library_id = keep_id
  WHERE exercise_library_id = absorb_id;

  -- (d) delete absorb row (its remaining junction rows cascade)
  DELETE FROM public.exercise_library WHERE id = absorb_id;
  RAISE NOTICE 'dedup72: merged EX0410 -> EX0258 (db preacher curl single arm)';
END $$;

DO $$
DECLARE
  keep_id uuid;
  absorb_id uuid;
BEGIN
  SELECT id INTO keep_id FROM public.exercise_library WHERE code = 'EX0259';
  SELECT id INTO absorb_id FROM public.exercise_library WHERE code = 'EX0412';
  IF keep_id IS NULL OR absorb_id IS NULL THEN
    RAISE NOTICE 'dedup72: SKIP EX0259/EX0412 (row missing — already merged or renamed)';
    RETURN;
  END IF;

  -- (a) metadata merge: fill NULL/empty keep content columns from absorb; never overwrite
  UPDATE public.exercise_library k
  SET
    primary_muscle   = COALESCE(NULLIF(k.primary_muscle, ''), a.primary_muscle),
    secondary_muscle = COALESCE(NULLIF(k.secondary_muscle, ''), a.secondary_muscle),
    equipment        = COALESCE(NULLIF(k.equipment, ''), a.equipment),
    difficulty       = COALESCE(k.difficulty, a.difficulty),
    exercise_type    = COALESCE(NULLIF(k.exercise_type, ''), a.exercise_type),
    type             = COALESCE(NULLIF(k.type, ''), a.type),
    met_value        = COALESCE(k.met_value, a.met_value),
    description      = COALESCE(NULLIF(k.description, ''), a.description),
    safety_notes     = COALESCE(NULLIF(k.safety_notes, ''), a.safety_notes),
    youtube_url      = COALESCE(NULLIF(k.youtube_url, ''), a.youtube_url),
    image_url        = COALESCE(NULLIF(k.image_url, ''), a.image_url),
    updated_at       = now()
  FROM public.exercise_library a
  WHERE k.id = keep_id AND a.id = absorb_id;

  -- (b) junction merge: unique absorb tags survive (composite PKs dedupe)
  INSERT INTO public.exercise_library_muscles (exercise_library_id, muscle_name, is_primary)
  SELECT keep_id, m.muscle_name, m.is_primary
  FROM public.exercise_library_muscles m
  WHERE m.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  INSERT INTO public.exercise_library_equipment (exercise_library_id, equipment_type_id)
  SELECT keep_id, e.equipment_type_id
  FROM public.exercise_library_equipment e
  WHERE e.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  -- (c) repoint non-junction FK references
  UPDATE public.trial_assessment_items
  SET exercise_library_id = keep_id
  WHERE exercise_library_id = absorb_id;

  -- (d) delete absorb row (its remaining junction rows cascade)
  DELETE FROM public.exercise_library WHERE id = absorb_id;
  RAISE NOTICE 'dedup72: merged EX0412 -> EX0259 (db scott curl)';
END $$;

DO $$
DECLARE
  keep_id uuid;
  absorb_id uuid;
BEGIN
  SELECT id INTO keep_id FROM public.exercise_library WHERE code = 'EX0260';
  SELECT id INTO absorb_id FROM public.exercise_library WHERE code = 'EX0413';
  IF keep_id IS NULL OR absorb_id IS NULL THEN
    RAISE NOTICE 'dedup72: SKIP EX0260/EX0413 (row missing — already merged or renamed)';
    RETURN;
  END IF;

  -- (a) metadata merge: fill NULL/empty keep content columns from absorb; never overwrite
  UPDATE public.exercise_library k
  SET
    primary_muscle   = COALESCE(NULLIF(k.primary_muscle, ''), a.primary_muscle),
    secondary_muscle = COALESCE(NULLIF(k.secondary_muscle, ''), a.secondary_muscle),
    equipment        = COALESCE(NULLIF(k.equipment, ''), a.equipment),
    difficulty       = COALESCE(k.difficulty, a.difficulty),
    exercise_type    = COALESCE(NULLIF(k.exercise_type, ''), a.exercise_type),
    type             = COALESCE(NULLIF(k.type, ''), a.type),
    met_value        = COALESCE(k.met_value, a.met_value),
    description      = COALESCE(NULLIF(k.description, ''), a.description),
    safety_notes     = COALESCE(NULLIF(k.safety_notes, ''), a.safety_notes),
    youtube_url      = COALESCE(NULLIF(k.youtube_url, ''), a.youtube_url),
    image_url        = COALESCE(NULLIF(k.image_url, ''), a.image_url),
    updated_at       = now()
  FROM public.exercise_library a
  WHERE k.id = keep_id AND a.id = absorb_id;

  -- (b) junction merge: unique absorb tags survive (composite PKs dedupe)
  INSERT INTO public.exercise_library_muscles (exercise_library_id, muscle_name, is_primary)
  SELECT keep_id, m.muscle_name, m.is_primary
  FROM public.exercise_library_muscles m
  WHERE m.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  INSERT INTO public.exercise_library_equipment (exercise_library_id, equipment_type_id)
  SELECT keep_id, e.equipment_type_id
  FROM public.exercise_library_equipment e
  WHERE e.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  -- (c) repoint non-junction FK references
  UPDATE public.trial_assessment_items
  SET exercise_library_id = keep_id
  WHERE exercise_library_id = absorb_id;

  -- (d) delete absorb row (its remaining junction rows cascade)
  DELETE FROM public.exercise_library WHERE id = absorb_id;
  RAISE NOTICE 'dedup72: merged EX0413 -> EX0260 (db scott curl fat grip)';
END $$;

DO $$
DECLARE
  keep_id uuid;
  absorb_id uuid;
BEGIN
  SELECT id INTO keep_id FROM public.exercise_library WHERE code = 'EX0261';
  SELECT id INTO absorb_id FROM public.exercise_library WHERE code = 'EX0414';
  IF keep_id IS NULL OR absorb_id IS NULL THEN
    RAISE NOTICE 'dedup72: SKIP EX0261/EX0414 (row missing — already merged or renamed)';
    RETURN;
  END IF;

  -- (a) metadata merge: fill NULL/empty keep content columns from absorb; never overwrite
  UPDATE public.exercise_library k
  SET
    primary_muscle   = COALESCE(NULLIF(k.primary_muscle, ''), a.primary_muscle),
    secondary_muscle = COALESCE(NULLIF(k.secondary_muscle, ''), a.secondary_muscle),
    equipment        = COALESCE(NULLIF(k.equipment, ''), a.equipment),
    difficulty       = COALESCE(k.difficulty, a.difficulty),
    exercise_type    = COALESCE(NULLIF(k.exercise_type, ''), a.exercise_type),
    type             = COALESCE(NULLIF(k.type, ''), a.type),
    met_value        = COALESCE(k.met_value, a.met_value),
    description      = COALESCE(NULLIF(k.description, ''), a.description),
    safety_notes     = COALESCE(NULLIF(k.safety_notes, ''), a.safety_notes),
    youtube_url      = COALESCE(NULLIF(k.youtube_url, ''), a.youtube_url),
    image_url        = COALESCE(NULLIF(k.image_url, ''), a.image_url),
    updated_at       = now()
  FROM public.exercise_library a
  WHERE k.id = keep_id AND a.id = absorb_id;

  -- (b) junction merge: unique absorb tags survive (composite PKs dedupe)
  INSERT INTO public.exercise_library_muscles (exercise_library_id, muscle_name, is_primary)
  SELECT keep_id, m.muscle_name, m.is_primary
  FROM public.exercise_library_muscles m
  WHERE m.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  INSERT INTO public.exercise_library_equipment (exercise_library_id, equipment_type_id)
  SELECT keep_id, e.equipment_type_id
  FROM public.exercise_library_equipment e
  WHERE e.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  -- (c) repoint non-junction FK references
  UPDATE public.trial_assessment_items
  SET exercise_library_id = keep_id
  WHERE exercise_library_id = absorb_id;

  -- (d) delete absorb row (its remaining junction rows cascade)
  DELETE FROM public.exercise_library WHERE id = absorb_id;
  RAISE NOTICE 'dedup72: merged EX0414 -> EX0261 (db scott curl single arm)';
END $$;

DO $$
DECLARE
  keep_id uuid;
  absorb_id uuid;
BEGIN
  SELECT id INTO keep_id FROM public.exercise_library WHERE code = 'EX0262';
  SELECT id INTO absorb_id FROM public.exercise_library WHERE code = 'EX0415';
  IF keep_id IS NULL OR absorb_id IS NULL THEN
    RAISE NOTICE 'dedup72: SKIP EX0262/EX0415 (row missing — already merged or renamed)';
    RETURN;
  END IF;

  -- (a) metadata merge: fill NULL/empty keep content columns from absorb; never overwrite
  UPDATE public.exercise_library k
  SET
    primary_muscle   = COALESCE(NULLIF(k.primary_muscle, ''), a.primary_muscle),
    secondary_muscle = COALESCE(NULLIF(k.secondary_muscle, ''), a.secondary_muscle),
    equipment        = COALESCE(NULLIF(k.equipment, ''), a.equipment),
    difficulty       = COALESCE(k.difficulty, a.difficulty),
    exercise_type    = COALESCE(NULLIF(k.exercise_type, ''), a.exercise_type),
    type             = COALESCE(NULLIF(k.type, ''), a.type),
    met_value        = COALESCE(k.met_value, a.met_value),
    description      = COALESCE(NULLIF(k.description, ''), a.description),
    safety_notes     = COALESCE(NULLIF(k.safety_notes, ''), a.safety_notes),
    youtube_url      = COALESCE(NULLIF(k.youtube_url, ''), a.youtube_url),
    image_url        = COALESCE(NULLIF(k.image_url, ''), a.image_url),
    updated_at       = now()
  FROM public.exercise_library a
  WHERE k.id = keep_id AND a.id = absorb_id;

  -- (b) junction merge: unique absorb tags survive (composite PKs dedupe)
  INSERT INTO public.exercise_library_muscles (exercise_library_id, muscle_name, is_primary)
  SELECT keep_id, m.muscle_name, m.is_primary
  FROM public.exercise_library_muscles m
  WHERE m.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  INSERT INTO public.exercise_library_equipment (exercise_library_id, equipment_type_id)
  SELECT keep_id, e.equipment_type_id
  FROM public.exercise_library_equipment e
  WHERE e.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  -- (c) repoint non-junction FK references
  UPDATE public.trial_assessment_items
  SET exercise_library_id = keep_id
  WHERE exercise_library_id = absorb_id;

  -- (d) delete absorb row (its remaining junction rows cascade)
  DELETE FROM public.exercise_library WHERE id = absorb_id;
  RAISE NOTICE 'dedup72: merged EX0415 -> EX0262 (db zottman curl)';
END $$;

DO $$
DECLARE
  keep_id uuid;
  absorb_id uuid;
BEGIN
  SELECT id INTO keep_id FROM public.exercise_library WHERE code = 'EX0263';
  SELECT id INTO absorb_id FROM public.exercise_library WHERE code = 'EX0339';
  IF keep_id IS NULL OR absorb_id IS NULL THEN
    RAISE NOTICE 'dedup72: SKIP EX0263/EX0339 (row missing — already merged or renamed)';
    RETURN;
  END IF;

  -- (a) metadata merge: fill NULL/empty keep content columns from absorb; never overwrite
  UPDATE public.exercise_library k
  SET
    primary_muscle   = COALESCE(NULLIF(k.primary_muscle, ''), a.primary_muscle),
    secondary_muscle = COALESCE(NULLIF(k.secondary_muscle, ''), a.secondary_muscle),
    equipment        = COALESCE(NULLIF(k.equipment, ''), a.equipment),
    difficulty       = COALESCE(k.difficulty, a.difficulty),
    exercise_type    = COALESCE(NULLIF(k.exercise_type, ''), a.exercise_type),
    type             = COALESCE(NULLIF(k.type, ''), a.type),
    met_value        = COALESCE(k.met_value, a.met_value),
    description      = COALESCE(NULLIF(k.description, ''), a.description),
    safety_notes     = COALESCE(NULLIF(k.safety_notes, ''), a.safety_notes),
    youtube_url      = COALESCE(NULLIF(k.youtube_url, ''), a.youtube_url),
    image_url        = COALESCE(NULLIF(k.image_url, ''), a.image_url),
    updated_at       = now()
  FROM public.exercise_library a
  WHERE k.id = keep_id AND a.id = absorb_id;

  -- (b) junction merge: unique absorb tags survive (composite PKs dedupe)
  INSERT INTO public.exercise_library_muscles (exercise_library_id, muscle_name, is_primary)
  SELECT keep_id, m.muscle_name, m.is_primary
  FROM public.exercise_library_muscles m
  WHERE m.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  INSERT INTO public.exercise_library_equipment (exercise_library_id, equipment_type_id)
  SELECT keep_id, e.equipment_type_id
  FROM public.exercise_library_equipment e
  WHERE e.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  -- (c) repoint non-junction FK references
  UPDATE public.trial_assessment_items
  SET exercise_library_id = keep_id
  WHERE exercise_library_id = absorb_id;

  -- (d) delete absorb row (its remaining junction rows cascade)
  DELETE FROM public.exercise_library WHERE id = absorb_id;
  RAISE NOTICE 'dedup72: merged EX0339 -> EX0263 (deadbug)';
END $$;

DO $$
DECLARE
  keep_id uuid;
  absorb_id uuid;
BEGIN
  SELECT id INTO keep_id FROM public.exercise_library WHERE code = 'EX0264';
  SELECT id INTO absorb_id FROM public.exercise_library WHERE code = 'EX0361';
  IF keep_id IS NULL OR absorb_id IS NULL THEN
    RAISE NOTICE 'dedup72: SKIP EX0264/EX0361 (row missing — already merged or renamed)';
    RETURN;
  END IF;

  -- (a) metadata merge: fill NULL/empty keep content columns from absorb; never overwrite
  UPDATE public.exercise_library k
  SET
    primary_muscle   = COALESCE(NULLIF(k.primary_muscle, ''), a.primary_muscle),
    secondary_muscle = COALESCE(NULLIF(k.secondary_muscle, ''), a.secondary_muscle),
    equipment        = COALESCE(NULLIF(k.equipment, ''), a.equipment),
    difficulty       = COALESCE(k.difficulty, a.difficulty),
    exercise_type    = COALESCE(NULLIF(k.exercise_type, ''), a.exercise_type),
    type             = COALESCE(NULLIF(k.type, ''), a.type),
    met_value        = COALESCE(k.met_value, a.met_value),
    description      = COALESCE(NULLIF(k.description, ''), a.description),
    safety_notes     = COALESCE(NULLIF(k.safety_notes, ''), a.safety_notes),
    youtube_url      = COALESCE(NULLIF(k.youtube_url, ''), a.youtube_url),
    image_url        = COALESCE(NULLIF(k.image_url, ''), a.image_url),
    updated_at       = now()
  FROM public.exercise_library a
  WHERE k.id = keep_id AND a.id = absorb_id;

  -- (b) junction merge: unique absorb tags survive (composite PKs dedupe)
  INSERT INTO public.exercise_library_muscles (exercise_library_id, muscle_name, is_primary)
  SELECT keep_id, m.muscle_name, m.is_primary
  FROM public.exercise_library_muscles m
  WHERE m.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  INSERT INTO public.exercise_library_equipment (exercise_library_id, equipment_type_id)
  SELECT keep_id, e.equipment_type_id
  FROM public.exercise_library_equipment e
  WHERE e.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  -- (c) repoint non-junction FK references
  UPDATE public.trial_assessment_items
  SET exercise_library_id = keep_id
  WHERE exercise_library_id = absorb_id;

  -- (d) delete absorb row (its remaining junction rows cascade)
  DELETE FROM public.exercise_library WHERE id = absorb_id;
  RAISE NOTICE 'dedup72: merged EX0361 -> EX0264 (deadmill)';
END $$;

DO $$
DECLARE
  keep_id uuid;
  absorb_id uuid;
BEGIN
  SELECT id INTO keep_id FROM public.exercise_library WHERE code = 'EX0266';
  SELECT id INTO absorb_id FROM public.exercise_library WHERE code = 'EX0340';
  IF keep_id IS NULL OR absorb_id IS NULL THEN
    RAISE NOTICE 'dedup72: SKIP EX0266/EX0340 (row missing — already merged or renamed)';
    RETURN;
  END IF;

  -- (a) metadata merge: fill NULL/empty keep content columns from absorb; never overwrite
  UPDATE public.exercise_library k
  SET
    primary_muscle   = COALESCE(NULLIF(k.primary_muscle, ''), a.primary_muscle),
    secondary_muscle = COALESCE(NULLIF(k.secondary_muscle, ''), a.secondary_muscle),
    equipment        = COALESCE(NULLIF(k.equipment, ''), a.equipment),
    difficulty       = COALESCE(k.difficulty, a.difficulty),
    exercise_type    = COALESCE(NULLIF(k.exercise_type, ''), a.exercise_type),
    type             = COALESCE(NULLIF(k.type, ''), a.type),
    met_value        = COALESCE(k.met_value, a.met_value),
    description      = COALESCE(NULLIF(k.description, ''), a.description),
    safety_notes     = COALESCE(NULLIF(k.safety_notes, ''), a.safety_notes),
    youtube_url      = COALESCE(NULLIF(k.youtube_url, ''), a.youtube_url),
    image_url        = COALESCE(NULLIF(k.image_url, ''), a.image_url),
    updated_at       = now()
  FROM public.exercise_library a
  WHERE k.id = keep_id AND a.id = absorb_id;

  -- (b) junction merge: unique absorb tags survive (composite PKs dedupe)
  INSERT INTO public.exercise_library_muscles (exercise_library_id, muscle_name, is_primary)
  SELECT keep_id, m.muscle_name, m.is_primary
  FROM public.exercise_library_muscles m
  WHERE m.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  INSERT INTO public.exercise_library_equipment (exercise_library_id, equipment_type_id)
  SELECT keep_id, e.equipment_type_id
  FROM public.exercise_library_equipment e
  WHERE e.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  -- (c) repoint non-junction FK references
  UPDATE public.trial_assessment_items
  SET exercise_library_id = keep_id
  WHERE exercise_library_id = absorb_id;

  -- (d) delete absorb row (its remaining junction rows cascade)
  DELETE FROM public.exercise_library WHERE id = absorb_id;
  RAISE NOTICE 'dedup72: merged EX0340 -> EX0266 (decline leg raise)';
END $$;

DO $$
DECLARE
  keep_id uuid;
  absorb_id uuid;
BEGIN
  SELECT id INTO keep_id FROM public.exercise_library WHERE code = 'EX0266';
  SELECT id INTO absorb_id FROM public.exercise_library WHERE code = 'EX0375';
  IF keep_id IS NULL OR absorb_id IS NULL THEN
    RAISE NOTICE 'dedup72: SKIP EX0266/EX0375 (row missing — already merged or renamed)';
    RETURN;
  END IF;

  -- (a) metadata merge: fill NULL/empty keep content columns from absorb; never overwrite
  UPDATE public.exercise_library k
  SET
    primary_muscle   = COALESCE(NULLIF(k.primary_muscle, ''), a.primary_muscle),
    secondary_muscle = COALESCE(NULLIF(k.secondary_muscle, ''), a.secondary_muscle),
    equipment        = COALESCE(NULLIF(k.equipment, ''), a.equipment),
    difficulty       = COALESCE(k.difficulty, a.difficulty),
    exercise_type    = COALESCE(NULLIF(k.exercise_type, ''), a.exercise_type),
    type             = COALESCE(NULLIF(k.type, ''), a.type),
    met_value        = COALESCE(k.met_value, a.met_value),
    description      = COALESCE(NULLIF(k.description, ''), a.description),
    safety_notes     = COALESCE(NULLIF(k.safety_notes, ''), a.safety_notes),
    youtube_url      = COALESCE(NULLIF(k.youtube_url, ''), a.youtube_url),
    image_url        = COALESCE(NULLIF(k.image_url, ''), a.image_url),
    updated_at       = now()
  FROM public.exercise_library a
  WHERE k.id = keep_id AND a.id = absorb_id;

  -- (b) junction merge: unique absorb tags survive (composite PKs dedupe)
  INSERT INTO public.exercise_library_muscles (exercise_library_id, muscle_name, is_primary)
  SELECT keep_id, m.muscle_name, m.is_primary
  FROM public.exercise_library_muscles m
  WHERE m.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  INSERT INTO public.exercise_library_equipment (exercise_library_id, equipment_type_id)
  SELECT keep_id, e.equipment_type_id
  FROM public.exercise_library_equipment e
  WHERE e.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  -- (c) repoint non-junction FK references
  UPDATE public.trial_assessment_items
  SET exercise_library_id = keep_id
  WHERE exercise_library_id = absorb_id;

  -- (d) delete absorb row (its remaining junction rows cascade)
  DELETE FROM public.exercise_library WHERE id = absorb_id;
  RAISE NOTICE 'dedup72: merged EX0375 -> EX0266 (decline leg raise)';
END $$;

DO $$
DECLARE
  keep_id uuid;
  absorb_id uuid;
BEGIN
  SELECT id INTO keep_id FROM public.exercise_library WHERE code = 'EX0268';
  SELECT id INTO absorb_id FROM public.exercise_library WHERE code = 'EX0341';
  IF keep_id IS NULL OR absorb_id IS NULL THEN
    RAISE NOTICE 'dedup72: SKIP EX0268/EX0341 (row missing — already merged or renamed)';
    RETURN;
  END IF;

  -- (a) metadata merge: fill NULL/empty keep content columns from absorb; never overwrite
  UPDATE public.exercise_library k
  SET
    primary_muscle   = COALESCE(NULLIF(k.primary_muscle, ''), a.primary_muscle),
    secondary_muscle = COALESCE(NULLIF(k.secondary_muscle, ''), a.secondary_muscle),
    equipment        = COALESCE(NULLIF(k.equipment, ''), a.equipment),
    difficulty       = COALESCE(k.difficulty, a.difficulty),
    exercise_type    = COALESCE(NULLIF(k.exercise_type, ''), a.exercise_type),
    type             = COALESCE(NULLIF(k.type, ''), a.type),
    met_value        = COALESCE(k.met_value, a.met_value),
    description      = COALESCE(NULLIF(k.description, ''), a.description),
    safety_notes     = COALESCE(NULLIF(k.safety_notes, ''), a.safety_notes),
    youtube_url      = COALESCE(NULLIF(k.youtube_url, ''), a.youtube_url),
    image_url        = COALESCE(NULLIF(k.image_url, ''), a.image_url),
    updated_at       = now()
  FROM public.exercise_library a
  WHERE k.id = keep_id AND a.id = absorb_id;

  -- (b) junction merge: unique absorb tags survive (composite PKs dedupe)
  INSERT INTO public.exercise_library_muscles (exercise_library_id, muscle_name, is_primary)
  SELECT keep_id, m.muscle_name, m.is_primary
  FROM public.exercise_library_muscles m
  WHERE m.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  INSERT INTO public.exercise_library_equipment (exercise_library_id, equipment_type_id)
  SELECT keep_id, e.equipment_type_id
  FROM public.exercise_library_equipment e
  WHERE e.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  -- (c) repoint non-junction FK references
  UPDATE public.trial_assessment_items
  SET exercise_library_id = keep_id
  WHERE exercise_library_id = absorb_id;

  -- (d) delete absorb row (its remaining junction rows cascade)
  DELETE FROM public.exercise_library WHERE id = absorb_id;
  RAISE NOTICE 'dedup72: merged EX0341 -> EX0268 (dish hold)';
END $$;

DO $$
DECLARE
  keep_id uuid;
  absorb_id uuid;
BEGIN
  SELECT id INTO keep_id FROM public.exercise_library WHERE code = 'EX0268';
  SELECT id INTO absorb_id FROM public.exercise_library WHERE code = 'EX0376';
  IF keep_id IS NULL OR absorb_id IS NULL THEN
    RAISE NOTICE 'dedup72: SKIP EX0268/EX0376 (row missing — already merged or renamed)';
    RETURN;
  END IF;

  -- (a) metadata merge: fill NULL/empty keep content columns from absorb; never overwrite
  UPDATE public.exercise_library k
  SET
    primary_muscle   = COALESCE(NULLIF(k.primary_muscle, ''), a.primary_muscle),
    secondary_muscle = COALESCE(NULLIF(k.secondary_muscle, ''), a.secondary_muscle),
    equipment        = COALESCE(NULLIF(k.equipment, ''), a.equipment),
    difficulty       = COALESCE(k.difficulty, a.difficulty),
    exercise_type    = COALESCE(NULLIF(k.exercise_type, ''), a.exercise_type),
    type             = COALESCE(NULLIF(k.type, ''), a.type),
    met_value        = COALESCE(k.met_value, a.met_value),
    description      = COALESCE(NULLIF(k.description, ''), a.description),
    safety_notes     = COALESCE(NULLIF(k.safety_notes, ''), a.safety_notes),
    youtube_url      = COALESCE(NULLIF(k.youtube_url, ''), a.youtube_url),
    image_url        = COALESCE(NULLIF(k.image_url, ''), a.image_url),
    updated_at       = now()
  FROM public.exercise_library a
  WHERE k.id = keep_id AND a.id = absorb_id;

  -- (b) junction merge: unique absorb tags survive (composite PKs dedupe)
  INSERT INTO public.exercise_library_muscles (exercise_library_id, muscle_name, is_primary)
  SELECT keep_id, m.muscle_name, m.is_primary
  FROM public.exercise_library_muscles m
  WHERE m.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  INSERT INTO public.exercise_library_equipment (exercise_library_id, equipment_type_id)
  SELECT keep_id, e.equipment_type_id
  FROM public.exercise_library_equipment e
  WHERE e.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  -- (c) repoint non-junction FK references
  UPDATE public.trial_assessment_items
  SET exercise_library_id = keep_id
  WHERE exercise_library_id = absorb_id;

  -- (d) delete absorb row (its remaining junction rows cascade)
  DELETE FROM public.exercise_library WHERE id = absorb_id;
  RAISE NOTICE 'dedup72: merged EX0376 -> EX0268 (dish hold)';
END $$;

DO $$
DECLARE
  keep_id uuid;
  absorb_id uuid;
BEGIN
  SELECT id INTO keep_id FROM public.exercise_library WHERE code = 'EX0271';
  SELECT id INTO absorb_id FROM public.exercise_library WHERE code = 'EX0432';
  IF keep_id IS NULL OR absorb_id IS NULL THEN
    RAISE NOTICE 'dedup72: SKIP EX0271/EX0432 (row missing — already merged or renamed)';
    RETURN;
  END IF;

  -- (a) metadata merge: fill NULL/empty keep content columns from absorb; never overwrite
  UPDATE public.exercise_library k
  SET
    primary_muscle   = COALESCE(NULLIF(k.primary_muscle, ''), a.primary_muscle),
    secondary_muscle = COALESCE(NULLIF(k.secondary_muscle, ''), a.secondary_muscle),
    equipment        = COALESCE(NULLIF(k.equipment, ''), a.equipment),
    difficulty       = COALESCE(k.difficulty, a.difficulty),
    exercise_type    = COALESCE(NULLIF(k.exercise_type, ''), a.exercise_type),
    type             = COALESCE(NULLIF(k.type, ''), a.type),
    met_value        = COALESCE(k.met_value, a.met_value),
    description      = COALESCE(NULLIF(k.description, ''), a.description),
    safety_notes     = COALESCE(NULLIF(k.safety_notes, ''), a.safety_notes),
    youtube_url      = COALESCE(NULLIF(k.youtube_url, ''), a.youtube_url),
    image_url        = COALESCE(NULLIF(k.image_url, ''), a.image_url),
    updated_at       = now()
  FROM public.exercise_library a
  WHERE k.id = keep_id AND a.id = absorb_id;

  -- (b) junction merge: unique absorb tags survive (composite PKs dedupe)
  INSERT INTO public.exercise_library_muscles (exercise_library_id, muscle_name, is_primary)
  SELECT keep_id, m.muscle_name, m.is_primary
  FROM public.exercise_library_muscles m
  WHERE m.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  INSERT INTO public.exercise_library_equipment (exercise_library_id, equipment_type_id)
  SELECT keep_id, e.equipment_type_id
  FROM public.exercise_library_equipment e
  WHERE e.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  -- (c) repoint non-junction FK references
  UPDATE public.trial_assessment_items
  SET exercise_library_id = keep_id
  WHERE exercise_library_id = absorb_id;

  -- (d) delete absorb row (its remaining junction rows cascade)
  DELETE FROM public.exercise_library WHERE id = absorb_id;
  RAISE NOTICE 'dedup72: merged EX0432 -> EX0271 (dual rope overhead tricep extension)';
END $$;

DO $$
DECLARE
  keep_id uuid;
  absorb_id uuid;
BEGIN
  SELECT id INTO keep_id FROM public.exercise_library WHERE code = 'EX0272';
  SELECT id INTO absorb_id FROM public.exercise_library WHERE code = 'EX0433';
  IF keep_id IS NULL OR absorb_id IS NULL THEN
    RAISE NOTICE 'dedup72: SKIP EX0272/EX0433 (row missing — already merged or renamed)';
    RETURN;
  END IF;

  -- (a) metadata merge: fill NULL/empty keep content columns from absorb; never overwrite
  UPDATE public.exercise_library k
  SET
    primary_muscle   = COALESCE(NULLIF(k.primary_muscle, ''), a.primary_muscle),
    secondary_muscle = COALESCE(NULLIF(k.secondary_muscle, ''), a.secondary_muscle),
    equipment        = COALESCE(NULLIF(k.equipment, ''), a.equipment),
    difficulty       = COALESCE(k.difficulty, a.difficulty),
    exercise_type    = COALESCE(NULLIF(k.exercise_type, ''), a.exercise_type),
    type             = COALESCE(NULLIF(k.type, ''), a.type),
    met_value        = COALESCE(k.met_value, a.met_value),
    description      = COALESCE(NULLIF(k.description, ''), a.description),
    safety_notes     = COALESCE(NULLIF(k.safety_notes, ''), a.safety_notes),
    youtube_url      = COALESCE(NULLIF(k.youtube_url, ''), a.youtube_url),
    image_url        = COALESCE(NULLIF(k.image_url, ''), a.image_url),
    updated_at       = now()
  FROM public.exercise_library a
  WHERE k.id = keep_id AND a.id = absorb_id;

  -- (b) junction merge: unique absorb tags survive (composite PKs dedupe)
  INSERT INTO public.exercise_library_muscles (exercise_library_id, muscle_name, is_primary)
  SELECT keep_id, m.muscle_name, m.is_primary
  FROM public.exercise_library_muscles m
  WHERE m.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  INSERT INTO public.exercise_library_equipment (exercise_library_id, equipment_type_id)
  SELECT keep_id, e.equipment_type_id
  FROM public.exercise_library_equipment e
  WHERE e.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  -- (c) repoint non-junction FK references
  UPDATE public.trial_assessment_items
  SET exercise_library_id = keep_id
  WHERE exercise_library_id = absorb_id;

  -- (d) delete absorb row (its remaining junction rows cascade)
  DELETE FROM public.exercise_library WHERE id = absorb_id;
  RAISE NOTICE 'dedup72: merged EX0433 -> EX0272 (dual rope tricep extension kneeling)';
END $$;

DO $$
DECLARE
  keep_id uuid;
  absorb_id uuid;
BEGIN
  SELECT id INTO keep_id FROM public.exercise_library WHERE code = 'EX0273';
  SELECT id INTO absorb_id FROM public.exercise_library WHERE code = 'EX0434';
  IF keep_id IS NULL OR absorb_id IS NULL THEN
    RAISE NOTICE 'dedup72: SKIP EX0273/EX0434 (row missing — already merged or renamed)';
    RETURN;
  END IF;

  -- (a) metadata merge: fill NULL/empty keep content columns from absorb; never overwrite
  UPDATE public.exercise_library k
  SET
    primary_muscle   = COALESCE(NULLIF(k.primary_muscle, ''), a.primary_muscle),
    secondary_muscle = COALESCE(NULLIF(k.secondary_muscle, ''), a.secondary_muscle),
    equipment        = COALESCE(NULLIF(k.equipment, ''), a.equipment),
    difficulty       = COALESCE(k.difficulty, a.difficulty),
    exercise_type    = COALESCE(NULLIF(k.exercise_type, ''), a.exercise_type),
    type             = COALESCE(NULLIF(k.type, ''), a.type),
    met_value        = COALESCE(k.met_value, a.met_value),
    description      = COALESCE(NULLIF(k.description, ''), a.description),
    safety_notes     = COALESCE(NULLIF(k.safety_notes, ''), a.safety_notes),
    youtube_url      = COALESCE(NULLIF(k.youtube_url, ''), a.youtube_url),
    image_url        = COALESCE(NULLIF(k.image_url, ''), a.image_url),
    updated_at       = now()
  FROM public.exercise_library a
  WHERE k.id = keep_id AND a.id = absorb_id;

  -- (b) junction merge: unique absorb tags survive (composite PKs dedupe)
  INSERT INTO public.exercise_library_muscles (exercise_library_id, muscle_name, is_primary)
  SELECT keep_id, m.muscle_name, m.is_primary
  FROM public.exercise_library_muscles m
  WHERE m.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  INSERT INTO public.exercise_library_equipment (exercise_library_id, equipment_type_id)
  SELECT keep_id, e.equipment_type_id
  FROM public.exercise_library_equipment e
  WHERE e.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  -- (c) repoint non-junction FK references
  UPDATE public.trial_assessment_items
  SET exercise_library_id = keep_id
  WHERE exercise_library_id = absorb_id;

  -- (d) delete absorb row (its remaining junction rows cascade)
  DELETE FROM public.exercise_library WHERE id = absorb_id;
  RAISE NOTICE 'dedup72: merged EX0434 -> EX0273 (dual rope tricep extension standing)';
END $$;

DO $$
DECLARE
  keep_id uuid;
  absorb_id uuid;
BEGIN
  SELECT id INTO keep_id FROM public.exercise_library WHERE code = 'EX0274';
  SELECT id INTO absorb_id FROM public.exercise_library WHERE code = 'EX0417';
  IF keep_id IS NULL OR absorb_id IS NULL THEN
    RAISE NOTICE 'dedup72: SKIP EX0274/EX0417 (row missing — already merged or renamed)';
    RETURN;
  END IF;

  -- (a) metadata merge: fill NULL/empty keep content columns from absorb; never overwrite
  UPDATE public.exercise_library k
  SET
    primary_muscle   = COALESCE(NULLIF(k.primary_muscle, ''), a.primary_muscle),
    secondary_muscle = COALESCE(NULLIF(k.secondary_muscle, ''), a.secondary_muscle),
    equipment        = COALESCE(NULLIF(k.equipment, ''), a.equipment),
    difficulty       = COALESCE(k.difficulty, a.difficulty),
    exercise_type    = COALESCE(NULLIF(k.exercise_type, ''), a.exercise_type),
    type             = COALESCE(NULLIF(k.type, ''), a.type),
    met_value        = COALESCE(k.met_value, a.met_value),
    description      = COALESCE(NULLIF(k.description, ''), a.description),
    safety_notes     = COALESCE(NULLIF(k.safety_notes, ''), a.safety_notes),
    youtube_url      = COALESCE(NULLIF(k.youtube_url, ''), a.youtube_url),
    image_url        = COALESCE(NULLIF(k.image_url, ''), a.image_url),
    updated_at       = now()
  FROM public.exercise_library a
  WHERE k.id = keep_id AND a.id = absorb_id;

  -- (b) junction merge: unique absorb tags survive (composite PKs dedupe)
  INSERT INTO public.exercise_library_muscles (exercise_library_id, muscle_name, is_primary)
  SELECT keep_id, m.muscle_name, m.is_primary
  FROM public.exercise_library_muscles m
  WHERE m.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  INSERT INTO public.exercise_library_equipment (exercise_library_id, equipment_type_id)
  SELECT keep_id, e.equipment_type_id
  FROM public.exercise_library_equipment e
  WHERE e.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  -- (c) repoint non-junction FK references
  UPDATE public.trial_assessment_items
  SET exercise_library_id = keep_id
  WHERE exercise_library_id = absorb_id;

  -- (d) delete absorb row (its remaining junction rows cascade)
  DELETE FROM public.exercise_library WHERE id = absorb_id;
  RAISE NOTICE 'dedup72: merged EX0417 -> EX0274 (ez bar preacher curl)';
END $$;

DO $$
DECLARE
  keep_id uuid;
  absorb_id uuid;
BEGIN
  SELECT id INTO keep_id FROM public.exercise_library WHERE code = 'EX0275';
  SELECT id INTO absorb_id FROM public.exercise_library WHERE code = 'EX0419';
  IF keep_id IS NULL OR absorb_id IS NULL THEN
    RAISE NOTICE 'dedup72: SKIP EX0275/EX0419 (row missing — already merged or renamed)';
    RETURN;
  END IF;

  -- (a) metadata merge: fill NULL/empty keep content columns from absorb; never overwrite
  UPDATE public.exercise_library k
  SET
    primary_muscle   = COALESCE(NULLIF(k.primary_muscle, ''), a.primary_muscle),
    secondary_muscle = COALESCE(NULLIF(k.secondary_muscle, ''), a.secondary_muscle),
    equipment        = COALESCE(NULLIF(k.equipment, ''), a.equipment),
    difficulty       = COALESCE(k.difficulty, a.difficulty),
    exercise_type    = COALESCE(NULLIF(k.exercise_type, ''), a.exercise_type),
    type             = COALESCE(NULLIF(k.type, ''), a.type),
    met_value        = COALESCE(k.met_value, a.met_value),
    description      = COALESCE(NULLIF(k.description, ''), a.description),
    safety_notes     = COALESCE(NULLIF(k.safety_notes, ''), a.safety_notes),
    youtube_url      = COALESCE(NULLIF(k.youtube_url, ''), a.youtube_url),
    image_url        = COALESCE(NULLIF(k.image_url, ''), a.image_url),
    updated_at       = now()
  FROM public.exercise_library a
  WHERE k.id = keep_id AND a.id = absorb_id;

  -- (b) junction merge: unique absorb tags survive (composite PKs dedupe)
  INSERT INTO public.exercise_library_muscles (exercise_library_id, muscle_name, is_primary)
  SELECT keep_id, m.muscle_name, m.is_primary
  FROM public.exercise_library_muscles m
  WHERE m.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  INSERT INTO public.exercise_library_equipment (exercise_library_id, equipment_type_id)
  SELECT keep_id, e.equipment_type_id
  FROM public.exercise_library_equipment e
  WHERE e.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  -- (c) repoint non-junction FK references
  UPDATE public.trial_assessment_items
  SET exercise_library_id = keep_id
  WHERE exercise_library_id = absorb_id;

  -- (d) delete absorb row (its remaining junction rows cascade)
  DELETE FROM public.exercise_library WHERE id = absorb_id;
  RAISE NOTICE 'dedup72: merged EX0419 -> EX0275 (ez bar scott curl)';
END $$;

DO $$
DECLARE
  keep_id uuid;
  absorb_id uuid;
BEGIN
  SELECT id INTO keep_id FROM public.exercise_library WHERE code = 'EX0276';
  SELECT id INTO absorb_id FROM public.exercise_library WHERE code = 'EX0342';
  IF keep_id IS NULL OR absorb_id IS NULL THEN
    RAISE NOTICE 'dedup72: SKIP EX0276/EX0342 (row missing — already merged or renamed)';
    RETURN;
  END IF;

  -- (a) metadata merge: fill NULL/empty keep content columns from absorb; never overwrite
  UPDATE public.exercise_library k
  SET
    primary_muscle   = COALESCE(NULLIF(k.primary_muscle, ''), a.primary_muscle),
    secondary_muscle = COALESCE(NULLIF(k.secondary_muscle, ''), a.secondary_muscle),
    equipment        = COALESCE(NULLIF(k.equipment, ''), a.equipment),
    difficulty       = COALESCE(k.difficulty, a.difficulty),
    exercise_type    = COALESCE(NULLIF(k.exercise_type, ''), a.exercise_type),
    type             = COALESCE(NULLIF(k.type, ''), a.type),
    met_value        = COALESCE(k.met_value, a.met_value),
    description      = COALESCE(NULLIF(k.description, ''), a.description),
    safety_notes     = COALESCE(NULLIF(k.safety_notes, ''), a.safety_notes),
    youtube_url      = COALESCE(NULLIF(k.youtube_url, ''), a.youtube_url),
    image_url        = COALESCE(NULLIF(k.image_url, ''), a.image_url),
    updated_at       = now()
  FROM public.exercise_library a
  WHERE k.id = keep_id AND a.id = absorb_id;

  -- (b) junction merge: unique absorb tags survive (composite PKs dedupe)
  INSERT INTO public.exercise_library_muscles (exercise_library_id, muscle_name, is_primary)
  SELECT keep_id, m.muscle_name, m.is_primary
  FROM public.exercise_library_muscles m
  WHERE m.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  INSERT INTO public.exercise_library_equipment (exercise_library_id, equipment_type_id)
  SELECT keep_id, e.equipment_type_id
  FROM public.exercise_library_equipment e
  WHERE e.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  -- (c) repoint non-junction FK references
  UPDATE public.trial_assessment_items
  SET exercise_library_id = keep_id
  WHERE exercise_library_id = absorb_id;

  -- (d) delete absorb row (its remaining junction rows cascade)
  DELETE FROM public.exercise_library WHERE id = absorb_id;
  RAISE NOTICE 'dedup72: merged EX0342 -> EX0276 (explosive sled row)';
END $$;

DO $$
DECLARE
  keep_id uuid;
  absorb_id uuid;
BEGIN
  SELECT id INTO keep_id FROM public.exercise_library WHERE code = 'EX0278';
  SELECT id INTO absorb_id FROM public.exercise_library WHERE code = 'EX0344';
  IF keep_id IS NULL OR absorb_id IS NULL THEN
    RAISE NOTICE 'dedup72: SKIP EX0278/EX0344 (row missing — already merged or renamed)';
    RETURN;
  END IF;

  -- (a) metadata merge: fill NULL/empty keep content columns from absorb; never overwrite
  UPDATE public.exercise_library k
  SET
    primary_muscle   = COALESCE(NULLIF(k.primary_muscle, ''), a.primary_muscle),
    secondary_muscle = COALESCE(NULLIF(k.secondary_muscle, ''), a.secondary_muscle),
    equipment        = COALESCE(NULLIF(k.equipment, ''), a.equipment),
    difficulty       = COALESCE(k.difficulty, a.difficulty),
    exercise_type    = COALESCE(NULLIF(k.exercise_type, ''), a.exercise_type),
    type             = COALESCE(NULLIF(k.type, ''), a.type),
    met_value        = COALESCE(k.met_value, a.met_value),
    description      = COALESCE(NULLIF(k.description, ''), a.description),
    safety_notes     = COALESCE(NULLIF(k.safety_notes, ''), a.safety_notes),
    youtube_url      = COALESCE(NULLIF(k.youtube_url, ''), a.youtube_url),
    image_url        = COALESCE(NULLIF(k.image_url, ''), a.image_url),
    updated_at       = now()
  FROM public.exercise_library a
  WHERE k.id = keep_id AND a.id = absorb_id;

  -- (b) junction merge: unique absorb tags survive (composite PKs dedupe)
  INSERT INTO public.exercise_library_muscles (exercise_library_id, muscle_name, is_primary)
  SELECT keep_id, m.muscle_name, m.is_primary
  FROM public.exercise_library_muscles m
  WHERE m.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  INSERT INTO public.exercise_library_equipment (exercise_library_id, equipment_type_id)
  SELECT keep_id, e.equipment_type_id
  FROM public.exercise_library_equipment e
  WHERE e.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  -- (c) repoint non-junction FK references
  UPDATE public.trial_assessment_items
  SET exercise_library_id = keep_id
  WHERE exercise_library_id = absorb_id;

  -- (d) delete absorb row (its remaining junction rows cascade)
  DELETE FROM public.exercise_library WHERE id = absorb_id;
  RAISE NOTICE 'dedup72: merged EX0344 -> EX0278 (flat leg raise)';
END $$;

DO $$
DECLARE
  keep_id uuid;
  absorb_id uuid;
BEGIN
  SELECT id INTO keep_id FROM public.exercise_library WHERE code = 'EX0278';
  SELECT id INTO absorb_id FROM public.exercise_library WHERE code = 'EX0378';
  IF keep_id IS NULL OR absorb_id IS NULL THEN
    RAISE NOTICE 'dedup72: SKIP EX0278/EX0378 (row missing — already merged or renamed)';
    RETURN;
  END IF;

  -- (a) metadata merge: fill NULL/empty keep content columns from absorb; never overwrite
  UPDATE public.exercise_library k
  SET
    primary_muscle   = COALESCE(NULLIF(k.primary_muscle, ''), a.primary_muscle),
    secondary_muscle = COALESCE(NULLIF(k.secondary_muscle, ''), a.secondary_muscle),
    equipment        = COALESCE(NULLIF(k.equipment, ''), a.equipment),
    difficulty       = COALESCE(k.difficulty, a.difficulty),
    exercise_type    = COALESCE(NULLIF(k.exercise_type, ''), a.exercise_type),
    type             = COALESCE(NULLIF(k.type, ''), a.type),
    met_value        = COALESCE(k.met_value, a.met_value),
    description      = COALESCE(NULLIF(k.description, ''), a.description),
    safety_notes     = COALESCE(NULLIF(k.safety_notes, ''), a.safety_notes),
    youtube_url      = COALESCE(NULLIF(k.youtube_url, ''), a.youtube_url),
    image_url        = COALESCE(NULLIF(k.image_url, ''), a.image_url),
    updated_at       = now()
  FROM public.exercise_library a
  WHERE k.id = keep_id AND a.id = absorb_id;

  -- (b) junction merge: unique absorb tags survive (composite PKs dedupe)
  INSERT INTO public.exercise_library_muscles (exercise_library_id, muscle_name, is_primary)
  SELECT keep_id, m.muscle_name, m.is_primary
  FROM public.exercise_library_muscles m
  WHERE m.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  INSERT INTO public.exercise_library_equipment (exercise_library_id, equipment_type_id)
  SELECT keep_id, e.equipment_type_id
  FROM public.exercise_library_equipment e
  WHERE e.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  -- (c) repoint non-junction FK references
  UPDATE public.trial_assessment_items
  SET exercise_library_id = keep_id
  WHERE exercise_library_id = absorb_id;

  -- (d) delete absorb row (its remaining junction rows cascade)
  DELETE FROM public.exercise_library WHERE id = absorb_id;
  RAISE NOTICE 'dedup72: merged EX0378 -> EX0278 (flat leg raise)';
END $$;

DO $$
DECLARE
  keep_id uuid;
  absorb_id uuid;
BEGIN
  SELECT id INTO keep_id FROM public.exercise_library WHERE code = 'EX0279';
  SELECT id INTO absorb_id FROM public.exercise_library WHERE code = 'EX0345';
  IF keep_id IS NULL OR absorb_id IS NULL THEN
    RAISE NOTICE 'dedup72: SKIP EX0279/EX0345 (row missing — already merged or renamed)';
    RETURN;
  END IF;

  -- (a) metadata merge: fill NULL/empty keep content columns from absorb; never overwrite
  UPDATE public.exercise_library k
  SET
    primary_muscle   = COALESCE(NULLIF(k.primary_muscle, ''), a.primary_muscle),
    secondary_muscle = COALESCE(NULLIF(k.secondary_muscle, ''), a.secondary_muscle),
    equipment        = COALESCE(NULLIF(k.equipment, ''), a.equipment),
    difficulty       = COALESCE(k.difficulty, a.difficulty),
    exercise_type    = COALESCE(NULLIF(k.exercise_type, ''), a.exercise_type),
    type             = COALESCE(NULLIF(k.type, ''), a.type),
    met_value        = COALESCE(k.met_value, a.met_value),
    description      = COALESCE(NULLIF(k.description, ''), a.description),
    safety_notes     = COALESCE(NULLIF(k.safety_notes, ''), a.safety_notes),
    youtube_url      = COALESCE(NULLIF(k.youtube_url, ''), a.youtube_url),
    image_url        = COALESCE(NULLIF(k.image_url, ''), a.image_url),
    updated_at       = now()
  FROM public.exercise_library a
  WHERE k.id = keep_id AND a.id = absorb_id;

  -- (b) junction merge: unique absorb tags survive (composite PKs dedupe)
  INSERT INTO public.exercise_library_muscles (exercise_library_id, muscle_name, is_primary)
  SELECT keep_id, m.muscle_name, m.is_primary
  FROM public.exercise_library_muscles m
  WHERE m.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  INSERT INTO public.exercise_library_equipment (exercise_library_id, equipment_type_id)
  SELECT keep_id, e.equipment_type_id
  FROM public.exercise_library_equipment e
  WHERE e.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  -- (c) repoint non-junction FK references
  UPDATE public.trial_assessment_items
  SET exercise_library_id = keep_id
  WHERE exercise_library_id = absorb_id;

  -- (d) delete absorb row (its remaining junction rows cascade)
  DELETE FROM public.exercise_library WHERE id = absorb_id;
  RAISE NOTICE 'dedup72: merged EX0345 -> EX0279 (floor crunch)';
END $$;

DO $$
DECLARE
  keep_id uuid;
  absorb_id uuid;
BEGIN
  SELECT id INTO keep_id FROM public.exercise_library WHERE code = 'EX0279';
  SELECT id INTO absorb_id FROM public.exercise_library WHERE code = 'EX0379';
  IF keep_id IS NULL OR absorb_id IS NULL THEN
    RAISE NOTICE 'dedup72: SKIP EX0279/EX0379 (row missing — already merged or renamed)';
    RETURN;
  END IF;

  -- (a) metadata merge: fill NULL/empty keep content columns from absorb; never overwrite
  UPDATE public.exercise_library k
  SET
    primary_muscle   = COALESCE(NULLIF(k.primary_muscle, ''), a.primary_muscle),
    secondary_muscle = COALESCE(NULLIF(k.secondary_muscle, ''), a.secondary_muscle),
    equipment        = COALESCE(NULLIF(k.equipment, ''), a.equipment),
    difficulty       = COALESCE(k.difficulty, a.difficulty),
    exercise_type    = COALESCE(NULLIF(k.exercise_type, ''), a.exercise_type),
    type             = COALESCE(NULLIF(k.type, ''), a.type),
    met_value        = COALESCE(k.met_value, a.met_value),
    description      = COALESCE(NULLIF(k.description, ''), a.description),
    safety_notes     = COALESCE(NULLIF(k.safety_notes, ''), a.safety_notes),
    youtube_url      = COALESCE(NULLIF(k.youtube_url, ''), a.youtube_url),
    image_url        = COALESCE(NULLIF(k.image_url, ''), a.image_url),
    updated_at       = now()
  FROM public.exercise_library a
  WHERE k.id = keep_id AND a.id = absorb_id;

  -- (b) junction merge: unique absorb tags survive (composite PKs dedupe)
  INSERT INTO public.exercise_library_muscles (exercise_library_id, muscle_name, is_primary)
  SELECT keep_id, m.muscle_name, m.is_primary
  FROM public.exercise_library_muscles m
  WHERE m.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  INSERT INTO public.exercise_library_equipment (exercise_library_id, equipment_type_id)
  SELECT keep_id, e.equipment_type_id
  FROM public.exercise_library_equipment e
  WHERE e.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  -- (c) repoint non-junction FK references
  UPDATE public.trial_assessment_items
  SET exercise_library_id = keep_id
  WHERE exercise_library_id = absorb_id;

  -- (d) delete absorb row (its remaining junction rows cascade)
  DELETE FROM public.exercise_library WHERE id = absorb_id;
  RAISE NOTICE 'dedup72: merged EX0379 -> EX0279 (floor crunch)';
END $$;

DO $$
DECLARE
  keep_id uuid;
  absorb_id uuid;
BEGIN
  SELECT id INTO keep_id FROM public.exercise_library WHERE code = 'EX0281';
  SELECT id INTO absorb_id FROM public.exercise_library WHERE code = 'EX0346';
  IF keep_id IS NULL OR absorb_id IS NULL THEN
    RAISE NOTICE 'dedup72: SKIP EX0281/EX0346 (row missing — already merged or renamed)';
    RETURN;
  END IF;

  -- (a) metadata merge: fill NULL/empty keep content columns from absorb; never overwrite
  UPDATE public.exercise_library k
  SET
    primary_muscle   = COALESCE(NULLIF(k.primary_muscle, ''), a.primary_muscle),
    secondary_muscle = COALESCE(NULLIF(k.secondary_muscle, ''), a.secondary_muscle),
    equipment        = COALESCE(NULLIF(k.equipment, ''), a.equipment),
    difficulty       = COALESCE(k.difficulty, a.difficulty),
    exercise_type    = COALESCE(NULLIF(k.exercise_type, ''), a.exercise_type),
    type             = COALESCE(NULLIF(k.type, ''), a.type),
    met_value        = COALESCE(k.met_value, a.met_value),
    description      = COALESCE(NULLIF(k.description, ''), a.description),
    safety_notes     = COALESCE(NULLIF(k.safety_notes, ''), a.safety_notes),
    youtube_url      = COALESCE(NULLIF(k.youtube_url, ''), a.youtube_url),
    image_url        = COALESCE(NULLIF(k.image_url, ''), a.image_url),
    updated_at       = now()
  FROM public.exercise_library a
  WHERE k.id = keep_id AND a.id = absorb_id;

  -- (b) junction merge: unique absorb tags survive (composite PKs dedupe)
  INSERT INTO public.exercise_library_muscles (exercise_library_id, muscle_name, is_primary)
  SELECT keep_id, m.muscle_name, m.is_primary
  FROM public.exercise_library_muscles m
  WHERE m.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  INSERT INTO public.exercise_library_equipment (exercise_library_id, equipment_type_id)
  SELECT keep_id, e.equipment_type_id
  FROM public.exercise_library_equipment e
  WHERE e.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  -- (c) repoint non-junction FK references
  UPDATE public.trial_assessment_items
  SET exercise_library_id = keep_id
  WHERE exercise_library_id = absorb_id;

  -- (d) delete absorb row (its remaining junction rows cascade)
  DELETE FROM public.exercise_library WHERE id = absorb_id;
  RAISE NOTICE 'dedup72: merged EX0346 -> EX0281 (forward sled drag)';
END $$;

DO $$
DECLARE
  keep_id uuid;
  absorb_id uuid;
BEGIN
  SELECT id INTO keep_id FROM public.exercise_library WHERE code = 'EX0282';
  SELECT id INTO absorb_id FROM public.exercise_library WHERE code = 'EX0347';
  IF keep_id IS NULL OR absorb_id IS NULL THEN
    RAISE NOTICE 'dedup72: SKIP EX0282/EX0347 (row missing — already merged or renamed)';
    RETURN;
  END IF;

  -- (a) metadata merge: fill NULL/empty keep content columns from absorb; never overwrite
  UPDATE public.exercise_library k
  SET
    primary_muscle   = COALESCE(NULLIF(k.primary_muscle, ''), a.primary_muscle),
    secondary_muscle = COALESCE(NULLIF(k.secondary_muscle, ''), a.secondary_muscle),
    equipment        = COALESCE(NULLIF(k.equipment, ''), a.equipment),
    difficulty       = COALESCE(k.difficulty, a.difficulty),
    exercise_type    = COALESCE(NULLIF(k.exercise_type, ''), a.exercise_type),
    type             = COALESCE(NULLIF(k.type, ''), a.type),
    met_value        = COALESCE(k.met_value, a.met_value),
    description      = COALESCE(NULLIF(k.description, ''), a.description),
    safety_notes     = COALESCE(NULLIF(k.safety_notes, ''), a.safety_notes),
    youtube_url      = COALESCE(NULLIF(k.youtube_url, ''), a.youtube_url),
    image_url        = COALESCE(NULLIF(k.image_url, ''), a.image_url),
    updated_at       = now()
  FROM public.exercise_library a
  WHERE k.id = keep_id AND a.id = absorb_id;

  -- (b) junction merge: unique absorb tags survive (composite PKs dedupe)
  INSERT INTO public.exercise_library_muscles (exercise_library_id, muscle_name, is_primary)
  SELECT keep_id, m.muscle_name, m.is_primary
  FROM public.exercise_library_muscles m
  WHERE m.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  INSERT INTO public.exercise_library_equipment (exercise_library_id, equipment_type_id)
  SELECT keep_id, e.equipment_type_id
  FROM public.exercise_library_equipment e
  WHERE e.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  -- (c) repoint non-junction FK references
  UPDATE public.trial_assessment_items
  SET exercise_library_id = keep_id
  WHERE exercise_library_id = absorb_id;

  -- (d) delete absorb row (its remaining junction rows cascade)
  DELETE FROM public.exercise_library WHERE id = absorb_id;
  RAISE NOTICE 'dedup72: merged EX0347 -> EX0282 (front plank)';
END $$;

DO $$
DECLARE
  keep_id uuid;
  absorb_id uuid;
BEGIN
  SELECT id INTO keep_id FROM public.exercise_library WHERE code = 'EX0282';
  SELECT id INTO absorb_id FROM public.exercise_library WHERE code = 'EX0380';
  IF keep_id IS NULL OR absorb_id IS NULL THEN
    RAISE NOTICE 'dedup72: SKIP EX0282/EX0380 (row missing — already merged or renamed)';
    RETURN;
  END IF;

  -- (a) metadata merge: fill NULL/empty keep content columns from absorb; never overwrite
  UPDATE public.exercise_library k
  SET
    primary_muscle   = COALESCE(NULLIF(k.primary_muscle, ''), a.primary_muscle),
    secondary_muscle = COALESCE(NULLIF(k.secondary_muscle, ''), a.secondary_muscle),
    equipment        = COALESCE(NULLIF(k.equipment, ''), a.equipment),
    difficulty       = COALESCE(k.difficulty, a.difficulty),
    exercise_type    = COALESCE(NULLIF(k.exercise_type, ''), a.exercise_type),
    type             = COALESCE(NULLIF(k.type, ''), a.type),
    met_value        = COALESCE(k.met_value, a.met_value),
    description      = COALESCE(NULLIF(k.description, ''), a.description),
    safety_notes     = COALESCE(NULLIF(k.safety_notes, ''), a.safety_notes),
    youtube_url      = COALESCE(NULLIF(k.youtube_url, ''), a.youtube_url),
    image_url        = COALESCE(NULLIF(k.image_url, ''), a.image_url),
    updated_at       = now()
  FROM public.exercise_library a
  WHERE k.id = keep_id AND a.id = absorb_id;

  -- (b) junction merge: unique absorb tags survive (composite PKs dedupe)
  INSERT INTO public.exercise_library_muscles (exercise_library_id, muscle_name, is_primary)
  SELECT keep_id, m.muscle_name, m.is_primary
  FROM public.exercise_library_muscles m
  WHERE m.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  INSERT INTO public.exercise_library_equipment (exercise_library_id, equipment_type_id)
  SELECT keep_id, e.equipment_type_id
  FROM public.exercise_library_equipment e
  WHERE e.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  -- (c) repoint non-junction FK references
  UPDATE public.trial_assessment_items
  SET exercise_library_id = keep_id
  WHERE exercise_library_id = absorb_id;

  -- (d) delete absorb row (its remaining junction rows cascade)
  DELETE FROM public.exercise_library WHERE id = absorb_id;
  RAISE NOTICE 'dedup72: merged EX0380 -> EX0282 (front plank)';
END $$;

DO $$
DECLARE
  keep_id uuid;
  absorb_id uuid;
BEGIN
  SELECT id INTO keep_id FROM public.exercise_library WHERE code = 'EX0283';
  SELECT id INTO absorb_id FROM public.exercise_library WHERE code = 'EX0421';
  IF keep_id IS NULL OR absorb_id IS NULL THEN
    RAISE NOTICE 'dedup72: SKIP EX0283/EX0421 (row missing — already merged or renamed)';
    RETURN;
  END IF;

  -- (a) metadata merge: fill NULL/empty keep content columns from absorb; never overwrite
  UPDATE public.exercise_library k
  SET
    primary_muscle   = COALESCE(NULLIF(k.primary_muscle, ''), a.primary_muscle),
    secondary_muscle = COALESCE(NULLIF(k.secondary_muscle, ''), a.secondary_muscle),
    equipment        = COALESCE(NULLIF(k.equipment, ''), a.equipment),
    difficulty       = COALESCE(k.difficulty, a.difficulty),
    exercise_type    = COALESCE(NULLIF(k.exercise_type, ''), a.exercise_type),
    type             = COALESCE(NULLIF(k.type, ''), a.type),
    met_value        = COALESCE(k.met_value, a.met_value),
    description      = COALESCE(NULLIF(k.description, ''), a.description),
    safety_notes     = COALESCE(NULLIF(k.safety_notes, ''), a.safety_notes),
    youtube_url      = COALESCE(NULLIF(k.youtube_url, ''), a.youtube_url),
    image_url        = COALESCE(NULLIF(k.image_url, ''), a.image_url),
    updated_at       = now()
  FROM public.exercise_library a
  WHERE k.id = keep_id AND a.id = absorb_id;

  -- (b) junction merge: unique absorb tags survive (composite PKs dedupe)
  INSERT INTO public.exercise_library_muscles (exercise_library_id, muscle_name, is_primary)
  SELECT keep_id, m.muscle_name, m.is_primary
  FROM public.exercise_library_muscles m
  WHERE m.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  INSERT INTO public.exercise_library_equipment (exercise_library_id, equipment_type_id)
  SELECT keep_id, e.equipment_type_id
  FROM public.exercise_library_equipment e
  WHERE e.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  -- (c) repoint non-junction FK references
  UPDATE public.trial_assessment_items
  SET exercise_library_id = keep_id
  WHERE exercise_library_id = absorb_id;

  -- (d) delete absorb row (its remaining junction rows cascade)
  DELETE FROM public.exercise_library WHERE id = absorb_id;
  RAISE NOTICE 'dedup72: merged EX0421 -> EX0283 (gironda drag back curl)';
END $$;

DO $$
DECLARE
  keep_id uuid;
  absorb_id uuid;
BEGIN
  SELECT id INTO keep_id FROM public.exercise_library WHERE code = 'EX0285';
  SELECT id INTO absorb_id FROM public.exercise_library WHERE code = 'EX0348';
  IF keep_id IS NULL OR absorb_id IS NULL THEN
    RAISE NOTICE 'dedup72: SKIP EX0285/EX0348 (row missing — already merged or renamed)';
    RETURN;
  END IF;

  -- (a) metadata merge: fill NULL/empty keep content columns from absorb; never overwrite
  UPDATE public.exercise_library k
  SET
    primary_muscle   = COALESCE(NULLIF(k.primary_muscle, ''), a.primary_muscle),
    secondary_muscle = COALESCE(NULLIF(k.secondary_muscle, ''), a.secondary_muscle),
    equipment        = COALESCE(NULLIF(k.equipment, ''), a.equipment),
    difficulty       = COALESCE(k.difficulty, a.difficulty),
    exercise_type    = COALESCE(NULLIF(k.exercise_type, ''), a.exercise_type),
    type             = COALESCE(NULLIF(k.type, ''), a.type),
    met_value        = COALESCE(k.met_value, a.met_value),
    description      = COALESCE(NULLIF(k.description, ''), a.description),
    safety_notes     = COALESCE(NULLIF(k.safety_notes, ''), a.safety_notes),
    youtube_url      = COALESCE(NULLIF(k.youtube_url, ''), a.youtube_url),
    image_url        = COALESCE(NULLIF(k.image_url, ''), a.image_url),
    updated_at       = now()
  FROM public.exercise_library a
  WHERE k.id = keep_id AND a.id = absorb_id;

  -- (b) junction merge: unique absorb tags survive (composite PKs dedupe)
  INSERT INTO public.exercise_library_muscles (exercise_library_id, muscle_name, is_primary)
  SELECT keep_id, m.muscle_name, m.is_primary
  FROM public.exercise_library_muscles m
  WHERE m.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  INSERT INTO public.exercise_library_equipment (exercise_library_id, equipment_type_id)
  SELECT keep_id, e.equipment_type_id
  FROM public.exercise_library_equipment e
  WHERE e.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  -- (c) repoint non-junction FK references
  UPDATE public.trial_assessment_items
  SET exercise_library_id = keep_id
  WHERE exercise_library_id = absorb_id;

  -- (d) delete absorb row (its remaining junction rows cascade)
  DELETE FROM public.exercise_library WHERE id = absorb_id;
  RAISE NOTICE 'dedup72: merged EX0348 -> EX0285 (hanging leg raise)';
END $$;

DO $$
DECLARE
  keep_id uuid;
  absorb_id uuid;
BEGIN
  SELECT id INTO keep_id FROM public.exercise_library WHERE code = 'EX0285';
  SELECT id INTO absorb_id FROM public.exercise_library WHERE code = 'EX0381';
  IF keep_id IS NULL OR absorb_id IS NULL THEN
    RAISE NOTICE 'dedup72: SKIP EX0285/EX0381 (row missing — already merged or renamed)';
    RETURN;
  END IF;

  -- (a) metadata merge: fill NULL/empty keep content columns from absorb; never overwrite
  UPDATE public.exercise_library k
  SET
    primary_muscle   = COALESCE(NULLIF(k.primary_muscle, ''), a.primary_muscle),
    secondary_muscle = COALESCE(NULLIF(k.secondary_muscle, ''), a.secondary_muscle),
    equipment        = COALESCE(NULLIF(k.equipment, ''), a.equipment),
    difficulty       = COALESCE(k.difficulty, a.difficulty),
    exercise_type    = COALESCE(NULLIF(k.exercise_type, ''), a.exercise_type),
    type             = COALESCE(NULLIF(k.type, ''), a.type),
    met_value        = COALESCE(k.met_value, a.met_value),
    description      = COALESCE(NULLIF(k.description, ''), a.description),
    safety_notes     = COALESCE(NULLIF(k.safety_notes, ''), a.safety_notes),
    youtube_url      = COALESCE(NULLIF(k.youtube_url, ''), a.youtube_url),
    image_url        = COALESCE(NULLIF(k.image_url, ''), a.image_url),
    updated_at       = now()
  FROM public.exercise_library a
  WHERE k.id = keep_id AND a.id = absorb_id;

  -- (b) junction merge: unique absorb tags survive (composite PKs dedupe)
  INSERT INTO public.exercise_library_muscles (exercise_library_id, muscle_name, is_primary)
  SELECT keep_id, m.muscle_name, m.is_primary
  FROM public.exercise_library_muscles m
  WHERE m.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  INSERT INTO public.exercise_library_equipment (exercise_library_id, equipment_type_id)
  SELECT keep_id, e.equipment_type_id
  FROM public.exercise_library_equipment e
  WHERE e.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  -- (c) repoint non-junction FK references
  UPDATE public.trial_assessment_items
  SET exercise_library_id = keep_id
  WHERE exercise_library_id = absorb_id;

  -- (d) delete absorb row (its remaining junction rows cascade)
  DELETE FROM public.exercise_library WHERE id = absorb_id;
  RAISE NOTICE 'dedup72: merged EX0381 -> EX0285 (hanging leg raise)';
END $$;

DO $$
DECLARE
  keep_id uuid;
  absorb_id uuid;
BEGIN
  SELECT id INTO keep_id FROM public.exercise_library WHERE code = 'EX0295';
  SELECT id INTO absorb_id FROM public.exercise_library WHERE code = 'EX0436';
  IF keep_id IS NULL OR absorb_id IS NULL THEN
    RAISE NOTICE 'dedup72: SKIP EX0295/EX0436 (row missing — already merged or renamed)';
    RETURN;
  END IF;

  -- (a) metadata merge: fill NULL/empty keep content columns from absorb; never overwrite
  UPDATE public.exercise_library k
  SET
    primary_muscle   = COALESCE(NULLIF(k.primary_muscle, ''), a.primary_muscle),
    secondary_muscle = COALESCE(NULLIF(k.secondary_muscle, ''), a.secondary_muscle),
    equipment        = COALESCE(NULLIF(k.equipment, ''), a.equipment),
    difficulty       = COALESCE(k.difficulty, a.difficulty),
    exercise_type    = COALESCE(NULLIF(k.exercise_type, ''), a.exercise_type),
    type             = COALESCE(NULLIF(k.type, ''), a.type),
    met_value        = COALESCE(k.met_value, a.met_value),
    description      = COALESCE(NULLIF(k.description, ''), a.description),
    safety_notes     = COALESCE(NULLIF(k.safety_notes, ''), a.safety_notes),
    youtube_url      = COALESCE(NULLIF(k.youtube_url, ''), a.youtube_url),
    image_url        = COALESCE(NULLIF(k.image_url, ''), a.image_url),
    updated_at       = now()
  FROM public.exercise_library a
  WHERE k.id = keep_id AND a.id = absorb_id;

  -- (b) junction merge: unique absorb tags survive (composite PKs dedupe)
  INSERT INTO public.exercise_library_muscles (exercise_library_id, muscle_name, is_primary)
  SELECT keep_id, m.muscle_name, m.is_primary
  FROM public.exercise_library_muscles m
  WHERE m.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  INSERT INTO public.exercise_library_equipment (exercise_library_id, equipment_type_id)
  SELECT keep_id, e.equipment_type_id
  FROM public.exercise_library_equipment e
  WHERE e.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  -- (c) repoint non-junction FK references
  UPDATE public.trial_assessment_items
  SET exercise_library_id = keep_id
  WHERE exercise_library_id = absorb_id;

  -- (d) delete absorb row (its remaining junction rows cascade)
  DELETE FROM public.exercise_library WHERE id = absorb_id;
  RAISE NOTICE 'dedup72: merged EX0436 -> EX0295 (overhead extension single arm)';
END $$;

DO $$
DECLARE
  keep_id uuid;
  absorb_id uuid;
BEGIN
  SELECT id INTO keep_id FROM public.exercise_library WHERE code = 'EX0297';
  SELECT id INTO absorb_id FROM public.exercise_library WHERE code = 'EX0349';
  IF keep_id IS NULL OR absorb_id IS NULL THEN
    RAISE NOTICE 'dedup72: SKIP EX0297/EX0349 (row missing — already merged or renamed)';
    RETURN;
  END IF;

  -- (a) metadata merge: fill NULL/empty keep content columns from absorb; never overwrite
  UPDATE public.exercise_library k
  SET
    primary_muscle   = COALESCE(NULLIF(k.primary_muscle, ''), a.primary_muscle),
    secondary_muscle = COALESCE(NULLIF(k.secondary_muscle, ''), a.secondary_muscle),
    equipment        = COALESCE(NULLIF(k.equipment, ''), a.equipment),
    difficulty       = COALESCE(k.difficulty, a.difficulty),
    exercise_type    = COALESCE(NULLIF(k.exercise_type, ''), a.exercise_type),
    type             = COALESCE(NULLIF(k.type, ''), a.type),
    met_value        = COALESCE(k.met_value, a.met_value),
    description      = COALESCE(NULLIF(k.description, ''), a.description),
    safety_notes     = COALESCE(NULLIF(k.safety_notes, ''), a.safety_notes),
    youtube_url      = COALESCE(NULLIF(k.youtube_url, ''), a.youtube_url),
    image_url        = COALESCE(NULLIF(k.image_url, ''), a.image_url),
    updated_at       = now()
  FROM public.exercise_library a
  WHERE k.id = keep_id AND a.id = absorb_id;

  -- (b) junction merge: unique absorb tags survive (composite PKs dedupe)
  INSERT INTO public.exercise_library_muscles (exercise_library_id, muscle_name, is_primary)
  SELECT keep_id, m.muscle_name, m.is_primary
  FROM public.exercise_library_muscles m
  WHERE m.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  INSERT INTO public.exercise_library_equipment (exercise_library_id, equipment_type_id)
  SELECT keep_id, e.equipment_type_id
  FROM public.exercise_library_equipment e
  WHERE e.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  -- (c) repoint non-junction FK references
  UPDATE public.trial_assessment_items
  SET exercise_library_id = keep_id
  WHERE exercise_library_id = absorb_id;

  -- (d) delete absorb row (its remaining junction rows cascade)
  DELETE FROM public.exercise_library WHERE id = absorb_id;
  RAISE NOTICE 'dedup72: merged EX0349 -> EX0297 (parallel bar leg raise)';
END $$;

DO $$
DECLARE
  keep_id uuid;
  absorb_id uuid;
BEGIN
  SELECT id INTO keep_id FROM public.exercise_library WHERE code = 'EX0297';
  SELECT id INTO absorb_id FROM public.exercise_library WHERE code = 'EX0382';
  IF keep_id IS NULL OR absorb_id IS NULL THEN
    RAISE NOTICE 'dedup72: SKIP EX0297/EX0382 (row missing — already merged or renamed)';
    RETURN;
  END IF;

  -- (a) metadata merge: fill NULL/empty keep content columns from absorb; never overwrite
  UPDATE public.exercise_library k
  SET
    primary_muscle   = COALESCE(NULLIF(k.primary_muscle, ''), a.primary_muscle),
    secondary_muscle = COALESCE(NULLIF(k.secondary_muscle, ''), a.secondary_muscle),
    equipment        = COALESCE(NULLIF(k.equipment, ''), a.equipment),
    difficulty       = COALESCE(k.difficulty, a.difficulty),
    exercise_type    = COALESCE(NULLIF(k.exercise_type, ''), a.exercise_type),
    type             = COALESCE(NULLIF(k.type, ''), a.type),
    met_value        = COALESCE(k.met_value, a.met_value),
    description      = COALESCE(NULLIF(k.description, ''), a.description),
    safety_notes     = COALESCE(NULLIF(k.safety_notes, ''), a.safety_notes),
    youtube_url      = COALESCE(NULLIF(k.youtube_url, ''), a.youtube_url),
    image_url        = COALESCE(NULLIF(k.image_url, ''), a.image_url),
    updated_at       = now()
  FROM public.exercise_library a
  WHERE k.id = keep_id AND a.id = absorb_id;

  -- (b) junction merge: unique absorb tags survive (composite PKs dedupe)
  INSERT INTO public.exercise_library_muscles (exercise_library_id, muscle_name, is_primary)
  SELECT keep_id, m.muscle_name, m.is_primary
  FROM public.exercise_library_muscles m
  WHERE m.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  INSERT INTO public.exercise_library_equipment (exercise_library_id, equipment_type_id)
  SELECT keep_id, e.equipment_type_id
  FROM public.exercise_library_equipment e
  WHERE e.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  -- (c) repoint non-junction FK references
  UPDATE public.trial_assessment_items
  SET exercise_library_id = keep_id
  WHERE exercise_library_id = absorb_id;

  -- (d) delete absorb row (its remaining junction rows cascade)
  DELETE FROM public.exercise_library WHERE id = absorb_id;
  RAISE NOTICE 'dedup72: merged EX0382 -> EX0297 (parallel bar leg raise)';
END $$;

DO $$
DECLARE
  keep_id uuid;
  absorb_id uuid;
BEGIN
  SELECT id INTO keep_id FROM public.exercise_library WHERE code = 'EX0300';
  SELECT id INTO absorb_id FROM public.exercise_library WHERE code = 'EX0350';
  IF keep_id IS NULL OR absorb_id IS NULL THEN
    RAISE NOTICE 'dedup72: SKIP EX0300/EX0350 (row missing — already merged or renamed)';
    RETURN;
  END IF;

  -- (a) metadata merge: fill NULL/empty keep content columns from absorb; never overwrite
  UPDATE public.exercise_library k
  SET
    primary_muscle   = COALESCE(NULLIF(k.primary_muscle, ''), a.primary_muscle),
    secondary_muscle = COALESCE(NULLIF(k.secondary_muscle, ''), a.secondary_muscle),
    equipment        = COALESCE(NULLIF(k.equipment, ''), a.equipment),
    difficulty       = COALESCE(k.difficulty, a.difficulty),
    exercise_type    = COALESCE(NULLIF(k.exercise_type, ''), a.exercise_type),
    type             = COALESCE(NULLIF(k.type, ''), a.type),
    met_value        = COALESCE(k.met_value, a.met_value),
    description      = COALESCE(NULLIF(k.description, ''), a.description),
    safety_notes     = COALESCE(NULLIF(k.safety_notes, ''), a.safety_notes),
    youtube_url      = COALESCE(NULLIF(k.youtube_url, ''), a.youtube_url),
    image_url        = COALESCE(NULLIF(k.image_url, ''), a.image_url),
    updated_at       = now()
  FROM public.exercise_library a
  WHERE k.id = keep_id AND a.id = absorb_id;

  -- (b) junction merge: unique absorb tags survive (composite PKs dedupe)
  INSERT INTO public.exercise_library_muscles (exercise_library_id, muscle_name, is_primary)
  SELECT keep_id, m.muscle_name, m.is_primary
  FROM public.exercise_library_muscles m
  WHERE m.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  INSERT INTO public.exercise_library_equipment (exercise_library_id, equipment_type_id)
  SELECT keep_id, e.equipment_type_id
  FROM public.exercise_library_equipment e
  WHERE e.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  -- (c) repoint non-junction FK references
  UPDATE public.trial_assessment_items
  SET exercise_library_id = keep_id
  WHERE exercise_library_id = absorb_id;

  -- (d) delete absorb row (its remaining junction rows cascade)
  DELETE FROM public.exercise_library WHERE id = absorb_id;
  RAISE NOTICE 'dedup72: merged EX0350 -> EX0300 (prowler sprint)';
END $$;

DO $$
DECLARE
  keep_id uuid;
  absorb_id uuid;
BEGIN
  SELECT id INTO keep_id FROM public.exercise_library WHERE code = 'EX0313';
  SELECT id INTO absorb_id FROM public.exercise_library WHERE code = 'EX0362';
  IF keep_id IS NULL OR absorb_id IS NULL THEN
    RAISE NOTICE 'dedup72: SKIP EX0313/EX0362 (row missing — already merged or renamed)';
    RETURN;
  END IF;

  -- (a) metadata merge: fill NULL/empty keep content columns from absorb; never overwrite
  UPDATE public.exercise_library k
  SET
    primary_muscle   = COALESCE(NULLIF(k.primary_muscle, ''), a.primary_muscle),
    secondary_muscle = COALESCE(NULLIF(k.secondary_muscle, ''), a.secondary_muscle),
    equipment        = COALESCE(NULLIF(k.equipment, ''), a.equipment),
    difficulty       = COALESCE(k.difficulty, a.difficulty),
    exercise_type    = COALESCE(NULLIF(k.exercise_type, ''), a.exercise_type),
    type             = COALESCE(NULLIF(k.type, ''), a.type),
    met_value        = COALESCE(k.met_value, a.met_value),
    description      = COALESCE(NULLIF(k.description, ''), a.description),
    safety_notes     = COALESCE(NULLIF(k.safety_notes, ''), a.safety_notes),
    youtube_url      = COALESCE(NULLIF(k.youtube_url, ''), a.youtube_url),
    image_url        = COALESCE(NULLIF(k.image_url, ''), a.image_url),
    updated_at       = now()
  FROM public.exercise_library a
  WHERE k.id = keep_id AND a.id = absorb_id;

  -- (b) junction merge: unique absorb tags survive (composite PKs dedupe)
  INSERT INTO public.exercise_library_muscles (exercise_library_id, muscle_name, is_primary)
  SELECT keep_id, m.muscle_name, m.is_primary
  FROM public.exercise_library_muscles m
  WHERE m.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  INSERT INTO public.exercise_library_equipment (exercise_library_id, equipment_type_id)
  SELECT keep_id, e.equipment_type_id
  FROM public.exercise_library_equipment e
  WHERE e.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  -- (c) repoint non-junction FK references
  UPDATE public.trial_assessment_items
  SET exercise_library_id = keep_id
  WHERE exercise_library_id = absorb_id;

  -- (d) delete absorb row (its remaining junction rows cascade)
  DELETE FROM public.exercise_library WHERE id = absorb_id;
  RAISE NOTICE 'dedup72: merged EX0362 -> EX0313 (seated sled pull)';
END $$;

DO $$
DECLARE
  keep_id uuid;
  absorb_id uuid;
BEGIN
  SELECT id INTO keep_id FROM public.exercise_library WHERE code = 'EX0315';
  SELECT id INTO absorb_id FROM public.exercise_library WHERE code = 'EX0385';
  IF keep_id IS NULL OR absorb_id IS NULL THEN
    RAISE NOTICE 'dedup72: SKIP EX0315/EX0385 (row missing — already merged or renamed)';
    RETURN;
  END IF;

  -- (a) metadata merge: fill NULL/empty keep content columns from absorb; never overwrite
  UPDATE public.exercise_library k
  SET
    primary_muscle   = COALESCE(NULLIF(k.primary_muscle, ''), a.primary_muscle),
    secondary_muscle = COALESCE(NULLIF(k.secondary_muscle, ''), a.secondary_muscle),
    equipment        = COALESCE(NULLIF(k.equipment, ''), a.equipment),
    difficulty       = COALESCE(k.difficulty, a.difficulty),
    exercise_type    = COALESCE(NULLIF(k.exercise_type, ''), a.exercise_type),
    type             = COALESCE(NULLIF(k.type, ''), a.type),
    met_value        = COALESCE(k.met_value, a.met_value),
    description      = COALESCE(NULLIF(k.description, ''), a.description),
    safety_notes     = COALESCE(NULLIF(k.safety_notes, ''), a.safety_notes),
    youtube_url      = COALESCE(NULLIF(k.youtube_url, ''), a.youtube_url),
    image_url        = COALESCE(NULLIF(k.image_url, ''), a.image_url),
    updated_at       = now()
  FROM public.exercise_library a
  WHERE k.id = keep_id AND a.id = absorb_id;

  -- (b) junction merge: unique absorb tags survive (composite PKs dedupe)
  INSERT INTO public.exercise_library_muscles (exercise_library_id, muscle_name, is_primary)
  SELECT keep_id, m.muscle_name, m.is_primary
  FROM public.exercise_library_muscles m
  WHERE m.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  INSERT INTO public.exercise_library_equipment (exercise_library_id, equipment_type_id)
  SELECT keep_id, e.equipment_type_id
  FROM public.exercise_library_equipment e
  WHERE e.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  -- (c) repoint non-junction FK references
  UPDATE public.trial_assessment_items
  SET exercise_library_id = keep_id
  WHERE exercise_library_id = absorb_id;

  -- (d) delete absorb row (its remaining junction rows cascade)
  DELETE FROM public.exercise_library WHERE id = absorb_id;
  RAISE NOTICE 'dedup72: merged EX0385 -> EX0315 (side plank feet)';
END $$;

DO $$
DECLARE
  keep_id uuid;
  absorb_id uuid;
BEGIN
  SELECT id INTO keep_id FROM public.exercise_library WHERE code = 'EX0323';
  SELECT id INTO absorb_id FROM public.exercise_library WHERE code = 'EX0354';
  IF keep_id IS NULL OR absorb_id IS NULL THEN
    RAISE NOTICE 'dedup72: SKIP EX0323/EX0354 (row missing — already merged or renamed)';
    RETURN;
  END IF;

  -- (a) metadata merge: fill NULL/empty keep content columns from absorb; never overwrite
  UPDATE public.exercise_library k
  SET
    primary_muscle   = COALESCE(NULLIF(k.primary_muscle, ''), a.primary_muscle),
    secondary_muscle = COALESCE(NULLIF(k.secondary_muscle, ''), a.secondary_muscle),
    equipment        = COALESCE(NULLIF(k.equipment, ''), a.equipment),
    difficulty       = COALESCE(k.difficulty, a.difficulty),
    exercise_type    = COALESCE(NULLIF(k.exercise_type, ''), a.exercise_type),
    type             = COALESCE(NULLIF(k.type, ''), a.type),
    met_value        = COALESCE(k.met_value, a.met_value),
    description      = COALESCE(NULLIF(k.description, ''), a.description),
    safety_notes     = COALESCE(NULLIF(k.safety_notes, ''), a.safety_notes),
    youtube_url      = COALESCE(NULLIF(k.youtube_url, ''), a.youtube_url),
    image_url        = COALESCE(NULLIF(k.image_url, ''), a.image_url),
    updated_at       = now()
  FROM public.exercise_library a
  WHERE k.id = keep_id AND a.id = absorb_id;

  -- (b) junction merge: unique absorb tags survive (composite PKs dedupe)
  INSERT INTO public.exercise_library_muscles (exercise_library_id, muscle_name, is_primary)
  SELECT keep_id, m.muscle_name, m.is_primary
  FROM public.exercise_library_muscles m
  WHERE m.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  INSERT INTO public.exercise_library_equipment (exercise_library_id, equipment_type_id)
  SELECT keep_id, e.equipment_type_id
  FROM public.exercise_library_equipment e
  WHERE e.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  -- (c) repoint non-junction FK references
  UPDATE public.trial_assessment_items
  SET exercise_library_id = keep_id
  WHERE exercise_library_id = absorb_id;

  -- (d) delete absorb row (its remaining junction rows cascade)
  DELETE FROM public.exercise_library WHERE id = absorb_id;
  RAISE NOTICE 'dedup72: merged EX0354 -> EX0323 (swiss ball crunch)';
END $$;

DO $$
DECLARE
  keep_id uuid;
  absorb_id uuid;
BEGIN
  SELECT id INTO keep_id FROM public.exercise_library WHERE code = 'EX0323';
  SELECT id INTO absorb_id FROM public.exercise_library WHERE code = 'EX0387';
  IF keep_id IS NULL OR absorb_id IS NULL THEN
    RAISE NOTICE 'dedup72: SKIP EX0323/EX0387 (row missing — already merged or renamed)';
    RETURN;
  END IF;

  -- (a) metadata merge: fill NULL/empty keep content columns from absorb; never overwrite
  UPDATE public.exercise_library k
  SET
    primary_muscle   = COALESCE(NULLIF(k.primary_muscle, ''), a.primary_muscle),
    secondary_muscle = COALESCE(NULLIF(k.secondary_muscle, ''), a.secondary_muscle),
    equipment        = COALESCE(NULLIF(k.equipment, ''), a.equipment),
    difficulty       = COALESCE(k.difficulty, a.difficulty),
    exercise_type    = COALESCE(NULLIF(k.exercise_type, ''), a.exercise_type),
    type             = COALESCE(NULLIF(k.type, ''), a.type),
    met_value        = COALESCE(k.met_value, a.met_value),
    description      = COALESCE(NULLIF(k.description, ''), a.description),
    safety_notes     = COALESCE(NULLIF(k.safety_notes, ''), a.safety_notes),
    youtube_url      = COALESCE(NULLIF(k.youtube_url, ''), a.youtube_url),
    image_url        = COALESCE(NULLIF(k.image_url, ''), a.image_url),
    updated_at       = now()
  FROM public.exercise_library a
  WHERE k.id = keep_id AND a.id = absorb_id;

  -- (b) junction merge: unique absorb tags survive (composite PKs dedupe)
  INSERT INTO public.exercise_library_muscles (exercise_library_id, muscle_name, is_primary)
  SELECT keep_id, m.muscle_name, m.is_primary
  FROM public.exercise_library_muscles m
  WHERE m.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  INSERT INTO public.exercise_library_equipment (exercise_library_id, equipment_type_id)
  SELECT keep_id, e.equipment_type_id
  FROM public.exercise_library_equipment e
  WHERE e.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  -- (c) repoint non-junction FK references
  UPDATE public.trial_assessment_items
  SET exercise_library_id = keep_id
  WHERE exercise_library_id = absorb_id;

  -- (d) delete absorb row (its remaining junction rows cascade)
  DELETE FROM public.exercise_library WHERE id = absorb_id;
  RAISE NOTICE 'dedup72: merged EX0387 -> EX0323 (swiss ball crunch)';
END $$;

DO $$
DECLARE
  keep_id uuid;
  absorb_id uuid;
BEGIN
  SELECT id INTO keep_id FROM public.exercise_library WHERE code = 'EX0324';
  SELECT id INTO absorb_id FROM public.exercise_library WHERE code = 'EX0355';
  IF keep_id IS NULL OR absorb_id IS NULL THEN
    RAISE NOTICE 'dedup72: SKIP EX0324/EX0355 (row missing — already merged or renamed)';
    RETURN;
  END IF;

  -- (a) metadata merge: fill NULL/empty keep content columns from absorb; never overwrite
  UPDATE public.exercise_library k
  SET
    primary_muscle   = COALESCE(NULLIF(k.primary_muscle, ''), a.primary_muscle),
    secondary_muscle = COALESCE(NULLIF(k.secondary_muscle, ''), a.secondary_muscle),
    equipment        = COALESCE(NULLIF(k.equipment, ''), a.equipment),
    difficulty       = COALESCE(k.difficulty, a.difficulty),
    exercise_type    = COALESCE(NULLIF(k.exercise_type, ''), a.exercise_type),
    type             = COALESCE(NULLIF(k.type, ''), a.type),
    met_value        = COALESCE(k.met_value, a.met_value),
    description      = COALESCE(NULLIF(k.description, ''), a.description),
    safety_notes     = COALESCE(NULLIF(k.safety_notes, ''), a.safety_notes),
    youtube_url      = COALESCE(NULLIF(k.youtube_url, ''), a.youtube_url),
    image_url        = COALESCE(NULLIF(k.image_url, ''), a.image_url),
    updated_at       = now()
  FROM public.exercise_library a
  WHERE k.id = keep_id AND a.id = absorb_id;

  -- (b) junction merge: unique absorb tags survive (composite PKs dedupe)
  INSERT INTO public.exercise_library_muscles (exercise_library_id, muscle_name, is_primary)
  SELECT keep_id, m.muscle_name, m.is_primary
  FROM public.exercise_library_muscles m
  WHERE m.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  INSERT INTO public.exercise_library_equipment (exercise_library_id, equipment_type_id)
  SELECT keep_id, e.equipment_type_id
  FROM public.exercise_library_equipment e
  WHERE e.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  -- (c) repoint non-junction FK references
  UPDATE public.trial_assessment_items
  SET exercise_library_id = keep_id
  WHERE exercise_library_id = absorb_id;

  -- (d) delete absorb row (its remaining junction rows cascade)
  DELETE FROM public.exercise_library WHERE id = absorb_id;
  RAISE NOTICE 'dedup72: merged EX0355 -> EX0324 (swiss ball stir the pot)';
END $$;

DO $$
DECLARE
  keep_id uuid;
  absorb_id uuid;
BEGIN
  SELECT id INTO keep_id FROM public.exercise_library WHERE code = 'EX0324';
  SELECT id INTO absorb_id FROM public.exercise_library WHERE code = 'EX0386';
  IF keep_id IS NULL OR absorb_id IS NULL THEN
    RAISE NOTICE 'dedup72: SKIP EX0324/EX0386 (row missing — already merged or renamed)';
    RETURN;
  END IF;

  -- (a) metadata merge: fill NULL/empty keep content columns from absorb; never overwrite
  UPDATE public.exercise_library k
  SET
    primary_muscle   = COALESCE(NULLIF(k.primary_muscle, ''), a.primary_muscle),
    secondary_muscle = COALESCE(NULLIF(k.secondary_muscle, ''), a.secondary_muscle),
    equipment        = COALESCE(NULLIF(k.equipment, ''), a.equipment),
    difficulty       = COALESCE(k.difficulty, a.difficulty),
    exercise_type    = COALESCE(NULLIF(k.exercise_type, ''), a.exercise_type),
    type             = COALESCE(NULLIF(k.type, ''), a.type),
    met_value        = COALESCE(k.met_value, a.met_value),
    description      = COALESCE(NULLIF(k.description, ''), a.description),
    safety_notes     = COALESCE(NULLIF(k.safety_notes, ''), a.safety_notes),
    youtube_url      = COALESCE(NULLIF(k.youtube_url, ''), a.youtube_url),
    image_url        = COALESCE(NULLIF(k.image_url, ''), a.image_url),
    updated_at       = now()
  FROM public.exercise_library a
  WHERE k.id = keep_id AND a.id = absorb_id;

  -- (b) junction merge: unique absorb tags survive (composite PKs dedupe)
  INSERT INTO public.exercise_library_muscles (exercise_library_id, muscle_name, is_primary)
  SELECT keep_id, m.muscle_name, m.is_primary
  FROM public.exercise_library_muscles m
  WHERE m.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  INSERT INTO public.exercise_library_equipment (exercise_library_id, equipment_type_id)
  SELECT keep_id, e.equipment_type_id
  FROM public.exercise_library_equipment e
  WHERE e.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  -- (c) repoint non-junction FK references
  UPDATE public.trial_assessment_items
  SET exercise_library_id = keep_id
  WHERE exercise_library_id = absorb_id;

  -- (d) delete absorb row (its remaining junction rows cascade)
  DELETE FROM public.exercise_library WHERE id = absorb_id;
  RAISE NOTICE 'dedup72: merged EX0386 -> EX0324 (swiss ball stir the pot)';
END $$;

DO $$
DECLARE
  keep_id uuid;
  absorb_id uuid;
BEGIN
  SELECT id INTO keep_id FROM public.exercise_library WHERE code = 'EX0327';
  SELECT id INTO absorb_id FROM public.exercise_library WHERE code = 'EX0356';
  IF keep_id IS NULL OR absorb_id IS NULL THEN
    RAISE NOTICE 'dedup72: SKIP EX0327/EX0356 (row missing — already merged or renamed)';
    RETURN;
  END IF;

  -- (a) metadata merge: fill NULL/empty keep content columns from absorb; never overwrite
  UPDATE public.exercise_library k
  SET
    primary_muscle   = COALESCE(NULLIF(k.primary_muscle, ''), a.primary_muscle),
    secondary_muscle = COALESCE(NULLIF(k.secondary_muscle, ''), a.secondary_muscle),
    equipment        = COALESCE(NULLIF(k.equipment, ''), a.equipment),
    difficulty       = COALESCE(k.difficulty, a.difficulty),
    exercise_type    = COALESCE(NULLIF(k.exercise_type, ''), a.exercise_type),
    type             = COALESCE(NULLIF(k.type, ''), a.type),
    met_value        = COALESCE(k.met_value, a.met_value),
    description      = COALESCE(NULLIF(k.description, ''), a.description),
    safety_notes     = COALESCE(NULLIF(k.safety_notes, ''), a.safety_notes),
    youtube_url      = COALESCE(NULLIF(k.youtube_url, ''), a.youtube_url),
    image_url        = COALESCE(NULLIF(k.image_url, ''), a.image_url),
    updated_at       = now()
  FROM public.exercise_library a
  WHERE k.id = keep_id AND a.id = absorb_id;

  -- (b) junction merge: unique absorb tags survive (composite PKs dedupe)
  INSERT INTO public.exercise_library_muscles (exercise_library_id, muscle_name, is_primary)
  SELECT keep_id, m.muscle_name, m.is_primary
  FROM public.exercise_library_muscles m
  WHERE m.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  INSERT INTO public.exercise_library_equipment (exercise_library_id, equipment_type_id)
  SELECT keep_id, e.equipment_type_id
  FROM public.exercise_library_equipment e
  WHERE e.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  -- (c) repoint non-junction FK references
  UPDATE public.trial_assessment_items
  SET exercise_library_id = keep_id
  WHERE exercise_library_id = absorb_id;

  -- (d) delete absorb row (its remaining junction rows cascade)
  DELETE FROM public.exercise_library WHERE id = absorb_id;
  RAISE NOTICE 'dedup72: merged EX0356 -> EX0327 (waiters walk)';
END $$;

DO $$
DECLARE
  keep_id uuid;
  absorb_id uuid;
BEGIN
  SELECT id INTO keep_id FROM public.exercise_library WHERE code = 'EX0329';
  SELECT id INTO absorb_id FROM public.exercise_library WHERE code = 'EX0357';
  IF keep_id IS NULL OR absorb_id IS NULL THEN
    RAISE NOTICE 'dedup72: SKIP EX0329/EX0357 (row missing — already merged or renamed)';
    RETURN;
  END IF;

  -- (a) metadata merge: fill NULL/empty keep content columns from absorb; never overwrite
  UPDATE public.exercise_library k
  SET
    primary_muscle   = COALESCE(NULLIF(k.primary_muscle, ''), a.primary_muscle),
    secondary_muscle = COALESCE(NULLIF(k.secondary_muscle, ''), a.secondary_muscle),
    equipment        = COALESCE(NULLIF(k.equipment, ''), a.equipment),
    difficulty       = COALESCE(k.difficulty, a.difficulty),
    exercise_type    = COALESCE(NULLIF(k.exercise_type, ''), a.exercise_type),
    type             = COALESCE(NULLIF(k.type, ''), a.type),
    met_value        = COALESCE(k.met_value, a.met_value),
    description      = COALESCE(NULLIF(k.description, ''), a.description),
    safety_notes     = COALESCE(NULLIF(k.safety_notes, ''), a.safety_notes),
    youtube_url      = COALESCE(NULLIF(k.youtube_url, ''), a.youtube_url),
    image_url        = COALESCE(NULLIF(k.image_url, ''), a.image_url),
    updated_at       = now()
  FROM public.exercise_library a
  WHERE k.id = keep_id AND a.id = absorb_id;

  -- (b) junction merge: unique absorb tags survive (composite PKs dedupe)
  INSERT INTO public.exercise_library_muscles (exercise_library_id, muscle_name, is_primary)
  SELECT keep_id, m.muscle_name, m.is_primary
  FROM public.exercise_library_muscles m
  WHERE m.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  INSERT INTO public.exercise_library_equipment (exercise_library_id, equipment_type_id)
  SELECT keep_id, e.equipment_type_id
  FROM public.exercise_library_equipment e
  WHERE e.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  -- (c) repoint non-junction FK references
  UPDATE public.trial_assessment_items
  SET exercise_library_id = keep_id
  WHERE exercise_library_id = absorb_id;

  -- (d) delete absorb row (its remaining junction rows cascade)
  DELETE FROM public.exercise_library WHERE id = absorb_id;
  RAISE NOTICE 'dedup72: merged EX0357 -> EX0329 (weighted cable crunch)';
END $$;

DO $$
DECLARE
  keep_id uuid;
  absorb_id uuid;
BEGIN
  SELECT id INTO keep_id FROM public.exercise_library WHERE code = 'EX0329';
  SELECT id INTO absorb_id FROM public.exercise_library WHERE code = 'EX0389';
  IF keep_id IS NULL OR absorb_id IS NULL THEN
    RAISE NOTICE 'dedup72: SKIP EX0329/EX0389 (row missing — already merged or renamed)';
    RETURN;
  END IF;

  -- (a) metadata merge: fill NULL/empty keep content columns from absorb; never overwrite
  UPDATE public.exercise_library k
  SET
    primary_muscle   = COALESCE(NULLIF(k.primary_muscle, ''), a.primary_muscle),
    secondary_muscle = COALESCE(NULLIF(k.secondary_muscle, ''), a.secondary_muscle),
    equipment        = COALESCE(NULLIF(k.equipment, ''), a.equipment),
    difficulty       = COALESCE(k.difficulty, a.difficulty),
    exercise_type    = COALESCE(NULLIF(k.exercise_type, ''), a.exercise_type),
    type             = COALESCE(NULLIF(k.type, ''), a.type),
    met_value        = COALESCE(k.met_value, a.met_value),
    description      = COALESCE(NULLIF(k.description, ''), a.description),
    safety_notes     = COALESCE(NULLIF(k.safety_notes, ''), a.safety_notes),
    youtube_url      = COALESCE(NULLIF(k.youtube_url, ''), a.youtube_url),
    image_url        = COALESCE(NULLIF(k.image_url, ''), a.image_url),
    updated_at       = now()
  FROM public.exercise_library a
  WHERE k.id = keep_id AND a.id = absorb_id;

  -- (b) junction merge: unique absorb tags survive (composite PKs dedupe)
  INSERT INTO public.exercise_library_muscles (exercise_library_id, muscle_name, is_primary)
  SELECT keep_id, m.muscle_name, m.is_primary
  FROM public.exercise_library_muscles m
  WHERE m.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  INSERT INTO public.exercise_library_equipment (exercise_library_id, equipment_type_id)
  SELECT keep_id, e.equipment_type_id
  FROM public.exercise_library_equipment e
  WHERE e.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  -- (c) repoint non-junction FK references
  UPDATE public.trial_assessment_items
  SET exercise_library_id = keep_id
  WHERE exercise_library_id = absorb_id;

  -- (d) delete absorb row (its remaining junction rows cascade)
  DELETE FROM public.exercise_library WHERE id = absorb_id;
  RAISE NOTICE 'dedup72: merged EX0389 -> EX0329 (weighted cable crunch)';
END $$;

DO $$
DECLARE
  keep_id uuid;
  absorb_id uuid;
BEGIN
  SELECT id INTO keep_id FROM public.exercise_library WHERE code = 'EX0333';
  SELECT id INTO absorb_id FROM public.exercise_library WHERE code = 'EX0359';
  IF keep_id IS NULL OR absorb_id IS NULL THEN
    RAISE NOTICE 'dedup72: SKIP EX0333/EX0359 (row missing — already merged or renamed)';
    RETURN;
  END IF;

  -- (a) metadata merge: fill NULL/empty keep content columns from absorb; never overwrite
  UPDATE public.exercise_library k
  SET
    primary_muscle   = COALESCE(NULLIF(k.primary_muscle, ''), a.primary_muscle),
    secondary_muscle = COALESCE(NULLIF(k.secondary_muscle, ''), a.secondary_muscle),
    equipment        = COALESCE(NULLIF(k.equipment, ''), a.equipment),
    difficulty       = COALESCE(k.difficulty, a.difficulty),
    exercise_type    = COALESCE(NULLIF(k.exercise_type, ''), a.exercise_type),
    type             = COALESCE(NULLIF(k.type, ''), a.type),
    met_value        = COALESCE(k.met_value, a.met_value),
    description      = COALESCE(NULLIF(k.description, ''), a.description),
    safety_notes     = COALESCE(NULLIF(k.safety_notes, ''), a.safety_notes),
    youtube_url      = COALESCE(NULLIF(k.youtube_url, ''), a.youtube_url),
    image_url        = COALESCE(NULLIF(k.image_url, ''), a.image_url),
    updated_at       = now()
  FROM public.exercise_library a
  WHERE k.id = keep_id AND a.id = absorb_id;

  -- (b) junction merge: unique absorb tags survive (composite PKs dedupe)
  INSERT INTO public.exercise_library_muscles (exercise_library_id, muscle_name, is_primary)
  SELECT keep_id, m.muscle_name, m.is_primary
  FROM public.exercise_library_muscles m
  WHERE m.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  INSERT INTO public.exercise_library_equipment (exercise_library_id, equipment_type_id)
  SELECT keep_id, e.equipment_type_id
  FROM public.exercise_library_equipment e
  WHERE e.exercise_library_id = absorb_id
  ON CONFLICT DO NOTHING;

  -- (c) repoint non-junction FK references
  UPDATE public.trial_assessment_items
  SET exercise_library_id = keep_id
  WHERE exercise_library_id = absorb_id;

  -- (d) delete absorb row (its remaining junction rows cascade)
  DELETE FROM public.exercise_library WHERE id = absorb_id;
  RAISE NOTICE 'dedup72: merged EX0359 -> EX0333 (zercher carry)';
END $$;
COMMIT;
